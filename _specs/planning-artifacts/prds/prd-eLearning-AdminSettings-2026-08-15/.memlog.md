---
topic: Admin Settings & Runtime UI Configuration (incl. Course Wizard button relocation)
updated: 2026-08-16T01:36
---

- (decision) Working mode: coaching path, entry point: Vision + Features (capability-first, admin/internal-facing feature)
- (decision) Access scope: Admin Settings visible to Master + Support roles (mirrors Tag Management access level)
- (decision) Font setting UI is a curated dropdown of pre-approved font pairings, not free text -- respects existing DESIGN.md rule against introducing a second serif/sans-serif family
- (decision) Settings table (Key/Value/KeyType/IsActive) is designed generically from v1 to hold any future site-wide setting type, not just fonts; v1 UI exposes only font/typography controls
- (decision) Safety net: settings changes require preview-before-apply, plus per-setting change history (who changed what, when); IsActive remains as an on/off toggle per setting row
- (event) Draft prd.md (FR-1 through FR-17) and addendum.md written for review
- (change) Removed concurrent-edit conflict detection FR (was FR-9) per user request -- kept simple, documented as last-write-wins in new NFR-5. Renumbered FR-9 through FR-17 down to FR-9 through FR-16.
- (decision) One-click restore-from-history (FR-16) confirmed as firm v1 scope, not an assumption -- user explicitly requested it
- (change) Reviewer gate (rubric + edge-case-hunter + adversarial-general) run in parallel; applied all 9 recommended fixes: font-pairing made atomic (single row, new FR-11), added server-side curation enforcement (new FR-10), fixed FR-9 addendum overclaim, added nav-anchor preservation to FR-1, added Success Metrics data-source line, added access-level rationale to 4.2, tied restore (FR-17) to curation check, fixed FR-15 old-Value freshness, broadened NFR-4. Renumbered FR-10..FR-16 to FR-11..FR-17.
- (decision) FR-12 assumption confirmed as firm scope: fonts not already loaded via index.html are out of v1 scope. Assumption tag removed.
- (event) Polish complete: structural pass consolidated addendum open-items; prose pass fixed one hyphenation inconsistency (Tutor-Approvals-tier). No external handoffs configured. PRD finalized.
- (change) Corrected FR-4 and 4.2's access-level precedent: 'same as Tag Management' was factually wrong (Tag Management is Master-only, FeatureKeys.MasterDataManage) -- fixed during the follow-on bmad-architecture pass to cite Tutor Approvals instead, the actual existing Master+Support precedent (confirmed via RolePermissionSeedData.cs, FeatureKeys.TutorApprove seeded for both Master and Support). The Master+Support access REQUIREMENT itself was already correct; only the justifying comparison was wrong. Ties to backend architecture spine's new AD-27.
- (change) Amended PRD: added Font Size as a second, independent typography setting (FR-18 curated size-scale picker, FR-19 server-side curation enforcement, FR-20 independence from Font Pairing, FR-21 reuse of existing Preview/Apply/History/runtime-application mechanisms) plus NFR-6 (graceful sizing via design-QA curation, no runtime clamping). Triggered by user follow-up request after Epic 6 (Font Pairing) shipped to review. Updated Glossary, UJ section, Open Items (new [NOTE FOR PM] for curated size-scale list content).
- (change) Applied Reviewer Gate findings (rubric walker + adversarial-general, both converged on the same critical gap): switched Font Size roles from an invented heading/body/UI-text taxonomy to the identical Display/Body/Mono roles Font Pairing already uses; added FR-22 (atomicity guarantee mirroring FR-11) since Font Size has the same multi-role-collapse risk three reviewers already caught once for Font Pairing; reworded NFR-6 as an explicit pre-launch process gate (not a testable runtime behavior) and added combinatorial vetting (each Font Size scale checked against every curated Font Pairing, not in isolation); tagged FR-21's runtime-application claim as [ASSUMPTION] pending verification of how font-size is actually expressed in index.css/Tailwind; generalized FR-17's hardcoded 'FR-10' reference; added a Non-Goals subsection; refreshed addendum.md's now-stale dev-handoff grounding to point at Setting/SettingsService/SettingsController/FontPairingDefinition (already built in Story 6.1/6.2) instead of pre-implementation precedents, and resolved the 'curated list storage mechanism' open question (separate reference table, confirmed by existing code) rather than leaving it open.
- (decision) Triaged remaining reviewer findings: mechanical notes (KeyType naming asymmetry Font/FontSize, Glossary term layering) accepted as-is per reviewer's own 'reads as intentional' assessment, no fix needed. Strategic-coherence 'two bundled features' finding addressed with one clarifying sentence in Doc Purpose rather than splitting into two PRDs -- the bundling is a scoping convenience (same sprint), not a hidden dependency, and both halves already have independent, complete FR sets.
- (event) PRD amendment finalized: Font Size setting type (FR-18-FR-22, NFR-6) added. status remains final, updated=2026-08-15.
- (decision) Story 6.4 implementation research verified the FR-21 [ASSUMPTION] and found it FALSE: index.css has zero font-size CSS custom properties (only --font-display/--font-sans/--font-mono for family), no tailwind.config override exists, Tailwind's default rem-based text-* scale is in full effect. There is no natural Display/Body/Mono role split for font SIZE the way there is for font FAMILY -- text sizing varies per-element through Tailwind's granular scale, not three semantic buckets. Redesigned Font Size as a single proportional root-scale factor (one new --root-font-scale CSS custom property on html{font-size}, scaling every rem-based text-* utility site-wide) instead of the originally-assumed three-role atomic structure. Amended: Glossary's Font Size Scale definition, FR-18/FR-21/FR-22 rewritten (FR-22 now explains why no multi-role atomicity concern exists, rather than restating FR-11's fix), NFR-6 extended to require QA across real screens not just the preview surface, new Non-Goal + Open Item for Tailwind arbitrary-px classes (e.g. text-[10px]) not responding to this setting. This is a correction of an analogy-based assumption from the earlier reviewer-gate pass (which reasoned by pattern-matching to Font Pairing's Display/Body/Mono shape without checking the actual CSS mechanism available), not a new requirement.
