---
topic: New Course Wizard (v2) — AI-Assisted Course Creation & Adaptive Learning Module
updated: 2026-08-15T11:43
---

- (event) User pasted a full draft PRD (v2, dated 2026-08-10) as the brain dump: AI-assisted course wizard + adaptive learning module, ~12 sections, pluggable AI backbone as primary focus
- (note) Run folder disambiguated from default pattern: prd-eLearning-2026-08-10 already exists (status: final, unrelated topic — Dashboard merge); used prd-eLearning-CourseWizard-2026-08-10 instead, following the same precedent as prd-eLearning-Assignments-2026-08-10
- (note) Explore-agent scan of repo: Dashboard PRD's FR-18 IS the existing 4-step Course Creation Wizard prototype (FrontEnd/src/features/Dashboard/TutorEducatorHubView.tsx) that this PRD supersedes/extends; CourseUploadAsset type stub (types.ts:268-280) already shapes AI-extraction results (analysisStatus, extractedChapters) but is unwired; DrilldownPanel.tsx already implements a working 5-level drilldown UI (mock data) as prior art; ReaderCanvas.tsx has a fake/simulated LLM assistant, no real gateway; no AI/LLM gateway exists anywhere in the codebase; backend architecture spine explicitly deferred the AI pipeline as out of scope; taxonomy (Country/State/City/Board/Class/Subject) already has a real backend-integrated admin system via MasterDataManager — Step 3 of this wizard can reuse it; Course.tags today is a free-text string array with no master-data backing, so admin tag management is genuinely new work
- (note) PRD convention in this repo (from Dashboard + Assignments PRDs): sections 0 Document Purpose, 1 Vision, 2 Target User (JTBD/Non-Users/Key User Journeys), 3 Glossary, 4 Features (FRs), 5 Non-Goals, 6 MVP Scope, 7 Success Metrics, 8 Open Questions, 9 Assumptions Index. FR numbering restarts at FR-1 per doc; cross-doc refs cite explicitly e.g. 'Dashboard PRD's FR-18'
- (decision by user) Stakes: launch-grade (full rigor). Working mode: Fast path
- (decision by user) This PRD supersedes Dashboard PRD's FR-18 (existing 4-step Course Creation Wizard prototype, client-only, flat Module->Lesson, no AI)
- (decision by user) Reuse existing UI/systems: taxonomy step wires into existing MasterDataManager admin system (Country->State->City->Board->Class->Subject); drill-down feature wires AI generation behind existing DrilldownPanel.tsx UI instead of new component. Tags remain net-new (no master data exists for tags today)
- (decision by user) Dev-phase AI gateway: managed gateway (OpenRouter/Portkey) now for fast iteration, migrate to self-hosted LiteLLM at/before launch for data residency and cost. Free-tier dev usage restricted to providers that do not train on input (e.g. Groq) whenever real-ish student content is involved; Google AI Studio free tier flagged synthetic-data-only due to data-training terms
- (decision by user) Generation timing locked: pre-generate 5-level drill-down and 5-way alternative explanations at publish time (and re-publish after edits), cache, serve instantly to students -- was an open question in user's original draft, now decided per launch-grade rigor
- (note) External research digest (Aug 2026): LiteLLM (self-hosted) vs OpenRouter/Portkey (managed) as the two dominant OpenAI-compatible gateway patterns; Groq offers genuine no-card free tier that does not train on data (best privacy fit); Google AI Studio free tier trains on input outside EU/UK/EEA; document extraction needs a dedicated parsing/OCR step (LlamaParse/Docling/DeepSeek-OCR) ahead of the LLM outline call, not a raw LLM call, especially for scanned PDFs; cheap-tier models (GPT-5 Nano, Gemini 2.5 Flash-Lite, ~$0.10/M input) fit high-volume keyword definitions; frontier models (Claude Opus 4.6/4.8, GPT-5.5, Gemini 3.1 Pro) fit 5-level drill-down reasoning
- (event) Full draft PRD written to prd.md: 0 Document Purpose, 1 Vision, 2 Target User (JTBD/Non-Users/4 UJs), 3 Glossary, 4 Features (28 FRs across 14 subsections), 5 Non-Goals, 6 MVP Scope, 7 Success Metrics (5 SM + 2 counter-metrics), 8 Open Questions (7), 9 Assumptions Index (12 tagged assumptions). status still draft, awaiting user review
- (decision by user) Party review (BMM software-dev crew: Mary/John/Sally/Winston/Amelia) resolved 3 open items: (1) post-publish edit re-trigger rule = small-edit exception: text-only edit to an already-confirmed node keeps confirmation and skips re-review; structural edit or edit that regenerates cached Drill-Down/Way content resets confirmation and reverts to In Review, applies identically to first-publish and re-publish (FR-15, FR-24, FR-25 updated); (2) Five-Ways UX entry point = secondary menu relative to Drill-Down's primary action, but Level 1 must carry a visible nudge toward it, not a hidden menu (FR-18 Notes added, feeds bmad-ux); (3) Winston decided (user deferred): Publish is asynchronous via a transient Publishing sub-state with a background generation batch (FR-21); a single node's generation failure doesn't block the rest of the course, falls back to on-demand generation for that node (FR-21 updated)
- (event) Resolved and removed §8 Open Question 5 (post-publish review re-trigger) and its §9 assumption entries for FR-15/FR-25, now decided rather than open
- (decision by user) Resolved both parking-lot items from the party review: FR-16 commits to reusing existing KaTeX (already used in ReaderCanvas/DrilldownPanel/FlashcardsModal/CoursePlayer) + adding the mhchem extension (not yet installed) for chemistry, tested via golden-file/visual-regression diff; FR-9 commits to fixed-aspect-ratio client-side crop before thumbnail upload is accepted
- (event) Finalize step 1 (memlog audit): every prior memlog entry already reflected in prd.md. Split out the AI-gateway research digest (options considered, free-tier/pricing snapshot, repo prior-art file:line detail) into a new addendum.md since it's technical-how depth for downstream architecture, not PRD-body content; updated PRD's §0 and §8 Q3 to point at addendum.md instead of .memlog.md
- (decision by user) Finalize step 2 (input reconciliation) resolved 3 of 4 gaps found against original draft: (1) local/self-hosted model inference added back as a selectable AI Service Layer backend (FR-2), applies to embeddings too; (2) chemistry structural/skeletal diagrams added back to FR-16, handled as tutor-uploaded images like biology figures (KaTeX/mhchem covers formulas/reactions only, not structural diagrams); (3) budget guardrail added as new FR-29 (Admin AI Configuration) - threshold + warning + block/fallback on overage, not just SM-5 tracking. 4th gap (gateway 'not a product constraint' framing vs FR-2's committed two-phase posture) left as-is - already a conscious decision made earlier in session, just flagged not re-litigated
- (decision by user) Finalize step 3 (Reviewer Gate): ran rubric walker (review-rubric.md, 0 critical/2 high/4 medium/5 low) + ad-hoc architecture-feasibility reviewer (review-architecture-feasibility.md, 0 critical/2 high/3 medium/4 low). User resolved all 4 high findings one-by-one: FR-3 fallback now logs/flags degraded-model events via FR-4; ReaderCanvas.tsx's fake LLM assistant explicitly folded into FR-20 as its real implementation; FR-21 async publish batch flagged [NOTE FOR PM] as needing an architecture-phase job/worker infra decision; FR-2 gets a new consequence requiring the self-hosted phase's config store to support runtime updates so 'no redeploy' survives the phase-2 migration. Then walked all 15 remaining medium/low findings one-by-one (user explicitly chose this over batch auto-apply), all resolved with recommended fixes: Performance NFR numeric target, MVP scope phasing rationale, FR-12 OCR confidence-threshold gate, UJ-3 admin named 'Rohan', gateway migration-cost correction in addendum.md + Portability NFR, FR-16 golden-file CI [NOTE FOR PM], FR-25 versioning-storage [NOTE FOR PM] + retention [ASSUMPTION], self-host ops-cost trade named in Portability NFR, SM-C3 added, FR-11 access-control tied to FR-8's policy-gate pattern, Assumptions Index roundtrip fixed (UJ-1 + FR-9 entries), §4.6 DrilldownPanel reuse scope clarified to FR-17-only, FR-26 Tags stated as net-new (not a MasterDataManager plug-in), FR-29 concurrency-counter [NOTE FOR PM]
- (decision by user) Finalize step 4 (triage): resolved 2 phase-blocking open questions the rubric review flagged as gating FR acceptance criteria - State/City requirement is now board-dependent, decided per-board via master data (FR-8 updated, removed from §8/§9); v1 language scope confirmed as English+Hindi only, no other language committed for launch (FR-16 updated, removed from §8/§9). Remaining 4 open questions (exercise auto-grading, keyword-definition caching, final AI provider/model selection at build time, WCAG conformance level) deferred as genuine non-blockers - none gate a specific FR's buildability, all appropriately left for build-time/architecture decision. 4 [NOTE FOR PM] callouts (FR-16 visual-regression CI tooling, FR-21 async job/worker infra, FR-25 version-storage data modeling, FR-29 concurrency-safe budget counter) also deferred by design to the architecture pass - that is their intended purpose, not a gap
- (event by user) Finalize step 5 (polish): ran bmad-editorial-review-structure and bmad-editorial-review-prose on prd.md and addendum.md (structure before prose, per doc_standards). Structural fixes applied: added 'Publishing' transient sub-state to Glossary's Lifecycle State entry (was introduced in FR-21/FR-24 but never added to §3, violating the doc's own new-noun-to-Glossary discipline); moved addendum.md's misplaced Sources list from under 'Cost shape' to the end of the document since it sources all 4 research sections, not just the last one. Prose fixes applied: 3 minor grammar/clarity fixes in prd.md (missing preposition in FR-25 Notes, parallelism in §6 MVP rationale, awkward compound modifier in FR-11), 1 in addendum.md (mixed metaphor in opening sentence). External handoffs: none configured, step 6 skipped
- (event) PRD finalized
- (event by user) Post-finalization amendment from downstream UX accessibility review: added a 7th AI Task, describeNotation(), generating screen-reader alt-text for rendered math/chemistry notation -- was an unbacked accessibility claim in FR-16 with no generation mechanism. Updated §4.1 description, FR-16 consequences, FR-27's AI Task list, and the AI Task glossary entry. Also added explicit Hindi lang="hi" tagging requirement to FR-16. updated: bumped to 2026-08-11
- (decision by user) Post-finalization cost-optimization pass (triggered mid-epics-creation, user asked to revisit for free-tool substitutions): (1) AI gateway collapsed from managed-then-self-hosted phasing to self-hosted zero-markup OSS gateway (e.g. Portkey OSS, Apache-licensed) from day one -- OpenRouter's managed tier charges a real platform fee (5.5%/5%, free BYOK allotment now dollar-based ~$25k/month list-price not request-count), avoided entirely by self-hosting from the start since the OSS gateway is lightweight and just as fast to stand up. Updated FR-2, S4.14 Portability NFR, S0 assumption tag, S9 Assumptions Index. (2) OCR/parsing tool: Docling (free, MIT, self-hosted, real OCR via EasyOCR/Tesseract) chosen over LlamaParse (paid SaaS, 10k-credit free tier that shrinks with quality) -- accepted trade-off: weaker out-of-box accuracy on heavily degraded scans, mitigated by FR-12's existing confidence-threshold gate. (3) Malware scanning (FR-11, previously unnamed) filled with ClamAV (free, open-source, actively maintained) -- not a cost trade-off since no paid alternative was ever chosen, just naming a free default. Fonts (Fraunces/Outfit) confirmed already free, no change
- (change) Post-finalization amendment (2026-08-15) triggered by user report against the live implementation (FrontEnd/src/features/CourseContentEditor/CourseContentEditor.tsx is a fixed inset-0 full-viewport overlay covering the header, with a nested overflow-y-auto scroll container, and no UI to resume a saved Draft course): added FR-30 (Content Editor renders in the page body below the persistent header, page-level scroll instead of nested scroll -- refines UX-DR5, doesn't reopen it) and FR-31 (tutor can select a Draft course from their course-management area to reopen the Content Editor, picking up where they left off per FR-22) to S4.11. Amended FR-22's consequences to make explicit that the editor's own Close action commits to Draft state rather than discarding work. Updated SM-4's validates-list (+FR-31) and S9 Assumptions Index (+2 entries for FR-30/31's build-time specifics). updated bumped to 2026-08-15.
- (event) Amendment applied directly without a full re-run of Finalize (memlog audit/reconciliation/reviewer-gate), consistent with this doc's two prior post-finalization amendments -- scope is 2 new FRs + 1 amended consequence, self-consistent with the already-reviewed S4.11 lifecycle. status remains final.
- (decision) User resolved FR-30's open assumption (2026-08-15): site header stays sticky-top (existing, unchanged behavior); Content Editor's own header row (title+Close) becomes a second sticky bar directly beneath it, in a distinct background color from both the site header and the editor body; only the editor's body/content scrolls, not either header. Only the specific background color value remains open, deferred to bmad-ux/DESIGN.md.
- (decision) User resolved FR-31's open assumption (2026-08-15): the resume-a-course list shows ALL of the tutor's courses with a status badge (Draft/In Review/Review Confirmed/Published), not a Draft-only list. FR-31 and S9 Assumptions Index updated; the Draft-only alternative removed as no longer open.
- (event) Implemented FR-22/FR-30/FR-31 in code (2026-08-15), skipping bmad-ux per user's explicit choice. Backend: MyCourseSummaryDto + ICourseRepository.GetByTutorIdAsync + ICourseService.GetMyCoursesAsync + GET /api/v1/courses/mine (CoursesController), all Lifecycle States, ownership via ICurrentUserService. Frontend: CourseContentEditor.tsx dropped its fixed-inset-0 viewport takeover in favor of an in-page-body layout with two stacked sticky headers (Navbar unchanged, editor's own title+Close+PublishLifecycleBar row sticky at top-16 in a distinct #F3F0E6 background per DESIGN.md's surface-secondary token) and page-level scroll; new MyCoursesSection.tsx (TutorEducatorHubView) lists all of the tutor's courses with status badges, Resume action on Draft rows wired through useCourseCreationFlow's new openContentEditorForCourse. FR-22 verified as already-correct (Close only clears local UI state, never discards server-side work or touches LifecycleState). Tests: 6 new backend tests (Application+Infrastructure), 12 new frontend tests (CourseContentEditor layout, new MyCoursesSection.test.tsx, TutorEducatorHubView Resume flow); full suites green (BackEnd 696/696, FrontEnd 520/520). Not visually verified in a live browser -- verification is test-only for this pass.
- (decision) Added FR-32 (2026-08-15): permanent delete from My Courses for Draft/InReview/ReviewConfirmed, behind confirmation; Published courses get 'Take Offline' (reuses FR-25's existing Return-to-Draft) instead of delete, no direct-delete path for Published, enforced server-side too. User's explicit split: delete for unpublished, take-offline-only for Published. Implementation choice: soft delete via Course.IsDeleted (already has an active EF query filter, unused until now) rather than a hard row delete -- matches MasterDataService's existing Tag/Taxonomy soft-delete precedent, avoids a first-time cascade-delete design across CourseFile/CourseThumbnail/CourseVersion.
- (event) Implemented FR-32 (2026-08-15): backend CourseService.DeleteCourseAsync (soft delete via IsDeleted, rejects Published) + DELETE /api/v1/courses/drafts/{id}; frontend MyCoursesSection.tsx gained Delete (Draft/InReview/ReviewConfirmed, behind ConfirmModal) and Take Offline (Published, reuses existing returnToDraft/FR-25, no confirmation) actions. Found and fixed a real bug during this pass: FR-30's layout change made CourseContentEditor a normal in-flow element, but it was still rendered near the bottom of TutorEducatorHubView's JSX (after Teaching Analytics/Calendar Slots) -- clicking Resume opened it off-screen below the fold, looking like nothing happened. Fixed by moving its render position to the top of the view, directly below the sticky header. Also fixed a real bug found by a new test: mutation-action errors (Take Offline/Delete failure) were sharing one error state with the initial load error, so displaying an action error hid the entire course list; split into loadError/actionError. Tests: +4 backend (DeleteCourseAsync + soft-delete query-filter regression test), +9 frontend (MyCoursesSection Delete/Take Offline coverage). Full suites green: BackEnd 703/703, FrontEnd 527-528/528 (one different unrelated test flakes under full-suite parallel load each run -- confirmed pre-existing, not caused by this work, by re-running failures in isolation).
- (event) UI polish pass (2026-08-15), all frontend-only, no new FRs (implementation detail of FR-30/FR-31/FR-32's existing screens): (1) ConfirmModal.tsx now fades in on mount and fades out over 150ms before its real onConfirm/onCancel fires (FRONTEND_TRANSITIONS.md's existing fade-in-scale keyframe + row-exit-timing technique, generalized to a whole modal) -- used by both the per-file delete in CourseContentEditor and the course delete in MyCoursesSection. Found and fixed a real race during this: reusing the same ConfirmModal instance across a rapid cancel-then-reopen left it permanently pointer-events-none (stuck from the superseded close); fixed by keying ConfirmModal on a per-open-request counter so every open is a guaranteed-fresh instance. (2) MyCoursesSection's course grid now fades in on every refetch (key={refreshToken} forces remount). (3) CourseContentEditor: fades in on open; dropped the max-w-5xl centered-column cap on its content area (w-full now, matching DESIGN.md's 'no max-w-* wrapper anywhere' rule -- this was a pre-existing violation, not introduced by FR-30); converted the uploaded-file list from one full-width row per file to compact wrapping chips (flex flex-wrap) to save vertical space with many files. Tests: +2 in ConfirmModal.test.tsx (fade classes, later-action-supersedes-earlier via fake timers), existing ConfirmModal-consumer tests updated to await the new close delay. Full suite: 531/532 (1 pre-existing flaky unrelated test, confirmed flaky before this session's work too).
- (event) Added Maximize/Restore toggle to CourseContentEditor.tsx (2026-08-15), frontend-only, implementation detail of the FR-30 screen: a header icon-button (lucide Maximize2/Minimize2) switches between Maximized (current full-page behavior: full width, sticky header at top-16, shadow-xs -- the default, and what every fresh open resets to) and Restored (centered card, max-w-4xl, no sticky header -- scrolls with the page like any other card, shadow-lg since it's a floating panel over page content rather than a resting peer). Also gave the whole surface a border/rounded-2xl/shadow in both modes, matching every other top-level card in the app (TeachingStatsCards, MyCoursesSection) -- it had been flat/borderless against the page background since FR-30, a real inconsistency, not a deliberate choice. +6 new tests (default state, Restore, Maximize-after-Restore, reset-on-reopen, and a border/shadow/rounded consistency check). Full suite: 534-537/537 passing depending on run (2-3 different, unrelated pre-existing flaky tests observed across full-suite runs this session -- AiConfiguration.test.tsx confirmed flaky in isolation too, re-run passed; none touch any file this session modified).
