---
topic: FlexDemy UX spec (DESIGN.md + EXPERIENCE.md) -- reverse-engineered from the existing built product, kicked off from a request to constrain frontend to responsive + full-width layouts
updated: 2026-08-16T23:39
---

- (decision) Constraint (user-stated, triggered this session): all pages must be responsive and full-width. Applies across the whole frontend, not just newly-built Dashboard/Assignments sections. Existing FrontEnd/docs/FRONTEND_PRD.md SS3 already states a similar 100%-full-width principle; this UX spec formalizes it and extends it explicitly to responsiveness.
- (event) First full draft of DESIGN.md and EXPERIENCE.md written, grounded in codebase extraction (colors, typography Fraunces/Outfit, spacing, components) and the two PRDs' journeys mirrored verbatim into Key Flows. Corrected a stale claim in FRONTEND_PRD.md SS3 (wrong font names). Flagged DashboardSectionNav.tsx as a concrete, current violation of the new responsive constraint (hidden lg:flex with no mobile equivalent, unlike Navbar's proven swap pattern) -- presented to user for review.
- (change) Fixed DashboardSectionNav.tsx to add a lg:hidden horizontal sticky-top pill bar equivalent to the hidden lg:flex vertical sidebar (mirroring CourseOverviewScreen.tsx's existing scroll-jump pattern), and changed both Dashboard views' wrapper flex direction to flex-col lg:flex-row so the mobile nav stacks above content instead of squeezing beside it. Verified live at a 500px viewport (nav renders, click scrolls correctly). 289 tests still passing, tsc clean. Both DESIGN.md and EXPERIENCE.md updated from 'known violation' to 'resolved.'
- (event) Input reconciliation complete (2 parallel subagents vs the Dashboard PRD and Assignments PRD). Fixed: disambiguated duplicate UJ-1/UJ-2 labels across the two source PRDs (prefixed by PRD name), corrected a Dashboard-PRD-inherited claim that Dashboard is the default landing tab (real default is Home/Discover per App.tsx), added missing State Patterns (Master/Support no-impersonation boundary, booking race-condition error, points/confetti timing on Hold-then-Reviewed and Re-evaluate delta, persistent countdown toast vs transient toasts, static Recent Activity), added missing Component Patterns (no-live-edit-of-published-assignments, Public Live Masterclass card), and noted the unreconciled Course-Overview-assignment-badges vs Dashboard-Source-badges seam.
- (decision) Remediated citrus-amber to #B04C11 (from shipped #EC7B38) to clear WCAG AA 4.5:1 with white text (~5.4:1 computed); kept as citrus-amber-shipped for reference. Spec-level target only, not yet applied to live Tailwind/CSS code -- that is a separate user decision.
- (change) DESIGN.md: added dashboard-section-nav and toast component rows, surface-secondary/dark-canvas usage bullets, rounded.md alias note, warning/signal-green small-text-usage caveat -- resolving rubric walker medium/low findings.
- (change) EXPERIENCE.md: added State Patterns rows for Home/Discover, Group Study, Course Overview, Auth errors, Profile funnel Pending/Rejected, Certificates, Admin -- resolving rubric walker's high/medium state-coverage findings. Added concrete keyboard/focus/ARIA requirements for Dashboard nav, assignment modal, Hold-to-Reviewed transition, click-anywhere-card pattern, and a prefers-reduced-motion requirement -- resolving accessibility review findings beyond the color contrast fix.
- (event) spines finalized -- DESIGN.md and EXPERIENCE.md set to status: final after Reviewer Gate (rubric walker + accessibility) findings resolved: amber remediated to #B04C11, component/color token gaps closed, State Patterns coverage extended to all IA surfaces, keyboard/focus/ARIA and prefers-reduced-motion requirements added. Structural + prose polish passes applied.
- (change) Applied the remediated amber (#B04C11) to live code per user confirmation: swept all 46 files' hardcoded #EC7B38 hex (incl. Button.tsx, index.css, favicon.svg, docs, and Button.test.tsx's assertion, which auto-updated with it), recomputed index.css's light-theme --accent/--ring OKLCH values and --accent-foreground (now light text, since accent is dark enough for it), updated the rgba() shadow-glow values. Dark theme's brighter amber (#FA8746) left untouched -- different contrast pairing, not flagged by the review. Verified: 289/289 tests pass, tsc clean (same pre-existing unrelated FlashcardsModal errors only), and visually confirmed live at 127.0.0.1:3100 (Home nav pill, streak badge, Tutor Dashboard buttons/chart/section-nav all render the new color with legible white text).
- (override) User feedback on live product after the amber remediation shipped: #B04C11 was too dark, no longer read as visibly amber. Revised citrus-amber to #ee6a1d -- lighter/more vivid, still clears WCAG AA with white text at ~4.95:1 (real margin, not razor-edge). Applied to DESIGN.md, index.css (@theme + OKLCH :root tokens + rgba shadow-glow), and swept all 46 previously-updated files. Verified: 288/289 tests pass (1 unrelated flaky timeout, confirmed by isolated re-run), tsc clean of new errors, visually confirmed live.
- (decision) User reported Tutor Dashboard's Availability & Performance section not full-width. Root cause: TutorEducatorHubView.tsx's root div had max-w-7xl mx-auto, a direct violation of the finalized full-width rule -- pre-existing from before the Tutor Hub merge, missed by the earlier DashboardSectionNav-focused mobile-width audit. Fixed to w-full. Course Player's max-w-4xl reading-pane text column (inside an already-full-width flex-1 main) is a distinct, intentional readability pattern and was left alone -- confirmed not the same bug.
- (override) User manually swept the live code (index.css hex/comments, all 46 hardcoded-hex files, ProgressAndCertificate.tsx, DESIGN.md frontmatter+prose) from #BA5012 to #ee6a1d, then asked to validate it while stating their actual target as #BA5012. Validated #ee6a1d: fails WCAG AA against white text at ~3.13:1 (needs 4.5:1) -- reintroduces essentially the same failure as the original shipped #EC7B38. Reverted the full sweep back to #BA5012 everywhere (index.css's oklch numeric values were never touched by the ee6a1d edit, so only hex literals/comments needed reverting). Verified: 289/289 tests pass, tsc clean of new errors, visually confirmed live at 127.0.0.1:3100.
- (decision) User reported amber 'not properly visible with blue(dark) color'. Root cause: BA5012 (darkened for white-text-on-button AA) only gets ~2.58:1 against navy #143358 when used as icon/text color directly on navy -- the original #EC7B38 had ~4.535:1 there and was fine, but fails white-text-on-button at ~2.82:1. No single hex clears AA in both roles (contrast-vs-white and contrast-vs-navy move in opposite directions as lightness changes, with no overlap at the 4.5:1 floor). Split into two tokens: citrus-amber (#BA5012) for on-light/button-fill roles, citrus-amber-on-dark (#EC7B38, the original value, given a permanent semantic role instead of being a deprecated marker) for icon/text placed directly on navy or a translucent overlay on navy. Found and fixed 15 on-navy instances across 12 files (Navbar streak flame, TutorEducatorHubView Award badge, StudentDashboardView Sparkles badge + streak sentence, GroupStudyView session-active label+icon, WeeklyGoalCard target icon, StudentTutorBookingView graduation-cap icon, StudentAssignmentsSection score-report number + brain icon + target icon + bookopen icon, DashboardSectionNav active icon (desktop+mobile), FocusSessionTimer timer icon, CourseOverviewScreen active-tab icon, AuthLayout feature-list icons, ProgressAndCertificate download icon). Renamed DESIGN.md's citrus-amber-shipped token to citrus-amber-on-dark and rewrote the Colors entry to document the two-role architecture and why it can't be one token. Fixed dashboard-section-nav component token to match actual code (navy fill + white text, on-dark amber icon) instead of the previously-documented amber-tint pattern that didn't match shipped code. Verified: 289 tests (288 pass + 1 confirmed-flaky timeout, passes in isolation), tsc clean of new errors, visually confirmed live -- streak flame and Award/nav icons now clearly legible against navy.
- (decision) User requested (via bmad-help shorthand) two Dashboard UX changes, implemented directly: (1) Azure-Portal-style docked-right side panel (SidePanel.tsx: header/scrollable-body/sticky-footer, slide-in-right keyframe) replacing centered modals across Dashboard -- converted CreateAssignmentModal, SubmissionsModal, AssignmentQuizRunner (previously an inline div under Attempt, now a proper panel), AdaptiveSchedule's Schedule Lesson modal, and StudentTutorBookingView's booking-confirmation + group-request modals. Data-entry panels disable backdrop-click-dismiss; Escape always closes. (2) Wired the existing ToastContext/useToast() system (already used across Admin/Auth) into Dashboard's action hooks (useAssignmentsHub, useTutorHub) and replaced 4 blocking alert() calls (course-publish, join-meeting x2, join-pool) with toasts, so every dashboard save/publish/submit/book/schedule action now gets a proper non-blocking confirmation instead of silence or a JS alert. Verified: 289/289 tests pass, tsc clean, and live-confirmed both the SidePanel (Create Assignment, Attempt quiz, Schedule Lesson) and the toast (schedule-lesson confirmation) in the browser.
- (decision) User requested (via bmad-ux shorthand) two changes to the confirmation-toast system: (1) use amber instead of green for the success variant, for more visibility against the white/cream page chrome -- changed ToastContext.tsx's success VARIANT_CLASSES/VARIANT_ICON_CLASSES to citrus-amber (#BA5012, the on-light role token); error/info variants left unchanged (already distinct/clear). (2) moved the toast stack from bottom-right to bottom-left -- also flipped the enter/exit slide direction (-translate-x-4) to match. The persistent AppointmentToast/OfflineProgressToast (bottom-right, different component/purpose) were deliberately left alone -- now sit in the opposite corner from the general toast stack, which incidentally also removes their prior overlap risk. Updated DESIGN.md's toast component token and Components prose to match reality (also fixed a pre-existing spec/code mismatch: DESIGN.md described a '4px left accent bar' that was never actually implemented -- real component uses a full colored border). Verified: 289/289 tests pass, tsc clean, live-confirmed the amber bottom-left toast and unaffected bottom-right AppointmentToast.
- (decision) Extended the SidePanel (Azure-blade) migration to three more Tutor-side modals in TutorEducatorHubView.tsx: Course Creation Wizard (4-step, footer buttons/labels change per wizardStep via a computed wizardFooter, subtitle shows 'Step N of 4'), Add Teaching Calendar Slot, Create & Schedule Public Live Class. While in that file, also finished migrating its remaining two setToastMessage/inline-banner uses (slot-added, public-class-saved) to the proper useToast() system, consistent with the course-publish toast already wired there -- no more local toastMessage state in this file. Documented components.side-panel and components.modal's narrowed remaining scope in DESIGN.md. Also fixed a responsive bug in AdaptiveSchedule.tsx (Student view): the 'Drag & Drop Lesson Deck' section header (label + hint text) didn't wrap on narrow viewports, causing horizontal overflow that cascades into unwanted page-level vertical scroll -- changed to flex-col sm:flex-row with min-w-0/truncate on the label and shrink-0 on the hint, matching the flex-col-sm:flex-row pattern already used everywhere else in this codebase. Note: could not empirically verify the AdaptiveSchedule fix at a real narrow viewport in this session -- the browser tool's resize_window is unreliable in this environment (confirmed again: requests for 375x800 and 300x700 both landed at the stuck 1280x575 actual size) -- fix applied on code-level reasoning matching an established, already-proven-correct pattern. Verified: 289/289 tests pass, tsc clean, and live-confirmed all three new side panels (including full submit-to-toast round trip on Create & Schedule Public Live Class) at the available viewport size.
- (decision) Fixed the button color/size inconsistency the user flagged on 'Create & Schedule Public Live Class' (and audited the rest of the Dashboard's side panels for the same issue): normalized ~20 off-brand hardcoded Tailwind stock colors (indigo-600/50/700/900, purple-600, emerald-100/600/800/300, stock amber-600) across TutorEducatorHubView.tsx, TutorAssignmentsSection.tsx, and StudentAssignmentsSection.tsx to the actual DESIGN.md palette (navy #143358, citrus-amber #BA5012, signal-green #179765) -- covering the Course Creation Wizard, Add Teaching Calendar Slot, Create & Schedule Public Live Class, Create Assignment, and the quiz-attempt panel. Left two things deliberately alone after checking DESIGN.md: the amber-50 'submitted/pending' status badges (DESIGN.md explicitly documents warning states as deliberately stock-colored, not brand-colored) and the top-level online/offline toggle's rose-600 (outside any side panel, out of the stated scope). For the modular-components ask: extended the existing shared ui/Button.tsx (not a new parallel component) with a 'ghost' variant for Cancel/Back actions, and wired all 8 side-panel footers through it (ghost=cancel, secondary=navy default action, primary=amber highlighted action) instead of each panel hand-rolling its own button classes -- this is now the structural fix, not just a color patch, so future panels can't drift the same way. Also extracted features/Dashboard/ChoiceToggle.tsx, a shared two-option selector (icon+label+description card pair) used identically 3x (Assignment Target, Assignment Visibility Mode, Public Live Class Session Mode) with per-option selectedClassName so semantic toggles (green=Immediate/amber=Hold) and default/alternate toggles (navy=default/amber=alternate) both fit without forcing one hardcoded color rule. Verified: 289/289 tests pass, tsc clean, and live-confirmed the exact panel the user pointed at (Public Live Class) now uses consistent navy/amber styling with the shared Button component.
- (decision) Did a comprehensive second pass on all 9 Dashboard side panels per user's follow-up ('check the other dashboard modals for consistent styling'), going beyond button/toggle colors this time: found and fixed that NONE of the ~30 form inputs/selects/textareas across the panels had a focus ring at all, despite DESIGN.md's input component explicitly requiring one (focusRing: citrus-amber) -- added bg-white + border-[#E1DED4] + focus:ring-2 focus:ring-[#BA5012] uniformly. Also found labels inconsistently mixed slate-700/slate-800/slate-900 instead of the app's actual ink token (#142030), and several card/divider borders used stock slate-200/300 instead of the hairline token (#E1DED4) -- normalized all of it across TutorAssignmentsSection.tsx, TutorEducatorHubView.tsx, StudentAssignmentsSection.tsx, StudentTutorBookingView.tsx (panel portions only), and AdaptiveSchedule.tsx. Also fixed Dashboard.tsx's Master/Support 'Student Dashboard / Preview Tutor Dashboard' toggle pill, which had the identical indigo-600 bug -- not a modal, but the same bug class and highly prominent (top of every Dashboard page for Master/Support), so included it. Deliberately left three things alone after checking against precedent: (1) neutral gray Draft/Unpublish/Re-evaluate treatments (a consistently-applied 'inactive' pattern, not a bug), (2) StudentDashboardView's purple stat-card icon-well tint (DESIGN.md explicitly documents amber/navy/purple/green as the sanctioned rotation for stat cards), (3) StudentTutorBookingView.tsx's 'Group Class Pools & Public Masterclasses' section (lines ~473-562) -- this has real off-brand indigo/emerald usage but lives on the main page, not in a modal/panel, so it's outside this pass's stated scope; flagged to the user rather than fixed unilaterally. Verified: 289/289 tests pass, tsc clean.
- (change) Converted Admin section Add/Edit/Update operations to SidePanel (MasterDataTable Add+Edit, AdminUserStatusList Edit, SupportUserCreation Add incl. one-time-password reveal, TutorApprovals Reject), replacing the prior tested 'not a modal, inline Collapse+FormCard' pattern -- user explicitly chose full conversion for admin/dashboard SidePanel consistency, accepting the cost of rewriting MasterDataTable.test.tsx and AdminUserStatusList.test.tsx. All 289 frontend tests pass, tsc clean, live-verified in browser (Add/Edit Country, Add Support User + password flow).
- (change) DESIGN.md components.side-panel and components.modal entries broadened from Dashboard-only to also name Admin's converted flows (Master Data Add/Edit, Add/Edit Support User, Reject Tutor Application) and explicitly note Admin's delete-confirm intentionally stayed a centered modal (yes/no decision, not data-entry) rather than converting.
- (change) Fixed ui/FormCard.tsx's FieldsGrid: changed sm:grid-cols-3 to sm:grid-cols-2. All 3 fields squeezed onto one cramped row inside the 480px SidePanel once it moved off full-page width -- e.g. 'Email or Phone Number' label crushed into ~130px. Verified via DOM measurement in-browser (fields now 210px each, long labels get their own row) and full test suite (289/289 pass).
- (change) Further refined ui/FormCard.tsx's FieldsGrid per user follow-up: changed from sm:grid-cols-2 to grid-cols-1 -- every Admin SidePanel form field (Master Data Add/Edit, Support User Add/Edit) now stacks one control per row at the panel's full content width, rather than pairing two fields per row. Verified via DOM measurement (each input now 432px wide, same x, stacked y) and Admin test suite (55/55 pass).
- (event) Update triggered: new source prd-eLearning-CourseWizard-2026-08-10/prd.md (+ addendum.md), New Course Wizard PRD, status final. Supersedes this spine's existing 'Course Creation Wizard' side-panel flow (Dashboard row, Component Patterns' Side panel entry) and extends Course Player's '5-level concept drilldown' from mock to real AI-backed content
- (decision by user) Wizard surface split: read TutorEducatorHubView.tsx directly to confirm today's wizard is entirely a 640px side-panel blade with a flat single-line lesson list at Step 3. New PRD's content step (Chapter->Topic->Subtopic tree editor, per-node AI-extraction status/confirm, math/chemistry/image rendering per FR-16) doesn't fit that width. Decision: Steps 1-4 (Title/Tags/Taxonomy/Thumbnails) stay in the existing side-panel blade pattern; the Content Upload & AI Extraction step breaks out into its own full-width surface, same treatment as Course Player's reading pane, once metadata steps are complete
- (decision by user) Working mode: Fast path, matching this spine's own precedent of grounding directly in code/requirements over heavy elicitation
- (event) Fast-path draft complete: EXPERIENCE.md updated (sources, IA table: new Course Content Editor row, updated Dashboard/Course Player/Admin rows; Component Patterns: 9 new/updated rows incl. flagging DrilldownPanel.tsx's unremediated off-brand indigo/emerald colors found by reading live code; State Patterns: 8 new rows for upload/extraction/confirm/publishing/budget states; Interaction Primitives: drag-reorder extended to tree editor, popover dismiss, Ways free-cycling vs Drilldown sequential; Accessibility Floor: 4 new bullets; Voice and Tone: 2 new Do/Don't rows; Key Flows: New Course Wizard PRD UJ-1 through UJ-4 mirrored from PRD). DESIGN.md updated (5 new component tokens in frontmatter; Components prose: Side panel entry updated for 4-step-only scope, new Course Content Editor/content-tree-node/extraction-status-badge/ways-menu/keyword-popover/exercise-runner entries, all reusing existing color tokens -- no new colors introduced; Do's and Don'ts: DrilldownPanel remediation flagged as known gap). Both files still status: final in frontmatter -- not yet updated to reflect in-progress edit, will flip at Finalize close per skill convention (draft state isn't tracked separately for UX spines the way PRD frontmatter uses status:draft)
- (decision by user) Party review (BMM crew, Sally leading) resolved all 4 flagged open items: (1) Publishing async state = node-by-node checklist ('12 of 34 confirmed nodes generated'), not a spinner -- Winston estimated 200+ AI calls per publish batch (low minutes not seconds), rules out spinner; survives tab-close, runs server-side, resumable progress view on reopen; (2) keyword popover accessibility committed to aria-live announcement with focus staying on the reading text, no focus-steal into the popover -- consistent with the existing Hold-then-Reviewed aria-live pattern; (3) Ways menu placement deliberately left as an assumption, deferred to Finalize's key-screen mock-coverage step rather than decided in prose; (4) Admin sub-tab gating split by user decision: Support keeps Tag Management (expands Support's current single-subtab scope), AI Configuration & Usage is Master-only given its cost/budget control
- (event) All 3 party resolutions applied to EXPERIENCE.md: Admin IA row (split gating), Publishing state pattern (checklist treatment), keyword popover accessibility bullet (aria-live, no focus-steal). Ways menu placement left as-is, deferred to Finalize mock-coverage per Sally's explicit call
- (event) Finalize step 2 (input reconciliation) complete: 5 gaps found and fixed -- thumbnail reorder/delete/set-primary/cap-rejection added to wizard row; cascading-delete confirm modal added to Content Tree Node row + DESIGN.md Modal entry; new Course version history row added (lightly specified, [ASSUMPTION] tagged pending FR-25's architecture-side storage decision); per-node generation-failure fallback added as new State Pattern row (on-demand fallback, never visible to student); deactivated-but-attached tag chip state added to wizard row. reconcile-course-wizard-prd.md written
- (event) Finalize step 3 (accessibility Reviewer Gate) complete: 12 findings (2 critical, 3 high, 4 medium, 3 low), all resolved. Two real decisions: (1) DrilldownPanel.tsx color remediation folded into this implementation pass, not deferred; (2) math/chem alt-text gets a new 7th AI Task (describeNotation), synced back to the PRD itself (FR-1, FR-16, FR-27, Glossary). Remaining 10 findings applied directly as mechanical extensions of already-established patterns: aria-live for extraction-status and Publishing progress, keyword-popover keyboard activation, crop-tool keyboard equivalent, exercise-runner math input as plain text not a visual widget, Hindi lang=hi (WCAG 3.1.2, distinct from alt-text), node-reversion announcement, budget-warning icon+text, persistent lifecycle-stage indicator in Course Content Editor's sticky header, motion list extended to 3 new animations, autosave-on-blur named as the unsaved-edit mechanism. review-accessibility-coursewizard.md written
- (event) Finalize steps 5-6 complete: mock-coverage confirmed (5 spine-only surfaces need no visual reference, user confirmed); 3 key-screen mocks (Course Content Editor, Course Player adaptive affordances, Publishing checklist state) promoted from .working/ to mockups/, inline links added to IA table's Course Content Editor and Course Player rows plus updated Composition reference note
- (event) Finalize step 8 (polish) complete: structural pass found 2 real issues in EXPERIENCE.md - Adaptive Ways menu row still had a stale mock-coverage-pending assumption tag (now resolved, links to the rendered mock instead), and Dashboard PRD UJ-2's mirrored journey text referenced the superseded old wizard with no forward-pointer to the new flow (added [NOTE FOR UX] per the doc's own established correction-note precedent). DESIGN.md structure was clean, no changes. Prose pass: 1 clarity fix in EXPERIENCE.md's Accessibility Floor (awkward double-relative-clause sentence), DESIGN.md's new content was already clean. No stray characters found in either file (verified via CJK-range grep).
- (event) spines finalized (New Course Wizard PRD update)
- (event) Update triggered (2026-08-15): user requested (via bmad-ux shorthand) documenting the Course Content Editor's Maximize/Restore toggle and consistent border/shadow/rounded-2xl treatment, already implemented directly in code this session (CourseContentEditor.tsx) ahead of the spine -- same 'ground in already-built code' precedent this spine has followed since its first draft.
- (decision) Course Content Editor Maximize/Restore documented in both spines. New DESIGN.md component token 'course-content-editor' (white shell, hairline border, rounded.lg, shadow-xs maximized / shadow-lg restored) plus Components prose update -- previously described as flat/borderless, now a real bordered card shell in both states, matching every other top-level card (resolves the inconsistency the user flagged). Real tension surfaced and resolved: Restored's max-w-4xl directly reads against the Do's-and-Don'ts 'no max-w-*, ever' rule (which had a prior precedent example of treating this exact pattern as a bug, TutorEducatorHubView's max-w-7xl). Resolved by framing Restored as a narrow, explicit exception -- not a new top-level surface with its own width identity, but a temporary user-toggled alternate presentation of an already-open surface, same category the rule already implicitly exempts {components.modal} and {components.side-panel} (neither of which is full-width either). Documented explicitly in Do's and Don'ts so the exception doesn't get miscited as license elsewhere. EXPERIENCE.md: IA row updated + flagged the existing key-course-content-editor.html mock as visually stale (rendered before this toggle/card-shell existed, not yet refreshed); Component Patterns table gained a new Maximize/Restore row; Accessibility Floor's lifecycle-stage-orientation bullet caveated (sticky only in Maximized) and gained a new bullet for the toggle's own real-button/accessible-name discipline; Interaction Primitives' modal-dismiss bullet updated (neither state is a modal, so neither has a backdrop-dismiss question).
- (event) Finalize: user opted to skip the Reviewer Gate (small, already-shipped/tested change). No new external input document this round (grounded directly in already-implemented code, same precedent as prior updates to this spine) -- input reconciliation step is a no-op. Non-blocker deferred: mockups/key-course-content-editor.html is now visually stale (predates the Maximize/Restore toggle and card-shell border/shadow) -- flagged inline in EXPERIENCE.md's IA row rather than re-rendered this pass; revisit next time Course Content Editor's mock coverage is touched, or on request.
- (event) spines finalized (Maximize/Restore + course-content-editor card-shell update). Both files remain status: final, updated bumped to 2026-08-15.
- (override) User requested (via bmad-ux shorthand) that Course Content Editor keep its full-screen Maximize capability but default to Normal (non-maximized) on open -- reverses this same session's earlier decision that Maximized is the default. Renamed 'Restored' to 'Normal' throughout both spines for clarity (matches conventional windowed-app semantics: Normal is the resting state, Maximize/Restore are the two toggle actions). Updated: DESIGN.md's course-content-editor component token (normal/maximized/toggle keys, shadow description flipped), Components prose bullet, and the Do's-and-Don'ts max-w-* exception note -- explicitly flagged that this widens the exception (the surface's resting layout is now capped, not just a temporary toggled-away state, which is a bigger carve-out from the 'every surface full-width, ever' hard constraint than before). EXPERIENCE.md's IA table row, Component Patterns' Maximize/Restore row, UJ-1 journey step 2, Interaction Primitives' modal-dismiss bullet, and both Accessibility Floor bullets (lifecycle-stage sticky-header caveat, Maximize/Restore button naming) all renamed/flipped to match. No code change made -- CourseContentEditor.tsx's isMaximized still defaults to true; flagged to user as a following implementation step.
- (event) Finalize: skipped the Reviewer Gate (small, targeted default-state flip on an already-reviewed component, same precedent as the toggle's original introduction earlier this session). No new source input to reconcile -- change is a direct user instruction. mockups/key-course-content-editor.html remains flagged stale from the prior update (predates the card-shell/toggle); not re-rendered this pass. Both spines remain status: final, updated already at 2026-08-15 from the same-day prior update, no bump needed. Open item for a follow-up dev pass: CourseContentEditor.tsx's isMaximized state still initializes to true (both on mount and on the isOpen-reset effect) -- needs flipping to false to match the new default.
- (change) Applied the spec's Normal-default decision to code: CourseContentEditor.tsx's isMaximized now initializes to false (both the useState default and the isOpen-reset effect), flipped from true. Updated the accompanying comments and the maximize/restore + screen-presentation (FR-30) test blocks in CourseContentEditor.test.tsx to match (default assertions now check max-w-4xl/shadow-lg/no-sticky-header instead of shadow-xs/sticky; the 'stays sticky' test now clicks Maximize first). Verified: CourseContentEditor's own suite 24/24 pass, full frontend suite 534/537 pass with the 3 failures being pre-existing App.test.tsx/CourseWizard.test.tsx timeouts unrelated to this component (confirmed passing 28/28 in isolated re-run), tsc shows only the pre-existing unrelated FlashcardsModal/useBookingState errors already noted in this memlog.
- (decision) User requested (via bmad-ux shorthand) two follow-up changes to Course Content Editor's file upload area, implemented directly: (1) a small loading spinner ({components.spinner}, xs/white, the existing shared Loader2/animate-spin idiom already used app-wide) added inline on the Queued and Parsing status badges -- the two non-terminal states -- so 'still working' reads at a glance; Done/Failed (terminal) get no spinner. (2) User then clarified Maximize should be a TRUE full-screen takeover, not just full-width-in-page -- reversed FR-30's original 'no fixed inset-0, stays below the Navbar' decision, but scoped narrowly to the Maximize action only (which is no longer the default, per this session's earlier Normal-default change -- that's what makes reintroducing fixed inset-0 safe now: it's opt-in, not forced on every open). Maximized is now fixed inset-0 z-50 (stacking above Navbar's own z-50, same idiom as SidePanel/CourseReviewModal/FlashcardsModal), edge-to-edge with no border/rounding/shadow, header pinned as a shrink-0 flex item while the body becomes its own flex-1 overflow-y-auto scroll region. Normal is unchanged (centered max-w-4xl card, shadow-lg, scrolls with page). (3) User also requested a Delete option on Failed files during upload, not just Retry -- added a second icon button (Trash2, same red/hover-red-50 styling as Retry) on Failed rows only, wired directly to the existing deleteFile() with no ConfirmModal (unlike a Done file's content-card delete) since a failed file never had any content extracted, matching the established 'leaf node needs no confirm' precedent.
- (decision) User requested (via bmad-ux shorthand) moving Course Content Editor into My Courses (Tutor): clicking Resume on an existing Draft course should open the editor inline, directly beneath that course's own row, at full available width -- instead of at the top of the page like every other open path. Clarified via 2 questions before implementing: (1) kept the Resume trigger exactly as-is (Draft-only, same button) -- this is a placement change, not a scope change to which lifecycle states get an editor entry point; (2) kept the New Course Wizard's own hand-off (a freshly-created course) opening at the top of the page unchanged -- only the 'resume an existing draft' path moves inline. Implementation: useCourseCreationFlow.ts gained contentEditorOrigin ('wizard' | 'resume') alongside the existing isContentEditorOpen/contentEditorDraftId, so the two entry points share one source of truth (only one editor open at a time) but render in two different places -- TutorEducatorHubView.tsx's top-level <CourseContentEditor> now gates on origin==='wizard'; MyCoursesSection.tsx renders its own instance inline (interleaved into the courses.map via React.Fragment, a plain list-none <li> wrapper so the <ul> stays valid markup) when a course's id matches the new openDraftId prop. CourseContentEditor.tsx gained a fullWidth prop (default false, unchanged for the standalone top-of-page usage) that swaps Normal state's max-w-4xl mx-auto cap for w-full -- MyCoursesSection passes fullWidth since the editor is already nested inside that section's own bordered card, where centering/capping would read as a card-within-a-card. Also retroactively documented My Courses (Tutor) in EXPERIENCE.md's IA/Component Patterns/State Patterns -- this FR-31/FR-32 surface (resume list, Take Offline, Delete) was built directly in code in an earlier session and had never been captured in the spines until now, same 'ground in already-built code' precedent this spine has followed throughout. Verified: CourseContentEditor/MyCoursesSection/TutorEducatorHubView suites 55/55 pass (new tests cover inline placement/doc-order, fullWidth CSS, and onCloseContentEditor wiring), full frontend suite 546/547 (1 pre-existing unrelated flaky Admin timeout, confirmed passing in isolation), tsc shows only the pre-existing unrelated FlashcardsModal/useBookingState errors already noted in this memlog.
- (decision) User requested (via bmad-ux shorthand) a zoom in/zoom out effect on Course Content Editor's Maximize/Restore toggle, which previously swapped layouts with an instant class change and no transition. Added a new 'zoom-out' CSS keyframe to index.css (150ms ease-out, scale 1.05->1 + fade), the mirror-image counterpart to the existing shared 'fade-in-scale' keyframe (150ms ease-out, scale 0.95->1 + fade) already used app-wide for entrance animations. Maximize reuses fade-in-scale directly as its zoom-in effect (already exactly that -- grow-and-fade); Restore/Normal now uses the new zoom-out keyframe as its counterpart. Key implementation point: the two states must reference genuinely different keyframe names, not the same class reused twice -- CourseContentEditor.tsx's outer div persists across the toggle (only its className changes, no remount), and a CSS animation only restarts when its animation-name value actually changes; the two branches previously both said animate-[fade-in-scale_150ms_ease-out] literally, which is why toggling never replayed anything after the very first open. Side effect, accepted as reasonable: the default first-open (Normal) now plays the zoom-out-flavored entrance (shrink into place) instead of the old fade-in-scale-flavored one (grow into place) -- a minor, barely-noticeable direction change to the very first mount's entrance, not something a fresh state machine was added to avoid. Did not add this to the Motion accessibility bullet's prefers-reduced-motion list -- consistent with the existing fade-in-scale entrance animation itself, which was never covered by that mechanism anywhere in this codebase either; not fixing that pre-existing, unrelated gap as a side effect of this small addition. Verified: CourseContentEditor's own suite 31/31 pass (added a dedicated test asserting the two states carry genuinely different animate-[...] classes, not just different other classes), MyCoursesSection/TutorEducatorHubView suites 25/25 pass (both mount CourseContentEditor), tsc shows only the pre-existing unrelated FlashcardsModal/useBookingState errors already noted in this memlog. Not visually verified in a live browser -- a background session, and this is a self-contained CSS keyframe swap with direct test coverage of the exact mechanism (which class applies in which state), not a claim that needs eyes-on confirmation the way a layout/spacing change would.
- (event) Update session started: scoping UX for prd-eLearning-ContentAuthoring-2026-08-16 (Content Wizard + Page/Resources workspace), extending this shared eLearning UX workspace the same way Course Wizard and Assignments were folded in
- (decision) User requested mock-first: rendered key-screen HTML mock before spine elicitation, reusing DESIGN.md tokens and the Course Content Editor browser-frame/lifecycle-bar idiom from the existing key-course-content-editor.html sibling mock rather than inventing new visual language
- (change) Added .working/mock-content-wizard-workspace.html: 4 key screens (Wizard Step1 Chapter, Step2 Topics, Step3 Sub-Topics, Workspace Page-editor+Resources), grounded in PRD UJ-1 (Meera, CBSE Class 10 Science). Note: this supersedes the DESIGN.md 'content-tree-node' component's per-block-row pattern for the authoring surface -- new PRD's DD-3 replaces ContentBlock rows with a Markdown Page body + block palette, so DESIGN.md/EXPERIENCE.md will need an update pass to reflect this, not just an addition
- (decision) Pivoted away from the 4-step wizard mock: user wants a single continuous Confluence/Jira-style canvas (nested Chapter>Topic>Sub-Topic>Page sections, inline + Add affordances, no Back/Next/step-indicator chrome). This is a real interaction-model change from the PRD's FR-9/FR-13/FR-14/FR-16 wizard language -- flagged as a NOTE FOR PM in the mock itself, not silently absorbed
- (decision) All name/title/description/caption fields changed from single-line inputs to resizable wrapping textareas (.field-ml) -- every field treated as multi-line-capable, per user request
- (decision) Resources are no longer a fixed 'This Page's Resources' side panel. Replaced with a generic, insertable 'Learning Resources' block/section (same component reused at Chapter level and Page level in the mock) that a tutor can attach at any outline level via a '+ Learning Resources' chip -- mirrors Confluence's reusable macro/panel pattern rather than app-specific chrome
- (event) Published the v2 single-canvas mock as a separate standalone artifact (.working/mock-content-canvas-v2.html) at user's request, for side-by-side comparison against the original wizard-step mock rather than overwriting it
- (decision) Round-3 pivot (v3): user wants a truly Confluence/Notion-style document-writing experience, not boxed nested panels. Topic/Sub-topic are now plain document headings (H2/H3) inside one flowing Chapter document, not bordered cards. Primary authoring mechanic is now a '/' slash command menu (filterable, categorized: Resources/Structure/Basic/Media&Data) shown open mid-type as the hero interaction, replacing v2's always-visible block-palette toolbar
- (decision) User wants the block/authoring system explicitly generic -- reusable in future for any subject, not just this chemistry example. Addressed by keeping block type names domain-agnostic (Paragraph/Heading/Image/Table/Math/Code/Callout/Resources) and adding an explicit in-mock note that the same slash-menu authors a history timeline or language-vocab set identically -- chemistry is only this mock's worked example, not the system's shape
- (decision) Outline rail reframed as a Confluence-style auto-derived Table of Contents (built from heading blocks in the document) rather than a separately-managed entity tree -- reinforces 'the document is the source of truth'
- (event) Published v3 (.working/mock-content-doc-slash.html) as a new standalone artifact per established preference for separate comparable links rather than overwriting v1/v2
- (change) Distilled the validated v3 direction into DESIGN.md and EXPERIENCE.md: added content-doc-heading, content-page-marker, content-resource-block, content-slash-menu components/patterns; marked content-tree-node and its a11y/component-pattern rows as superseded-kept-as-history; updated Course Content Editor's IA row and Component Patterns; added slash-command to Interaction Primitives + Accessibility Floor (click '+' as accessible primary path, IME-safety note); added ContentAuthoring PRD UJ-1 to Key Flows superseding the PRD's own wizard-based UJ-1 text; bumped both spines to updated: 2026-08-16, added ContentAuthoring PRD to EXPERIENCE.md sources
- (event) Promoted mockups/key-content-doc-slash.html (from .working/mock-content-doc-slash.html) as the canonical rendered reference for the new document/slash-command authoring surface
- (change) Resolved validation findings in DESIGN.md/EXPERIENCE.md: real contenteditable h1-h4 heading model (Chapter=h1, Topic=h2, Sub-Topic=h3, Page marker=h4, resolving the ToC-vs-heading-tree contradiction); non-color-alone confirmation glyph; full slash-menu ARIA/keyboard contract (combobox/listbox roles, Tab-does-not-navigate, zero-match state, Escape focus-return, post-insert focus+aria-live); '+' trigger spec (focus-visible not hover-only); IME-composition + Firefox Quick-Find safety; real keyboard-operable resource-row controls (role select, caption, remove/reorder, real ancestor link); added missing State Patterns rows (empty-first-open, cold-load reopen, autosave-failure, published-readonly, slash-menu zero-match); added Key Flow entries for ContentAuthoring PRD UJ-2 (extraction-to-blocks) and UJ-3 (resource depth/inheritance/delete-guard); fixed spinner token, stale course-content-editor composition pointer, node terminology, component display-name alignment, Course version history disclaimer
- (change) Corrected mockups/key-content-doc-slash.html to match the fixed spec: real contenteditable h1/h2/h3/h4 elements replacing div+textarea headings, visible '+' block-insert affordance (previously absent from the mock entirely), real resource-row controls (role select, caption input, move/remove buttons) and real Upload/Attach-existing/Insert-from-file buttons in the drop zone replacing static hint text
- (decision) Consciously deferred as low-priority, not fixed this pass: DESIGN.md's multi-round remediation narrative (citrus-amber contrast fix, DrilldownPanel sweep, max-w-7xl fix) could move to a changelog appendix; EXPERIENCE.md's Key Flows/Responsive & Platform section order is swapped vs. canonical order. Both are cosmetic/ordering only per the rubric reviewer's own assessment, not load-bearing.
- (event) UX Finalize complete for the ContentAuthoring update: spines distilled, validated (rubric + accessibility reviewers), findings resolved, mockup corrected to match spec, status remains final, updated 2026-08-16. Outstanding: the ContentAuthoring PRD's own FR-9-17 wizard text still needs an Update pass to match this spine's validated document/slash-command model (tracked via [NOTE FOR UX->PM] in EXPERIENCE.md's IA table)
