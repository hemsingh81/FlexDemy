---
scope: BackEnd/ LearnSphere API - greenfield Clean Architecture setup
purpose: Layered architecture spine (Clean Architecture) for a new ASP.NET Web API C# backend on PostgreSQL, deployed via Docker, plus AI-assistant-followable coding rules and initial project scaffold
altitude: feature/project (single backend service, above epics)
updated: 2026-08-11T02:05
---

- (constraint) Greenfield: BackEnd/ folder exists but is empty. No existing code to ratify -- this is a from-scratch stack/structure decision, not a brownfield sweep.
- (constraint) Stack is user-decided, not inferred: ASP.NET Web API (C#), PostgreSQL, Docker-based deployment. These are fixed inputs, not open choices for this run.
- (constraint) FrontEnd/docs/BACKEND_PRD.md is the requirements input: entities (users, courses, course_notes, course_reviews, tutor_slots), REST endpoints under /api/v1, WebSocket protocols (session countdown, synchronous study rooms), an AI microservice pipeline (concept drilldown, auto-grading), JWT+OAuth2 auth, Redis-backed rate limiting. That PRD assumed a Node/FastAPI backend -- its endpoint/entity shape is requirements input, its suggested tech stack is superseded by the user's explicit ASP.NET/Postgres/Docker choice.
- (constraint) User explicitly scoped this run to 'just setup the project structure' -- structure + conventions + scaffold, not full endpoint implementation. Endpoint/use-case implementation is future work this spine sets up for, not this run's deliverable.
- (version) .NET 10 -- current LTS (supported through Nov 2028), web-verified Aug 2026. .NET 8 is the other live LTS but .NET 10 is the right default for a greenfield project starting now.
- (version) Npgsql.EntityFrameworkCore.PostgreSQL 10.0.3 -- current EF Core provider for PostgreSQL, requires Microsoft.EntityFrameworkCore >=10.0.4 <11.0.0, web-verified Aug 2026.
- (version) PostgreSQL 18 (18.4 latest stable) -- current stable major as of Aug 2026; PostgreSQL 19 still in beta. Docker image: postgres:18-alpine.
- (version) MediatR 13+ went commercial (RPL-1.5 copyleft or paid; free Community tier only under $5M revenue), web-verified Aug 2026. Project's commercial scale is unspecified, so avoid baking in a licensing decision the user didn't ask for.
- (version) FluentAssertions 8+ requires a paid Xceed license for commercial use ($130/dev/year); only v7.x (Apache 2.0, no further features) is free for commercial use. AwesomeAssertions is a free MIT-licensed community fork continuing v8's API. Web-verified Aug 2026.
- (version) Moq remains free (BSD-3, the 2023 SponsorLink telemetry incident was removed) but retains reputational distrust in parts of the .NET community; NSubstitute (MIT, no controversy) is the safer default recommendation for a fresh project. Web-verified Aug 2026.
- (decision) AD-1 [ASSUMPTION] Paradigm: Clean Architecture / Onion with 4 projects -- LearnSphere.Domain (entities, value objects, domain rules, zero project references), LearnSphere.Application (use-case interfaces + services + DTOs, references only Domain), LearnSphere.Infrastructure (EF Core/Npgsql, external services, implements Application interfaces, references Application+Domain), LearnSphere.Api (controllers, middleware, composition root, references Application+Infrastructure). Binds: whole BackEnd/src tree. Prevents: a controller or use-case reaching straight into EF Core/Postgres, and Domain acquiring a framework dependency. User confirmed this over simple N-Tier.
- (decision) AD-2 [ASSUMPTION] Layer communication rule: Application defines interfaces for everything it needs from outside (repositories, external services, clock/id providers); Infrastructure implements them; dependency injection wiring happens ONLY in LearnSphere.Api/Program.cs (the composition root). Binds: all cross-layer calls. Prevents: Infrastructure types (DbContext, EF entities-as-DTOs) leaking into Application or Api signatures, and DI registration scattered across multiple files. Rule: Application and Domain projects must never reference Infrastructure; only Api may reference Infrastructure, and only to register it in Program.cs.
- (decision) AD-3 [ASSUMPTION] No mediator/CQRS-pipeline library (no MediatR). Application layer exposes plain interface+implementation service classes per feature area (e.g. ICourseService/CourseService), called directly by controllers via constructor injection. Binds: Application layer structure. Prevents: encumbering the project with MediatR 13+'s RPL-1.5 copyleft/commercial licensing for an unscoped-revenue project. Deferred: revisit if the team later wants CQRS + cross-cutting pipeline behaviors (validation/logging/caching per request) and is fine accepting MediatR's license, or an alternative like Mediator (martinothamar, MIT, source-generator based, no licensing concern) or Mediatr-free source-generated approach.
- (decision) AD-4 [ASSUMPTION] Data access: EF Core (Npgsql.EntityFrameworkCore.PostgreSQL provider) via Repository + UnitOfWork interfaces defined in Application (e.g. ICourseRepository, IUnitOfWork), implemented in Infrastructure. Domain entities are persistence-ignorant (POCOs, no EF Core attributes) -- table/column mapping lives in Infrastructure via Fluent API IEntityTypeConfiguration<T> classes, one per entity. Binds: all data access. Prevents: EF Core-specific attributes or query patterns leaking into Domain/Application, and ad-hoc DbContext usage scattered through controllers.
- (decision) AD-5 [ASSUMPTION] API conventions: controllers are thin (HTTP <-> Application DTO mapping only, zero business logic), routes versioned as /api/v1/{resource} matching BACKEND_PRD.md's existing endpoint shapes, errors returned as RFC7807 ProblemDetails (ASP.NET Core's built-in support), DTO naming is {Entity}Dto for reads and Create{Entity}Request/Update{Entity}Request for writes. Binds: LearnSphere.Api and LearnSphere.Application DTO layer. Prevents: inconsistent error shapes and controllers accumulating business logic that belongs in Application services.
- (decision) AD-6 [ASSUMPTION] Feature-folder organization WITHIN each layer, not type-folder -- e.g. Application/Courses/{ICourseService.cs, CourseService.cs, CourseDto.cs}, Application/Tutoring/, Application/Notes/, Application/Reviews/, Application/Users/ -- mirroring the same feature-folder philosophy the frontend spine already adopted, rather than Application/Interfaces/ + Application/Services/ type-folders. Binds: internal structure of Domain/Application/Infrastructure. Prevents: a growing Services/ or Interfaces/ folder with no feature boundary, and keeps the two halves of the stack (frontend features/, backend feature-folders) conceptually aligned for anyone working across both.
- (decision) AD-7 [ASSUMPTION] Testing: xUnit (standard .NET test framework) + NSubstitute for mocking (MIT, avoids Moq's reputational baggage) + xUnit's built-in Assert (avoids FluentAssertions 8+'s commercial license; AwesomeAssertions noted as a free opt-in upgrade later if wanted). One test project per layer mirroring src/ -- LearnSphere.Application.Tests, LearnSphere.Infrastructure.Tests, LearnSphere.Api.Tests -- test classes live in a namespace-mirrored folder path (standard .NET convention: a parallel tests/ tree, not file-colocation like the frontend's *.test.tsx -- this is a deliberate per-ecosystem difference, not an inconsistency).
- (decision) AD-8 [ASSUMPTION] Migrations strategy: EF Core Migrations live in LearnSphere.Infrastructure/Persistence/Migrations, authored via 'dotnet ef migrations add' against the Api project (which holds the DbContext factory for design-time). Applied automatically on Api startup in Development/Docker-Compose (Program.cs calls db.Database.Migrate() behind an environment check) for fast local iteration; Deferred: switch to an explicit migration step/job before any real production deployment, since auto-migrate-on-startup is a known anti-pattern at scale (concurrent instances racing to migrate).
- (decision) FIX AD-7/Stack/Consistency Conventions: NSubstitute is BSD-3-Clause, not MIT (web-verified against its LICENSE.txt). AwesomeAssertions is Apache-2.0, not MIT (a maintainer states it will never move to MIT, retaining FluentAssertions v7's original grant) -- corrected 'free MIT fork' to 'free Apache-2.0 fork'. Neither correction changes the underlying conclusion (both remain free/permissive, safe defaults); this was a license-type labeling error, not a wrong recommendation.
- (event) Reviewer gate ran: rubric-walker, version-verify, adversarial -- all 3 completed. Findings saved under reviews/. Triaged below; fixes applied inline.
- (version) EFCore.NamingConventions 10.0.1 -- current, supports EF Core 10, web-verified Aug 2026. Applies snake_case globally instead of per-entity .HasColumnName() calls.
- (decision) FIX AD-4 extended: use EFCore.NamingConventions (SnakeCaseNamingConvention) registered once in Infrastructure's DbContext options, so C# PascalCase entities/properties map automatically to the PRD's snake_case SQL schema (users, course_notes, tutor_id, etc.) without per-property .HasColumnName() calls scattered across every IEntityTypeConfiguration. Prevents the first migration silently diverging from the PRD's documented schema.
- (decision) AD-9 [ASSUMPTION] Entity IDs: string IDs matching the PRD's VARCHAR(64) columns, generated application-side (not DB identity/serial) as ULIDs via a small IIdGenerator interface in Application, implemented in Infrastructure. Binds: all entity primary keys. Prevents two engineers independently choosing GUID vs auto-increment int vs ad-hoc string ids for new entities. Rule: every new aggregate root's Id is assigned by calling IIdGenerator.NewId() before the entity is constructed, never left to the database to generate.
- (decision) AD-10 [ASSUMPTION] Service DTO boundary + mapping ownership: every I{Feature}Service method accepts and returns DTOs only at its public signature -- Domain entities never cross out of Application. Mapping between Domain entities and DTOs happens inside the service implementation via a static {Entity}Mapper class (ToDto/ToEntity methods) colocated in the same feature folder -- no AutoMapper (Jimmy Bogard's AutoMapper went commercial alongside MediatR in 2026, same licensing concern as AD-3). Binds: every Application service. Prevents controllers doing their own ad-hoc mapping and Domain entities leaking into Api-layer JSON responses.
- (decision) AD-11 [ASSUMPTION] Transaction/commit ownership: only the Application service method calls IUnitOfWork.SaveChangesAsync(), exactly once per use-case, after all repository calls for that use-case have staged their changes. Repositories only stage changes (Add/Update/Remove via DbContext) and never call SaveChangesAsync themselves. Binds: all write use-cases. Prevents partial-commit bugs when a use-case needs to touch two repositories, and stops repositories from silently committing before a use-case's business rules finish validating.
- (decision) AD-12 [ASSUMPTION] Cross-feature-folder reference rule: a feature's service (e.g. TutorService) may depend on another feature's SERVICE interface (e.g. ICourseService) to reuse its business rules, but never on another feature's REPOSITORY interface directly (e.g. TutorService must not inject ICourseRepository). Binds: Application-layer feature folders (extends AD-6). Prevents a feature bypassing another feature's validation/business rules by reaching straight into its data access.
- (decision) FIX AD-5/AD-10: Application-layer failure signaling is via typed exceptions deriving from a common AppException base (NotFoundException, ValidationException, ConflictException), caught by one global exception-handling middleware in Api that maps each to the correct ProblemDetails status code. Not a Result<T> wrapper -- keeps the convention idiomatic ASP.NET and avoids inventing a second, incompatible failure-signaling convention alongside exceptions that framework code (EF Core, model binding) already throws.
- (decision) FIX AD-8: named the design-time factory explicitly -- LearnSphereDbContextFactory : IDesignTimeDbContextFactory<LearnSphereDbContext>, in LearnSphere.Infrastructure/Persistence/. Added a lightweight process note (not an enforceable rule): only one engineer adds an EF Core migration at a time against the latest main, to avoid ModelSnapshot.cs merge collisions.
- (decision) FIX AD-8: clarified the startup migrate condition -- ASPNETCORE_ENVIRONMENT defaults to Production inside a plain container, not Development, so 'behind an IsDevelopment() check' would silently never fire in Docker Compose as originally worded. Replaced with an explicit RUN_MIGRATIONS_ON_STARTUP env var (default true in docker-compose.yml, unset/false anywhere else), checked in Program.cs -- decoupled from ASPNETCORE_ENVIRONMENT entirely so the behavior is explicit rather than inferred.
- (decision) AD-13 [ASSUMPTION] Docker/Compose operational envelope (the dimension the rubric walker flagged as under-specified despite being the user's explicit deployment choice): LearnSphere.Api/Dockerfile is a multi-stage build (mcr.microsoft.com/dotnet/sdk:10.0 to restore+publish, mcr.microsoft.com/dotnet/aspnet:10.0 to run). docker-compose.yml defines api + postgres services; api reads its connection string from ConnectionStrings__Default (the ASP.NET config double-underscore env-var convention, not a bespoke key); postgres uses a named volume (pgdata) for persistence and a healthcheck (pg_isready); api's depends_on the postgres healthcheck being 'service_healthy', not just container start. Binds: the deployment envelope. Prevents two engineers inventing different env var names for the same connection string, and the API container starting before Postgres is actually ready to accept connections.
- (decision) Deferred: PRD section 7's 'NGINX reverse proxy on port 3000' -- not adopted in this pass (no reverse proxy configured); explicitly deferred rather than silently dropped, revisit when actual deployment topology (cloud provider, TLS termination) is scoped.
- (event) spine finalized after reviewer-gate fixes applied.
- (decision) UPDATE AD-13: docker-compose.yml moved from BackEnd/ to repo root (per explicit user request to support frontend-only/backend-only/both-together deployment), now also defining a 'web' service (FrontEnd/Dockerfile, nginx-served static build) alongside postgres+api. All three services use Docker Compose profiles: postgres/api tagged [backend, all], web tagged [frontend, all] -- 'docker compose --profile backend up' / '--profile frontend up' / '--profile all up' select the right subset. This supersedes AD-13's original 'docker-compose.yml at BackEnd root' structural-seed line.
- (event) Verified end-to-end: dotnet build and dotnet test pass on host (0 errors, 6/6 tests). Docker build of the frontend 'web' image succeeds. Docker build of the 'api' image is blocked in this local environment by a NuGet restore TLS/certificate-chain failure (NU1301 UntrustedRoot) reaching api.nuget.org from inside the dotnet/sdk:10.0 container -- reproduced twice, non-transient. This is a local network/corporate-proxy TLS-interception characteristic of the current machine, not a defect in the Dockerfile, csproj, or code (dotnet build/test on the host, outside Docker, both succeed cleanly). Typical fix is trusting the org's root CA inside the SDK build stage (COPY the cert + update-ca-certificates) or pointing NuGet at an internal proxy -- left for the user's environment, noted in BackEnd README rather than worked around insecurely (no TLS-verification bypass).
- (decision) FIX AD-13: postgres:18-alpine refuses to start when the pgdata volume is mounted at /var/lib/postgresql/data (the pre-18 convention) -- reproduced live, container exits 1 with 'unused mount/volume' error. The 18+ images use pg_ctlcluster-style version-specific subdirectories under /var/lib/postgresql itself. Corrected docker-compose.yml's postgres volume mount to pgdata:/var/lib/postgresql.
- (event by user) Resumed run to resolve the 'AI microservice pipeline' Deferred item, now scoped by the New Course Wizard PRD + its UX spine update: AI Service Layer/gateway client placement, async publish job/worker execution model (~200+ AI calls per batch, per-node status, survives tab-close), describeNotation pipeline placement, version-storage data model (FR-25), budget-threshold concurrency mechanism. User chose Coaching path for this continuation (original run was Fast path/assumption-driven per this memlog's own texture) -- working the load-bearing calls interactively this time
- (version) Web-verified Aug 2026: Hangfire Core 1.8.24, LGPLv3, no 2025-2026 licensing change, different author/company than MediatR/AutoMapper (Hangfire OEU vs Jimmy Bogard) so no risk-pattern overlap with AD-3's MediatR rejection; Hangfire.PostgreSql 1.21.1 actively maintained, Postgres-backed job store, no Redis needed, ships retries+dashboard. Quartz.NET 3.19.0 Apache-2.0, scheduler/cron-centric, heavier fit for recurring jobs not a one-off 200-item burst. Native BackgroundService+hand-rolled Postgres jobs table (SELECT...FOR UPDATE SKIP LOCKED idiom): zero license concern, reasonable at this scale (single container, ~200 items/batch, low overall volume), but no free crash-recovery requeue or distributed locking (matters only if scaled to multiple API replicas later). NCronJob 4.10.2 MIT, scheduling-only, no durable persistence, not a full substitute
- (decision by user) Async publish job execution: Hangfire Core + Hangfire.PostgreSql (LGPLv3, Postgres-backed job store, no Redis needed, retries+dashboard built in) -- user chose over hand-rolled BackgroundService+Postgres-table after seeing both alternatives with licensing/fit research. Confirmed no risk-pattern overlap with the MediatR/AutoMapper author already rejected under AD-3
- (decision by user) FR-25 version storage: deep-copy snapshot per version (full confirmed content tree + cached Drill-Down/Way content copied on each publish), not diff/audit-log -- user chose simplicity/literal-match-to-FR-25-wording over storage efficiency, explicitly accepting the storage cost at this stage
- (decision by user) AI Service Layer placement follows the existing Clean Architecture shape (AD-2, AD-6): new IAiGateway interface (one method per AI Task -- ExtractStructureAsync, ExplainTopicAsync, RewriteExplanationAsync, GenerateExerciseAsync, DefineKeywordAsync, DescribeNotationAsync, embeddings) lives in a new cross-cutting Application/AiGateway/ folder (same tier as Common/, not owned by a single feature since extraction/drilldown/keywords/exercises all call it), single fat interface matching the PRD's FR-1 framing of ONE internal AI-service layer rather than per-task interfaces. HTTP-calling implementation (OpenAI-compatible client per PRD FR-2) lives in Infrastructure/AiGateway/, DI wired in Program.cs per AD-2. describeNotation runs as part of the same per-node extraction/authoring pipeline (not the publish-time batch) since alt-text is an authoring-time accessibility requirement (FR-16), not publish-gated content like Drill-Down/Ways
- (decision by user) Budget-threshold concurrency (FR-29): atomic Postgres UPDATE ... SET spent = spent + cost WHERE task_id = ... RETURNING spent counter, not a cached running total with periodic reconciliation -- standard, low-risk choice at this project's stated scale (single container, moderate volume); cached-reconciliation's added complexity (eventual consistency, background reconciliation job) only pays off at QPS this product isn't near. Low genuine debate, stated with rationale rather than full coaching round
- (question) AD-11's single-SaveChangesAsync-per-use-case rule needs an explicit carve-out for the async publish job: the use-case that KICKS OFF the Hangfire batch commits once (course -> Publishing sub-state), but each background job item commits its own generated content independently as it completes -- this is a new AD (job-item-commit pattern), not a bent AD-11, since publish-batch commits are legitimately many small independent commits, not one use-case's single unit of work
- (decision by user) Hangfire's server runs in-process inside the existing api container (app.UseHangfireServer() in Program.cs) against the existing Postgres instance -- no new Docker Compose service needed, resolves the earlier architecture-feasibility review's 'no worker/queue container' concern without adding deployment-envelope complexity. AD-13 gets a note, not a new service
- (event) Finalize step 1 (distill) complete: AD-14 through AD-18 added (IAiGateway placement, Hangfire async publish, AD-11 carve-out for job-item commits, FR-25 deep-copy snapshot, FR-29 atomic budget counter), AD-13 got an additive Hangfire in-process note, Stack table gained Hangfire+Hangfire.PostgreSql, Structural Seed gained AiUsage/AiGateway/Jobs folders, Deferred section's 'AI microservice pipeline' item resolved and replaced with 2 genuinely still-open items (Hangfire retry/backoff policy specifics, snapshot storage retention policy)
- (event) Finalize step 2 (reconcile) found and fixed 4 gaps: AD-14 extended with Polly-based fallback (FR-3) and fallback-event logging tie to AD-18; AD-15's Binds extended to cover extraction (FR-11-13), same Hangfire-job-per-item shape, not a second mechanism; new AD-19 added for DB-backed AI task config + prompt versioning (resolves FR-2/FR-5/FR-27/FR-28's central 'no redeploy' requirement, which was silent end-to-end before this pass); Domain/Application/Api additions for AiConfig and Tags (FR-26, confirmed net-new)
- (event) Finalize step 3 (Reviewer Gate) complete: rubric walker (2C/3H/4M/3L), version-verify lens (1H/1M/1L), adversarial lens (3C/3H/2M/2L) all dispatched as parallel subagents, reviews written to reviews/. All critical+high findings fixed directly: added AD-20 (Chapter/Topic/Subtopic/ContentBlock as 4 explicit entities, replacing stale Module/Lesson seed reference); AD-16 extended to cover extraction not just publish, plus a claimed-last-item atomic-counter pattern for batch-completion finalize (avoids needing commercial Hangfire Pro batches); AD-15 gained a Domain-level JobItemStatus contract confining Hangfire types to Infrastructure/Jobs/ (AD-1 compliance); AD-18 rewritten as pre-flight atomic reserve-against-threshold (was post-hoc recording only, didn't satisfy FR-29's 'block before' requirement) reading AD-19's AiTaskConfig as the single threshold source (was at risk of duplicating onto AiTaskBudget); AD-14's Polly license corrected MIT->BSD-3-Clause with version 8.7.0 added to Stack table; Deferred section's stale 'Auth not wired up' claim corrected against live code (AuthController/JwtTokenService/FeatureAuthorizationHandler already exist) -- also fixed the same stale claim in BackEnd/CLAUDE.md since it's derived from this spine and was actively misleading. Medium/low findings not independently actionable or already resolved as side effects of the critical/high fixes
- (event) Finalize step 4 (triage): no phase-blockers found. Deferred section's Hangfire retry/backoff policy and snapshot storage retention policy are legitimately post-implementation-observation items, not blockers. Remaining [ASSUMPTION]-tagged sub-decisions (Polly fallback wiring, job-item status contract, batch-completion latch pattern, budget pre-flight check, AiConfig DB-backed model, content-tree entity shape) are implementation-level judgment calls filling in gaps below the user's 3 explicit coaching choices (Hangfire, deep-copy snapshot, and by extension the architecture they enable) -- not blocking ambiguities. No renderings requested; build-substrate spine is the deliverable
- (event) spine finalized
- (decision by user) Cost-optimization amendment (2026-08-11, triggered by user mid-epics-creation): AD-14 updated to name the self-hosted Portkey OSS gateway (Apache-2.0, zero markup) as the target, replacing the PRD's original managed-then-self-hosted phasing with one gateway from day one. AD-13 grows from 3 to 6 Docker Compose services: ai-gateway (Portkey OSS), docling (new AD-21, free MIT self-hosted OCR/parsing, replaces paid LlamaParse), clamav (new AD-22, free GPLv2 self-hosted malware scanning, fills a previously-unnamed FR-11 gap). New Infrastructure/Parsing/ and Infrastructure/Scanning/ folders, new IFileScanner in Application/Common/. Stack table and deployment mermaid diagram updated. AD IDs stay stable and sequential (AD-21, AD-22 appended after AD-20, not renumbering anything)
