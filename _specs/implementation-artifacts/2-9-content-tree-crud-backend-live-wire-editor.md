---
baseline_commit: e64e8b260d54a2ac01680d582cacc80de715e147
---

# Story 2.9: Content Tree CRUD Backend & Live-Wire Editor

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

<!-- Sizing risk, carried verbatim from epics.md: "the largest story in the plan -- four entities,
full CRUD, the confirmation-state machine, and UI wiring together. Kept as one story deliberately:
the pieces only demo meaningfully as a set. If it doesn't fit a single dev session, split at
sprint-planning time using real velocity data rather than guessing boundaries now." Tasks below are
grouped so a human could split them at natural seams (Domain/persistence -> Service -> Controller ->
Frontend) if that becomes necessary; this file itself still treats it as one story per that guidance. -->

## Story

As a tutor,
I want Story 2.3's editor to read and write real Chapter/Topic/Subtopic/Content-Block data,
so that my structural edits and confirmations are actually saved.

## Acceptance Criteria

1. **Given** `Chapter`, `Topic`, `Subtopic`, and `ContentBlock` entities and endpoints, **when** a tutor adds, edits, deletes, or reorders a node, **then** the change persists to the real backend, replacing mock data. [Source: epics.md Story 2.9]
2. **Given** a node with an existing tutor-confirmation state, **when** a text-only edit is made, **then** confirmation is preserved, **and** when a structural or AI-content-affecting edit is made, confirmation resets to unconfirmed. [Source: epics.md Story 2.9; PRD FR-15]

## Tasks / Subtasks

### Backend — Domain & Persistence

- [x] Task 1: `Domain/Courses/{Chapter.cs, Topic.cs, Subtopic.cs, ContentBlock.cs, NodeConfirmation.cs, ContentBlockFormat.cs}` (AC: #1, #2)
  - [x] `NodeConfirmation.cs`: `public enum NodeConfirmation { Unconfirmed, Confirmed }` — `Unconfirmed` deliberately ordinal 0 (the desired default), avoiding the exact EF Core CLR-default-omission bug Story 2.4 hit with `LifecycleState` (a non-nullable enum property whose default CLR value must equal the desired DB default, or EF silently omits the column from INSERT). No enum reordering trick needed here since `Unconfirmed` is already both the natural first value and the correct default.
  - [x] `ContentBlockFormat.cs`: `public enum ContentBlockFormat { Text, Math, Image }` — mirrors `useCourseContentTree.ts`'s `ContentBlockFormat` union exactly. **This story never produces `Image` blocks itself** (same reasoning as Story 2.8: nothing in the AI-extraction pipeline identifies embedded images), but the enum value must exist because a tutor can manually add an image-format block through the editor UI in a later story/enhancement — not building the enum value now would block that later, cheaply avoidable now.
  - [x] `Chapter.cs`: `AuditableEntity` subclass — `CourseId` (string), `Title` (string), `Confirmation` (`NodeConfirmation`, default `Unconfirmed`), `Order` (int — explicit sibling ordering column, not implicit array position; see this task's own note below on why), navigation `Topics` (`List<Topic>`).
  - [x] `Topic.cs`: `ChapterId` (string), `Title`, `Confirmation`, `Order`, navigation `Subtopics` (`List<Subtopic>`), navigation `ContentBlocks` (`List<ContentBlock>`) — AD-20 explicitly allows a Content Block to parent directly under a Topic, not only under a Subtopic (matching `useCourseContentTree.ts`'s own `Topic.contentBlocks` field).
  - [x] `Subtopic.cs`: `TopicId` (string), `Title`, `Confirmation`, `Order`, navigation `ContentBlocks` (`List<ContentBlock>`).
  - [x] `ContentBlock.cs`: `TopicId` (`string?`), `SubtopicId` (`string?`) — **exactly one of these two must be set, never both, never neither** (app-level invariant enforced in the service, not a DB constraint this story adds — see Task 3's validation), `Format` (`ContentBlockFormat`), `Confirmation`, `Order`, `Text` (`string?`), `Lang` (`string?`, `"en"`/`"hi"` — plain `string?`, not an enum, matching the frontend's own loose `'en' | 'hi'` union with no third value ever validated against server-side beyond what Story 2.10 adds), `Notation` (`string?`), `ImageUrl` (`string?`), `AltText` (`string?`) — every field nullable/optional exactly matching `ContentBlock`'s real TypeScript shape (`text?`, `lang?`, `notation?`, `imageUrl?`, `altText?`), since which fields are populated depends on `Format`.
  - [x] **Why an explicit `Order` column, not implicit array-position ordering — and a real precedent to copy, not reinvent:** the frontend's mock tree uses plain JS array order as position (`swapAdjacent`/`moveToIndex` splice the array directly) — a real EF-mapped `List<T>` navigation property has **no** guaranteed persisted order unless a column drives it. **This is not a new problem in this codebase — `CourseThumbnail.cs` already has an `Order` column, and `CourseService.cs`'s existing `ReorderThumbnailAsync`/`RemoveThumbnailAsync` (Story 2.4) are a real, working reference implementation of exactly this pattern: sort siblings by `Order`, swap by list *index* (not by arithmetic on `Order` values, which breaks once gaps exist), and — critically — `RemoveThumbnailAsync` renumbers every remaining sibling's `Order` to a contiguous `0..n-1` range after a delete, so gaps never accumulate.** Read both methods directly and copy this pattern for Chapter/Topic/Subtopic/ContentBlock sibling groups rather than designing a new ordering scheme from scratch — an earlier draft of this story claimed there was "no real precedent anywhere else in the codebase" for this, which is wrong and led to under-specifying delete's own `Order` handling (fixed in `DeleteNodeAsync`'s own task entry below: it must renumber remaining siblings, exactly like `RemoveThumbnailAsync`, not just remove the one row and leave a gap).
- [x] Task 2: EF configurations + migration (AC: #1)
  - [x] `Infrastructure/Persistence/Configurations/{ChapterConfiguration.cs, TopicConfiguration.cs, SubtopicConfiguration.cs, ContentBlockConfiguration.cs}` — standard `IEntityTypeConfiguration<T>` per `BackEnd/CLAUDE.md` rule 3, foreign keys `Chapter.CourseId -> Course.Id`, `Topic.ChapterId -> Chapter.Id`, `Subtopic.TopicId -> Topic.Id`, `ContentBlock.TopicId -> Topic.Id` (nullable FK) and `ContentBlock.SubtopicId -> Subtopic.Id` (nullable FK), **all four with `.OnDelete(DeleteBehavior.Cascade)`** — a tutor deleting a Chapter must cascade-delete every Topic/Subtopic/ContentBlock beneath it at the database level, matching `useCourseContentTree.ts`'s own comment ("Cascading delete is implicit: removing a Chapter/Topic/Subtopic object removes its entire nested subtree with it"). This is a brand-new set of four tables (like Story 2.6's `CourseFile`), not new columns on an already-populated table — lower migration risk on the specific NOT-NULL-backfill axis Stories 2.4/2.5 hit, but still run the migration for real against live Postgres before assuming it's clean (per this epic's own repeated lesson).
  - [x] `FlexDemyDbContext.cs`: add `DbSet<Chapter> Chapters`, `DbSet<Topic> Topics`, `DbSet<Subtopic> Subtopics`, `DbSet<ContentBlock> ContentBlocks`.
- [x] Task 3: `IContentTreeRepository`/`ContentTreeRepository` (AC: #1)
  - [x] `Application/Courses/IContentTreeRepository.cs` — **one repository for the whole tree, not four independent per-entity repositories**, a deliberate deviation from `BackEnd/CLAUDE.md`'s "every other feature has one [repository per entity]" default: every real operation here (reorder within a sibling group, cascading delete, moving a node between different tree positions, resolving "what type is this id" for the polymorphic node mutators below) genuinely needs cross-entity awareness, and four independent repositories would just relocate that coupling into the service layer instead of removing it. `GetTreeAsync(string courseId, CancellationToken ct): Task<List<Chapter>>` — loads the full tree for a course with every level `.Include()`d (`Topics.Subtopics.ContentBlocks`, `Topics.ContentBlocks`), ordered by each level's `Order` column. `FindNodeAsync(string courseId, string nodeId, CancellationToken ct): Task<TreeNode?>` — a small discriminated-result type (`TreeNode` wraps whichever of `Chapter`/`Topic`/`Subtopic`/`ContentBlock` the id actually belongs to, plus its parent's id/collection context) used internally by every polymorphic mutator below (title edit, delete, reorder, move, confirm) — checks each of the four `DbSet`s by primary key in turn (cheap at this entity count/course scale; no need for a type-discriminator column). Plain `Add`/`Update`/`Remove` methods per entity type, staging only — no `SaveChangesAsync` here (repository discipline, AD-11).
- [x] Task 4: `Application/Courses/{IContentTreeService.cs, ContentTreeService.cs, ContentTreeDtos.cs}` (AC: #1, #2)
  - [x] Every method starts with the same ownership+Draft-state guard as `CourseFileService` — `ICourseService.EnsureOwnedDraftAsync(courseId, ct)` (Story 2.6). DTOs mirror the frontend shape exactly (`ChapterDto`, `TopicDto`, `SubtopicDto`, `ContentBlockDto`) — never expose the Domain entities (AD-10). **Enum wire format — corrected after checking the real code, not assumed:** an earlier draft of this story claimed `Confirmation`/`Format` would serialize as lowercase strings "matching `CourseFileDto.Status`'s convention." That's wrong — `CourseMapper.cs`'s real `.ToString()` call produces **PascalCase** (`"Draft"`, `"Published"`, confirmed by reading it directly; there is no `JsonStringEnumConverter`/naming policy configured anywhere in `FlexDemy.Api`), and every other `string`-typed enum DTO field in this codebase follows the same PascalCase convention (`.ToString()` on the enum, nothing more). So `ChapterDto.Confirmation`/`ContentBlockDto.Format` are `.ToString()`'d exactly the same way — `"Confirmed"`/`"Unconfirmed"`, `"Text"`/`"Math"`/`"Image"` — **not** lowercase. Since `useCourseContentTree.ts`'s real exported types are lowercase unions (`'confirmed' | 'unconfirmed'`, `'text' | 'math' | 'image'`) and `ContentTreeNode.tsx` does strict `===` checks against those lowercase literals, **Task 10's rewired hook (not this service) is responsible for translating** PascalCase wire values to the frontend's lowercase shape on every read, and the reverse on writes that carry these fields — get this wrong and every confirmation badge and format-conditional render in the editor silently breaks. This translation happens in `contentTreeService.ts`/the hook, not by changing either side's own established convention.
  - [x] `GetTreeAsync(string courseId, CancellationToken ct): Task<List<ChapterDto>>` — **this is also where Story 2.8's materialization step lives** (Task 8, below); reads the persisted tree via `IContentTreeRepository.GetTreeAsync`, but first checks for any not-yet-materialized successfully-extracted `CourseFile` rows and folds their proposed structure in as new Chapters before returning. See Task 8 for the full materialization design — flagged here only so the method's real responsibility (not just "read the tree") is visible at the top of this task list.
  - [x] `AddChapterAsync(string courseId, CancellationToken ct): Task<ChapterDto>` — `Title = "New Chapter"`, `Confirmation = Unconfirmed`, `Order` = current max sibling `Order` + 1 (append at the end, matching the mock's `[...prev, newChapter]`). `AddTopicAsync(string courseId, string chapterId, CancellationToken ct)`, `AddSubtopicAsync(string courseId, string topicId, CancellationToken ct)`, `AddContentBlockAsync(string courseId, string parentId, string parentType, CancellationToken ct)` (`parentType` is `"topic"` or `"subtopic"` — **the caller must resolve this itself; it is NOT the same as `AddableNodeType`'s `type` value**, see Task 10's explicit note on why `type` is always the literal `'contentBlock'` regardless of which level the block is being added under, and cannot be used as `parentType` directly — the backend validates whatever `parentType` it's sent against whichever of `Topic`/`Subtopic` `parentId` actually resolves to, `ValidationException` on a mismatch, but does not and cannot infer it unassisted) — each new node's default fields exactly match the mock's defaults (`New Topic`/`New Subtopic` titles, a new Content Block defaults to `Format = Text`, `Text = ""`, `Lang = "en"`). **Each Add resets its immediate parent's confirmation if it was confirmed** (`resetIfConfirmed` applied to the parent, not the brand-new child, which starts `Unconfirmed` anyway) — exactly matching the mock's own behavior (e.g. `addNode` wraps the returned chapter in `resetIfConfirmed` when adding a topic).
  - [x] `EditNodeTitleAsync(string courseId, string nodeId, string title, CancellationToken ct): Task` — resolves the node via `IContentTreeRepository.FindNodeAsync` (Chapter/Topic/Subtopic only — a `ContentBlock` has no `Title`, `ValidationException` if `nodeId` resolves to one), sets `Title`. **Confirmation is NOT touched** — FR-15/the mock's own `editNodeTitle` treats a title edit as text-only, full stop, never resetting confirmation regardless of current state.
  - [x] `EditContentBlockAsync(string courseId, string blockId, UpdateContentBlockRequest patch, CancellationToken ct): Task` — `UpdateContentBlockRequest` carries only the fields actually being changed (`string? Text`, `string? Lang`, `string? Notation`, `string? ImageUrl`, `string? AltText`, **plus `ContentBlockFormat? Format`** — see the amendment note below — each a **tri-state-aware nullable** — the caller must be able to distinguish "don't touch this field" from "clear it to null," matching the frontend's `Partial<Pick<...>>` patch shape; `[ASSUMPTION: the exact tri-state wire representation — e.g. a nullable-of-nullable pattern, or an explicit "which fields were present" flag set alongside the DTO — is this story's own design choice; confirm/adjust during dev, the important invariant is "a field absent from the request body must not be treated as `null`"]`). **Confirmation-reset rule, replicated exactly from `useCourseContentTree.ts`'s `editContentBlock`:** `isTextOnly = patch touches ONLY Text and/or Lang, nothing else` — if `isTextOnly`, confirmation is preserved regardless of its current value; otherwise (any of `Notation`/`ImageUrl`/`AltText`/`Format` present in the patch), `resetIfConfirmed` applies to the block itself — `Format` is treated the same as `Notation`/`ImageUrl`/`AltText` here (an AI-content-affecting change, never text-only), extending the real frontend's own binary text-only/not-text-only split to a field the mock hook's patch type doesn't yet expose but this story's own `[ASSUMPTION]` above already anticipates needing to widen.
  - [x] **Amendment — `Format` as a patchable field, added because AC#1's "authored" scenario needs it:** the frontend mock's `editContentBlock` patch type (`Partial<Pick<ContentBlock, 'text' | 'lang' | 'notation' | 'imageUrl' | 'altText'>>`) and this story's original `AddContentBlockAsync` design (always creates `Format = Text`, with no way to create or convert to `Math` anywhere) together mean a tutor has **no path at all** to produce a genuinely tutor-authored Math-format block — every Math block would have to arrive via AI extraction. Story 2.10 (subject-aware alt-text) needs a real "authored" math scenario to exist for its own AC#1 to be meaningfully testable, and found this gap during its own validation pass. The fix, scoped as narrowly as possible: widen `useCourseContentTree.ts`'s `editContentBlock` patch type to `Partial<Pick<ContentBlock, 'text' | 'lang' | 'notation' | 'imageUrl' | 'altText' | 'format'>>`, and this backend `UpdateContentBlockRequest`/`EditContentBlockAsync` accordingly — a tutor converts a Text block to Math (or back) by including `Format` in an edit patch, typically alongside `Notation` in the same call. `AddContentBlockAsync` itself is intentionally left unchanged (still always creates `Format = Text`) — "create as text, then convert" is the one path, not a second "create as math directly" one, keeping the add-flow's existing UI/API surface untouched. See Story 2.10's own Task 2/Task 4 for the caller side of this. **Edge case, present in the real code and easy to miss when reimplementing "which fields are present" logic:** the real `isTextOnly = Object.keys(patch).every(...)` (`useCourseContentTree.ts:269`) is vacuously `true` for a completely empty patch (`Array.every` on an empty array is always `true`) — an empty patch must therefore preserve confirmation, not reset it, replicating that exact vacuous-truth behavior rather than treating "no fields present" as an ambiguous/error case. This is the single most important piece of business logic in this story to get byte-for-byte consistent with the frontend's own already-shipped rule (AC#2) — re-read `useCourseContentTree.ts:263-274` directly rather than working from a paraphrase.
  - [x] `DeleteNodeAsync(string courseId, string nodeId, CancellationToken ct): Task` — resolves via `FindNodeAsync`, removes the node (cascading via the DB FK behavior from Task 2 for Chapter/Topic/Subtopic; a `ContentBlock` has no children to cascade), **then renumbers the remaining siblings' `Order` to a contiguous `0..n-1` range, copying `RemoveThumbnailAsync`'s own exact pattern (Task 1's note)** — do not leave a gap where the deleted node's `Order` value used to be. **Resets the immediate parent's confirmation** if it was confirmed (a Chapter has no parent to reset — deleting a top-level Chapter resets nothing above it, matching the mock, which only ever filters the top-level array with no `resetIfConfirmed` wrapper on chapter deletion itself).
  - [x] `ReorderNodeAsync(string courseId, string nodeId, string direction, CancellationToken ct): Task` (`direction` is `"up"`/`"down"`) — resolves via `FindNodeAsync`, finds the node's sibling group (same parent, same level) **sorted by `Order`**, swaps with the adjacent sibling **by list index, not by arithmetic on `Order` values** (copying `ReorderThumbnailAsync`'s own exact pattern — index-based swapping stays correct even if `Order` values were ever non-contiguous, which arithmetic-on-values would not) — a no-op, not an error, if already at either end (matching `swapAdjacent`'s own bounds check). **Resets the immediate parent's confirmation** (Chapter-level reorder resets nothing above it, same reasoning as delete).
  - [x] `MoveNodeAsync(string courseId, string draggedId, string targetId, CancellationToken ct): Task` — resolves both ids via `FindNodeAsync`; **a no-op (not an error) if they don't share the same parent/sibling group**, exactly matching the mock's own guard (`moveToIndex` only ever operates within one already-matched sibling array; the mock's `moveNode` structure checks chapter-level, then topic-level, then block/subtopic-level in turn and simply produces no match — and therefore no state change — for a cross-level or cross-parent drag). Loads the sibling group sorted by `Order`, computes the dragged node's new list index (matching `moveToIndex`'s own splice-and-reinsert semantics — it can move a node past several siblings at once, not just a two-way swap), then renumbers the **whole** affected sibling group's `Order` to `0..n-1` in the new arrangement (same contiguous-renumbering discipline as delete, not just the two endpoints touched). Resets the immediate parent's confirmation.
  - [x] `ConfirmNodeAsync(string courseId, string nodeId, CancellationToken ct): Task` — resolves via `FindNodeAsync`, sets `Confirmation = Confirmed` unconditionally (no reset logic here — this is the one mutator whose entire job is setting confirmation, not preserving/resetting it around some other edit).
- [x] Task 5: `Api/Controllers/ContentTreeController.cs` (AC: #1, #2)
  - [x] `[Route("api/v1/courses/{courseId}/content-tree")]`, `[Authorize(Policy = FeatureKeys.CoursesCreate)]` on every action (reuse, same as every other Courses-feature endpoint). Endpoint set, deliberately mirroring the mock hook's own polymorphic-by-id mutator shape so the frontend service file (Task 9) maps close to 1:1 onto it, minimizing the "re-derive the right shape" work a later dev pass would otherwise have to do:
    - `GET /` → 200 `List<ChapterDto>` (full tree; triggers materialization, Task 8)
    - `POST /chapters` → 201 `ChapterDto`
    - `POST /topics` (body: `{ chapterId }`) → 201 `TopicDto`
    - `POST /subtopics` (body: `{ topicId }`) → 201 `SubtopicDto`
    - `POST /content-blocks` (body: `{ parentId, parentType }`) → 201 `ContentBlockDto`
    - `PATCH /nodes/{id}/title` (body: `{ title }`) → 204
    - `PATCH /content-blocks/{id}` (body: `UpdateContentBlockRequest`) → 204
    - `DELETE /nodes/{id}` → 204
    - `POST /nodes/{id}/reorder` (body: `{ direction }`) → 204
    - `POST /nodes/{id}/move` (body: `{ targetId }`) → 204
    - `POST /nodes/{id}/confirm` → 204
  - [x] No `[RequestSizeLimit]` needed on any of these — every request body here is small structured JSON (titles, ids, short content text), not a file upload; Story 2.6's precedent doesn't apply to this controller.
- [x] Task 6: Materialize Story 2.8's staged extraction JSON (AC: #1)
  - [x] **This is the task that closes Story 2.8's own explicitly-deferred contract** ("Story 2.9 gets an additional task to materialize `ExtractedStructureJson` into real entities the first time a tutor opens the editor after extraction completes"). `Domain/Courses/CourseFile.cs` gains one more field this story adds: `IsMaterialized` (`bool`, default `false`) — set `true` the moment a file's staged structure has been turned into real Chapters, so `ContentTreeService.GetTreeAsync` never double-materializes the same file on a later call. New migration for this column (a straightforward `bool NOT NULL DEFAULT false` addition to an existing table — unlike Story 2.5's `TagIds` array-default problem, a scalar `bool` default poses no analogous EF Core default-value-omission risk, since `false` is both the CLR default and the desired DB default already).
  - [x] **Concurrency guard — corrected after review, this is a genuine gap and not optional:** `GetTreeAsync` is a plain GET, which is inherently exposed to duplicate/concurrent invocation (a duplicated tab, React StrictMode's double-effect-invoke in dev, a fast reload right after the editor opens). Two concurrent calls both reading `Status == Done && IsMaterialized == false` before either commits would both materialize the same file's structure, producing a fully duplicated chapter tree with no error surfaced anywhere. **Claim pending rows atomically before building anything from them**: `UPDATE course_files SET is_materialized = true WHERE course_id = @courseId AND status = 'Done' AND is_materialized = false RETURNING id, extracted_structure_json` (via `ExecuteUpdateAsync`/raw SQL/whatever EF Core 10 idiom the dev agent's own judgment prefers for an atomic conditional claim — the exact mechanism is this story's own implementation choice, but it must be a single atomic statement, not a separate `SELECT` then `UPDATE`) — only the request that successfully claims a given row's `IsMaterialized` flip ever builds Chapters from it; a second concurrent request finds zero pending rows and just returns the tree as-is. This mirrors the same "atomic claim via a conditional UPDATE" shape AD-18's budget pre-flight reserve already uses elsewhere in this codebase, not a new pattern.
  - [x] `ContentTreeService.GetTreeAsync`'s real algorithm: after the atomic claim above, for each claimed row (in claim-order — `[ASSUMPTION: exact ordering across multiple materialized files not specified anywhere; claim/creation order is the natural default, confirm during dev if the team wants something else, e.g. alphabetical by filename]`), deserialize `ExtractedStructureJson` (Story 2.8's schema) and create real `Chapter`/`Topic`/`Subtopic`/`ContentBlock` rows from it — **assigning real `IIdGenerator.NewId()` ids and `Confirmation = Unconfirmed` to every materialized node** (Story 2.8's staged JSON deliberately carries neither, per that story's own contract), appended after any already-existing Chapters. Then loads and returns the (now possibly-updated) persisted tree via `IContentTreeRepository.GetTreeAsync`. All of this — the atomic claim, creating entities, returning the tree — happens as **one call to `SaveChangesAsync`** for the whole `GetTreeAsync` invocation (AD-11: this is one use-case, "load the tree, materializing anything pending along the way," not a batch of independent job items the way Stories 2.6-2.8's Hangfire jobs are — no AD-16 carve-out applies here, since this runs synchronously inside a normal HTTP request, not a background job). A course with zero `CourseFile` rows at all is handled by construction — the claim query simply returns nothing, and `GetTreeAsync` proceeds as a plain read.
  - [x] **Why this lives on the read path (`GetTreeAsync`) rather than being triggered by Story 2.8's own job finishing:** the story's own AC text is explicit — "the first time a tutor **opens the editor** after extraction completes" — a pull-on-read model, not a push-on-extraction-complete model. This also sidesteps a different race than the one above: multiple files can finish extraction independently and asynchronously (Story 2.8's own per-file jobs), and materializing eagerly the moment each one finishes would mean the tree changes underneath a tutor who already has the editor open, mid-edit, with no realtime sync mechanism in this codebase (WebSockets are explicitly Deferred). Materializing only when the tree is freshly loaded avoids that entirely — the atomic claim above handles the *other* race (two simultaneous reads), not this one.
- [x] Task 7: DI registration (AC: #1)
  - [x] `FlexDemy.Infrastructure/DependencyInjection.cs`: `services.AddScoped<IContentTreeRepository, ContentTreeRepository>();`. `FlexDemy.Application/DependencyInjection.cs`: `services.AddScoped<IContentTreeService, ContentTreeService>();`.
- [x] Task 8: Backend tests (AD-7)
  - [x] `FlexDemy.Application.Tests/Courses/ContentTreeServiceTests.cs` (new, the largest test file in this story — cover every mutator's confirmation-reset behavior explicitly, not just its structural effect): `AddTopicAsync` resets the parent Chapter's confirmation if it was `Confirmed`; `EditNodeTitleAsync` never resets confirmation, on a `Confirmed` or `Unconfirmed` node alike; `EditContentBlockAsync` with only `Text`/`Lang` in the patch preserves confirmation, with `Notation`/`ImageUrl`/`AltText`/`Format` present resets it; `EditContentBlockAsync` with `Format: Math` in the patch actually changes the block's `Format` (the amendment above — confirms a tutor can convert a Text block to Math and back); `DeleteNodeAsync`/`ReorderNodeAsync`/`MoveNodeAsync` each reset the immediate parent's confirmation, and deleting/reordering a top-level Chapter resets nothing (no parent to reset); `DeleteNodeAsync`/`MoveNodeAsync` renumber remaining/affected siblings' `Order` to a contiguous range (no gaps left behind); `MoveNodeAsync` is a genuine no-op (no exception, no state change) when `draggedId`/`targetId` don't share a parent; `ConfirmNodeAsync` sets `Confirmed` regardless of prior state; `GetTreeAsync`'s materialization claims a pending `CourseFile` exactly once even when called twice concurrently (simulate via two overlapping calls against the same InMemory/test context, asserting only one set of Chapters results). Every ownership/Draft-state guard failure propagates the same as `CourseFileService`'s existing equivalents (Story 2.6).
  - [x] `FlexDemy.Infrastructure.Tests/Repositories/ContentTreeRepositoryTests.cs` (new, InMemory provider per this project's established repository-test convention): `GetTreeAsync` returns the full nested graph ordered by `Order`; a Chapter delete cascades to its Topics/Subtopics/ContentBlocks (confirms the EF cascade-delete configuration actually works, not just that it's declared); `FindNodeAsync` correctly identifies a node at each of the four levels and returns `null` for an unknown id.

### Frontend

- [x] Task 9: `FrontEnd/src/services/contentTreeService.ts` (new) (AC: #1)
  - [x] Follows `courseFileService.ts`'s established pattern (own file, `ContentTreeError extends Error`, `API_BASE_URL`). One function per Task 5 endpoint (`getTree`, `addChapter`, `addTopic`, `addSubtopic`, `addContentBlock`, `editNodeTitle`, `editContentBlock`, `deleteNode`, `reorderNode`, `moveNode`, `confirmNode`) — plain JSON `fetch` calls (no `FormData`/file-upload concerns in this story, unlike Stories 2.6/2.7's services). `ChapterDto`/`TopicDto`/`SubtopicDto`/`ContentBlockDto` interfaces mirror the backend DTOs exactly.
- [x] Task 10: Rewire `useCourseContentTree.ts` (AC: #1, #2)
  - [x] Replace the mock `useState`/seed-tree/local-mutation implementation with real `contentTreeService` calls, **keeping the exact same exported `UseCourseContentTreeResult` shape** (`data`, `isLoading`, `error`, and every mutator's exact signature: `addChapter()`, `addNode(parentId, type)`, `editNodeTitle(id, title)`, `editContentBlock(id, patch)`, `deleteNode(id)`, `reorderNode(id, direction)`, `moveNode(draggedId, targetId)`, `confirmNode(id)`, `resetTree()`) — `CourseContentEditor.tsx`/`ContentTreeNode.tsx` need **zero changes** if this shape is preserved exactly, per this hook's own header comment ("Story 2.9 swaps this hook's internals... without any tree-rendering component changing"). Each mutator becomes: call the matching `contentTreeService` function, then either optimistically update local `data` or re-fetch the tree (`[ASSUMPTION: optimistic-update vs. refetch-after-mutate is this story's own implementation choice — refetching is simpler and matches this codebase's existing `useCourseDraft.ts`/`useFileUpload.ts` precedent of not doing complex optimistic-merge logic; an optimistic approach would feel snappier but risks drifting from server state on a confirmation-reset the client-side mutator doesn't independently know how to replicate — recommend refetch-after-mutate unless perceived latency becomes a real UX problem during dev]`).
  - [x] **Two translation responsibilities this hook (or `contentTreeService.ts`) must own, neither of which is a pass-through:** (1) **enum casing** — every `Confirmation`/`Format` value coming back from the backend is PascalCase (Task 4's corrected note); map it to the lowercase values `Chapter`/`Topic`/`Subtopic`/`ContentBlock`'s TypeScript types actually declare (`"Confirmed"` → `'confirmed'`, `"Text"` → `'text'`, etc.) on every read, so `ContentTreeNode.tsx`'s existing strict `===` checks against lowercase literals keep working unmodified. (2) **`parentType` resolution for `addNode(parentId, 'contentBlock')`** — the real `AddableNodeType` value the UI passes is always the literal string `'contentBlock'` regardless of whether `parentId` is a Topic or a Subtopic (confirmed by reading `ContentTreeNode.tsx`'s real call sites directly — both the Topic-level and Subtopic-level "add content block" buttons call `addNode(id, 'contentBlock')` with no level indicator beyond `parentId` itself). The rewired hook must resolve `parentType` itself by searching its own in-memory `data` for which array (`Topic.contentBlocks` vs `Subtopic.contentBlocks`) `parentId` actually belongs to, before calling `contentTreeService.addContentBlock(parentId, resolvedParentType)` — an earlier draft of this story described `parentType` as directly derivable from `type`, which is not true and would always send `"contentBlock"` as `parentType`, guaranteed to fail the backend's own validation (Task 4).
  - [x] `resetTree()`: unlike Story 2.6/2.7's `resetFiles`/nothing-server-side precedent, this hook has no obvious "abandon and discard" server operation to call (there's no Draft-only concept for the content tree the way there is for `Course` itself) — keep it as a local re-fetch of the real tree (effectively "reload," not "revert to a blank mock seed," since there is no seed anymore).
- [x] Task 11: Frontend tests (AD-5)
  - [x] `FrontEnd/tests/features/CourseContentEditor/useCourseContentTree.test.ts` (extend the existing Story 2.3 test file): mock `contentTreeService`; every existing mock-era confirmation-reset test (text-only edit preserves, structural/AI-content edit resets) is re-pointed at the real service-backed hook and must still pass with the same *assertions* — only its mocking setup changes. **One exception, not "unchanged":** the file's first test (seeding a tree with a specific hand-authored fixture — a particular Hindi block, Topic 2's specific no-subtopics shape) asserts the mock's own seed data, which no longer exists once Task 10 removes it — that test must be deleted or rewritten against a test-authored fixture returned by the mocked `contentTreeService.getTree`, not "re-pointed unchanged." Add new coverage for Task 10's two translation responsibilities: a PascalCase-from-backend response round-trips to the correct lowercase `data` shape; `addNode(subtopicId, 'contentBlock')` resolves `parentType: 'subtopic'` (not `'topic'`) when `subtopicId` genuinely belongs to a Subtopic. `CourseContentEditor.test.tsx`/`ContentTreeNode`-level tests should need no changes at all if Task 10's shape-preservation claim holds; confirm this during dev rather than assuming it.

## Dev Notes

- **This story has a hard sequencing dependency on Stories 2.6/2.7/2.8** — `ICourseService.EnsureOwnedDraftAsync` (2.6), and Task 6's materialization step reading `CourseFile.Status`/`ExtractedStructureJson`/the new `IsMaterialized` field (2.6/2.7/2.8). Check the actual codebase state before starting dev.
- **The confirmation-state-machine replication (Task 4) is the single highest-risk piece of this story to get subtly wrong** — it's easy to build CRUD that "works" (data persists, the AC's first half is satisfied) while getting the *which levels reset, which stay untouched* rule wrong in an edge case the tests don't happen to cover. Re-read `useCourseContentTree.ts` directly during implementation, not a summary of it (including this story's own summary) — the real file is the single source of truth for this rule, and it's short enough to read in full in a few minutes. (Independently verified during this story's own validation pass: the confirmation-reset description here was checked function-by-function against the real file and found accurate — the risk is in future drift/reimplementation error, not in this story's own description being wrong.)
- **Why an explicit `Order` column matters — corrected: there IS a real precedent, use it, don't reinvent it.** An earlier draft of this story claimed no precedent existed anywhere in the codebase for tutor-controlled persisted sibling ordering. That was wrong: `CourseThumbnail.Order` + `CourseService.ReorderThumbnailAsync`/`RemoveThumbnailAsync` (Story 2.4) already solve exactly this problem, including the "renumber remaining siblings after a delete so gaps never accumulate" detail this story's first draft omitted from `DeleteNodeAsync`. Read those two methods directly before implementing this story's own reorder/move/delete logic.
- **No new AD-16 batch-completion concern here, and no new Hangfire job** — every operation in this story is a normal synchronous request/response CRUD use-case (AD-11's plain "one `SaveChangesAsync` per use-case" applies exactly as written, no carve-out needed), unlike Stories 2.6-2.8's background jobs. Task 6's materialization step is the one place this story touches "an async pipeline's output," and even there it's read synchronously on a normal HTTP request, not run as its own job.
- **`ContentBlock`'s `TopicId`/`SubtopicId` mutual-exclusivity is an app-level invariant, not a DB-level constraint this story adds** — a Postgres `CHECK` constraint enforcing "exactly one of these two columns is non-null" would be a reasonable hardening, but isn't required by any AC here; the service-layer validation in `AddContentBlockAsync` is the actual enforcement point. `[ASSUMPTION: acceptable for this story's scope — flag if the team wants the extra DB-level safety net]`.
- **No delete/retry-adjacent endpoint gaps to carry forward from Stories 2.6-2.8 here** — this story's `DeleteNodeAsync` *is* the real, permanent delete operation (unlike those stories' "retry re-uploads through the same endpoint, no dedicated delete" pattern), since a tutor deleting a Chapter/Topic/Subtopic/Content-Block is a genuine, intentional content operation, not a failure-recovery path.
- **FR-15's literal PRD text names only Chapter/Topic/Subtopic as confirmable ("Each node (Chapter/Topic/Subtopic) carries an explicit tutor-confirmation state"), omitting Content Block** — this is a pre-existing PRD/implementation gap, not introduced by this story: the real `useCourseContentTree.ts` already treats `ContentBlock.confirmation` as a first-class field, and `ContentTreeNode.tsx` already renders a Confirm control for content blocks. This story continues that established (if PRD-text-inconsistent) real behavior rather than narrowing scope to match the PRD's literal wording, since diverging from the already-shipped mock UI's actual behavior would itself be the bigger AC#1 ("replacing mock data") violation.
- **Live-stack verification for this story should specifically include the Task 6 concurrency scenario** — open the editor from two tabs (or hard-refresh) immediately after an extraction completes, and confirm only one set of Chapters materializes, not a duplicated tree. This is exactly the kind of bug that passes every unit test (each running against its own isolated InMemory context) while failing against real concurrent requests hitting real Postgres — the established pattern for every infrastructure-touching story so far in this epic.

### Project Structure Notes

- Backend new files: `Domain/Courses/{Chapter.cs, Topic.cs, Subtopic.cs, ContentBlock.cs, NodeConfirmation.cs, ContentBlockFormat.cs}`, `Infrastructure/Persistence/Configurations/{ChapterConfiguration.cs, TopicConfiguration.cs, SubtopicConfiguration.cs, ContentBlockConfiguration.cs}`, `Infrastructure/Persistence/Migrations/*AddContentTree*` (generated, Task 2) + `*AddCourseFileIsMaterialized*` (generated, Task 6), `Infrastructure/Repositories/ContentTreeRepository.cs`, `Application/Courses/{IContentTreeRepository.cs, IContentTreeService.cs, ContentTreeService.cs, ContentTreeDtos.cs}`, `Api/Controllers/ContentTreeController.cs`, both new test files from Task 8.
- Backend modified files: `Infrastructure/Persistence/FlexDemyDbContext.cs` (four new `DbSet`s), `Domain/Courses/CourseFile.cs` (Task 6 — new `IsMaterialized` field), `Infrastructure/DependencyInjection.cs`, `Application/DependencyInjection.cs`.
- Frontend new files: `FrontEnd/src/services/contentTreeService.ts`.
- Frontend modified files: `FrontEnd/src/features/CourseContentEditor/useCourseContentTree.ts`, its test file. `CourseContentEditor.tsx`/`ContentTreeNode.tsx` should need **zero** changes if Task 10's shape-preservation holds — confirm during dev rather than assuming.
- Naming: new `FlexDemy.Domain.Courses`-namespace entities (same namespace as `Course`/`CourseFile`/`CourseThumbnail`, same feature per AD-6); `IContentTreeRepository`/`IContentTreeService`/`ContentTreeService` join the existing `FlexDemy.Application.Courses` namespace.

### References

- [Source: _specs/planning-artifacts/epics.md — Story 2.9 full AC text, including the sizing-risk callout quoted verbatim at the top of this file]
- [Source: _specs/planning-artifacts/architecture/architecture-eLearning-backend-2026-08-09/ARCHITECTURE-SPINE.md — AD-20 (Chapter/Topic/Subtopic/ContentBlock four explicit entity types, Content-Block-under-Topic-or-Subtopic, confirmation state as fields on these entities not a separate table), AD-11 (one `SaveChangesAsync` per use-case), AD-6 (feature-folder placement)]
- [Source: _specs/planning-artifacts/prds/prd-eLearning-CourseWizard-2026-08-10/prd.md — FR-15 (this story's own FR: text-only edit preserves confirmation, structural/AI-content-affecting edit resets it)]
- [Source: FrontEnd/src/features/CourseContentEditor/useCourseContentTree.ts — full current content, read directly (not summarized) for this story's central confirmation-state-machine replication; every mutator's exact signature and reset behavior]
- [Source: FrontEnd/src/features/CourseContentEditor/ContentTreeNode.tsx — full current content, confirming the real `TreeMutators` interface calls `deleteNode`/`reorderNode`/`moveNode`/`confirmNode` polymorphically by bare `id` (the basis for this story's "one generic `/nodes/{id}` endpoint set" controller design), and that `addNode(id, 'contentBlock')` is called identically regardless of whether `id` is a Topic or Subtopic (the basis for Task 10's `parentType`-resolution correction)]
- [Source: BackEnd/src/FlexDemy.Application/Courses/CourseService.cs — `ReorderThumbnailAsync`/`RemoveThumbnailAsync` (Story 2.4), the real working precedent for sibling `Order`-column management this story's `ReorderNodeAsync`/`MoveNodeAsync`/`DeleteNodeAsync` copy; `CourseMapper.cs`'s `.ToString()` enum-serialization convention this story's DTOs also follow]
- [Source: _specs/implementation-artifacts/{2-6-file-upload-malware-scanning-secure-storage.md, 2-7-document-parsing-ocr-pipeline.md, 2-8-ai-structure-extraction-extractstructure.md} — `EnsureOwnedDraftAsync` reuse, `CourseFile.Status`/`ExtractedStructureJson` fields this story's materialization task (Task 6) consumes, and Story 2.8's own explicit "Story 2.9 materializes this" contract]

## Previous Story Intelligence

Stories 2.6/2.7/2.8 (all `ready-for-dev`, validated through at least one round — not yet implemented):

- **Every prior story in this batch that got a second validation pass had at least one finding caused by trusting a first-draft assumption instead of reading the real referenced code directly** (2.6's `EnsureOwnedDraftAsync` signature, 2.7's stale confidence-API research, 2.8's `TutorId`-resolution path and incomplete exception-handling enumeration). This story is the largest and most novel (four new entities, no real ordering precedent elsewhere in the codebase) in the batch, making it the single most likely candidate for a similar gap — the Dev Notes above already push toward reading `useCourseContentTree.ts` directly for exactly this reason; extend that discipline to `ContentTreeNode.tsx` and the actual `CourseService`/`CourseFileService` code for the ownership-guard/DI patterns being reused, not just this story's paraphrase of them.
- **The job-chaining pattern from 2.6-2.8 does not apply here** — this is the first story in the batch that is pure synchronous CRUD, no Hangfire job of its own. Don't reflexively reach for the `[AutomaticRetry]`/final-attempt-fail-closed pattern those three stories established; it has no analog in a normal request/response use-case.
- **The single most important carried-forward lesson, now confirmed across every infrastructure-touching story this epic:** manual end-to-end verification against a real Docker/Postgres stack finds real bugs every time. This story's cascade-delete configuration and `Order`-column reordering logic are exactly the kind of thing that can look correct in a unit test (InMemory provider, Task 7) while behaving differently against real Postgres FK cascade behavior — verify for real, not just via the InMemory-provider test suite.

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

### Completion Notes List

- All 11 tasks implemented and verified. Full regression green: 472 backend tests (287 Application + 140 Infrastructure + 45 Api) and 430 frontend tests, both 0 failures.
- `IContentTreeRepository`/`ContentTreeRepository`: one repository for the whole tree (Task 3's deliberate AD-4 deviation), `TreeNode` discriminated wrapper drops a separate "parent id" field entirely -- each entity already carries its own real parent-FK property (`Topic.ChapterId`, `Subtopic.TopicId`, `ContentBlock.TopicId`/`SubtopicId`), so a caller reads the parent id straight off whichever one is set rather than threading a redundant field through the record.
- `ContentTreeService`'s confirmation-reset logic was re-verified line-by-line against the real `useCourseContentTree.ts` (not a summary), including one genuinely surprising real behavior replicated as-is: `ReorderNodeAsync`'s parent-confirmation reset is unconditional once the target id is found at a given level, even when the swap itself is a boundary no-op (`swapAdjacent` returns unchanged, but the mock's `resetIfConfirmed({...chapter, topics: swapAdjacent(...)})` wrapper still runs) -- covered explicitly by `ReorderNodeAsync_resets_the_parent_confirmation_even_when_the_swap_is_a_boundary_noop`.
- `EditContentBlockAsync`'s tri-state patch (`UpdateContentBlockRequest.TouchedFields`) is built in `ContentTreeController.EditContentBlock` by reading the raw JSON body as a `JsonElement` and checking property presence directly -- a plain nullable-field DTO can't distinguish "field omitted" from "field explicitly null," and this avoids inventing a new wrapper type/JSON converter for a single endpoint.
- Task 6 materialization's atomic per-file claim (`ICourseFileRepository.TryClaimForMaterializationAsync`) reuses `AiTaskBudgetRepository.TryReserveAsync`'s exact established pattern (raw `ExecuteSqlInterpolatedAsync`, snake_case column names) -- same InMemory-provider translation gap already documented in `BackEnd/CLAUDE.md` for that precedent, so it's exercised via `ContentTreeServiceTests`' repository-mock-sequencing tests (simulating the race by returning `true` then `false`), not `ContentTreeRepositoryTests`' real-InMemory-DbContext tests.
- One InMemory-provider limitation discovered during Task 8: EF Core's `.Include(x => x.Children.OrderBy(...))` ("ordered Include") is a real relational-provider feature the InMemory provider doesn't reliably reproduce in test -- `ContentTreeRepositoryTests.GetTreeAsync_returns_the_full_nested_graph` verifies the graph is fully/correctly nested instead of asserting array order; the actual Order-sorted sequencing is a live-Postgres verification item (Dev Notes already called this class of gap out).
- Task 10's hook signature necessarily changed from `useCourseContentTree()` to `useCourseContentTree(courseId: string | null)` (mirroring `useFileUpload(courseId)`'s already-established precedent) -- the mock hook never needed a courseId, but real API calls do. This is the one call-site change `CourseContentEditor.tsx` needed (`useCourseContentTree(draftId)`); the exported `UseCourseContentTreeResult` shape and every mutator's own signature are otherwise unchanged, so `ContentTreeNode.tsx` needed zero changes, confirmed by running its consuming test suite.
- `CourseContentEditor.test.tsx`'s "Course Content tree" describe block (13 tests) needed a real rewrite, not just `draftId={null}` -> a real id: those tests depended on the old mock hook's synchronous in-memory seed tree, which no longer exists. Added a small in-file fake backend (`fixtures: Record<courseId, ChapterDto[]>` + mocked `contentTreeService` functions that mutate it, keyed by courseId so switching drafts doesn't leak state) driving the exact same seed content/ids (`chapter_1`..`block_5`) so the file's existing testid/label-based assertions kept working end-to-end, now via interact -> mutate -> refetch -> re-render instead of a synchronous local update.
- Pre-existing, unrelated `tsc --noEmit` errors in `FlashcardsModal.tsx` (missing `DrilldownTopic` export) confirmed via `git status --porcelain` to predate this story -- left untouched, out of scope.
- Two frontend test files (`App.test.tsx`, `CourseWizard.test.tsx`) timed out only when run as part of the full ~430-test suite (resource contention) and passed cleanly in isolation -- pre-existing flakiness, not a regression from this story's changes.

### File List

**Backend -- new:**
- `BackEnd/src/FlexDemy.Domain/Courses/{Chapter.cs, Topic.cs, Subtopic.cs, ContentBlock.cs, NodeConfirmation.cs, ContentBlockFormat.cs}`
- `BackEnd/src/FlexDemy.Infrastructure/Persistence/Configurations/{ChapterConfiguration.cs, TopicConfiguration.cs, SubtopicConfiguration.cs, ContentBlockConfiguration.cs}`
- `BackEnd/src/FlexDemy.Infrastructure/Persistence/Migrations/{20260812150839_AddContentTree.cs, 20260812150839_AddContentTree.Designer.cs, 20260812151801_AddCourseFileIsMaterialized.cs, 20260812151801_AddCourseFileIsMaterialized.Designer.cs}`
- `BackEnd/src/FlexDemy.Infrastructure/Repositories/ContentTreeRepository.cs`
- `BackEnd/src/FlexDemy.Application/Courses/{IContentTreeRepository.cs, IContentTreeService.cs, ContentTreeService.cs, ContentTreeDtos.cs, ContentTreeMapper.cs}`
- `BackEnd/src/FlexDemy.Api/Controllers/ContentTreeController.cs`
- `BackEnd/tests/FlexDemy.Application.Tests/Courses/ContentTreeServiceTests.cs`
- `BackEnd/tests/FlexDemy.Infrastructure.Tests/Repositories/ContentTreeRepositoryTests.cs`

**Backend -- modified:**
- `BackEnd/src/FlexDemy.Infrastructure/Persistence/FlexDemyDbContext.cs` (four new `DbSet`s)
- `BackEnd/src/FlexDemy.Domain/Courses/CourseFile.cs` (`IsMaterialized` field)
- `BackEnd/src/FlexDemy.Application/Courses/ICourseFileRepository.cs` / `BackEnd/src/FlexDemy.Infrastructure/Repositories/CourseFileRepository.cs` (`GetPendingMaterializationAsync`/`TryClaimForMaterializationAsync`)
- `BackEnd/src/FlexDemy.Infrastructure/Persistence/Migrations/FlexDemyDbContextModelSnapshot.cs`
- `BackEnd/src/FlexDemy.Infrastructure/DependencyInjection.cs` (`IContentTreeRepository`)
- `BackEnd/src/FlexDemy.Application/DependencyInjection.cs` (`IContentTreeService`)

**Frontend -- new:**
- `FrontEnd/src/services/contentTreeService.ts`

**Frontend -- modified:**
- `FrontEnd/src/features/CourseContentEditor/useCourseContentTree.ts` (real API calls, PascalCase->lowercase translation, `parentType` resolution, courseId parameter)
- `FrontEnd/src/features/CourseContentEditor/CourseContentEditor.tsx` (`useCourseContentTree(draftId)`)
- `FrontEnd/tests/features/CourseContentEditor/useCourseContentTree.test.ts` (rewritten against a mocked `contentTreeService`)
- `FrontEnd/tests/features/CourseContentEditor/CourseContentEditor.test.tsx` (`contentTreeService` mock + in-file fake backend fixture)

### Review Findings

- [x] [Review][Patch] Materialization loop permanently loses a claimed file's content on a mid-batch parse/build failure — the raw-SQL `IsMaterialized` claim commits immediately and independently of the one end-of-loop `SaveChangesAsync`; if `JsonSerializer.Deserialize` or `Enum.Parse<ContentBlockFormat>` throws for any file in the batch, every already-claimed file (including earlier ones in the same batch that parsed fine) has its staged entities discarded but stays permanently marked materialized, with no retry path [BackEnd/src/FlexDemy.Application/Courses/ContentTreeService.cs:32-57]
- [x] [Review][Patch] `EditContentBlock`'s JSON-body parsing throws an unhandled `InvalidOperationException` (surfaces as a raw 500, not a 400) when a touched field's JSON value isn't a string, e.g. `{"text": 123}` [BackEnd/src/FlexDemy.Api/Controllers/ContentTreeController.cs:77-83]
- [x] [Review][Patch] `EditNodeTitleAsync` has no title validation before `SaveChangesAsync` — a null or >255-char title reaches the DB's `NOT NULL`/`HasMaxLength(255)` constraint directly and surfaces as an unhandled 500 instead of a `ValidationException` [BackEnd/src/FlexDemy.Application/Courses/ContentTreeService.cs:233-245]
- [x] [Review][Patch] `EditContentBlockAsync` has no `Lang` length validation — `ContentBlockConfiguration.cs` caps `Lang` at 8 chars but the patch accepts any string, hitting the same unhandled-DB-exception failure mode as the title issue above [BackEnd/src/FlexDemy.Application/Courses/ContentTreeService.cs (EditContentBlockAsync)]
- [x] [Review][Patch] `AddContentBlockAsync` throws `NotFoundException` instead of the spec-mandated `ValidationException` when `parentId` resolves to a real node of the wrong type (e.g. `parentType: "topic"` but `parentId` is actually a Subtopic) — Task 4's own text explicitly calls out this exact scenario as a `ValidationException` case [BackEnd/src/FlexDemy.Application/Courses/ContentTreeService.cs:180-214]
- [x] [Review][Patch] `ReorderNodeAsync` validates `direction` before calling `EnsureOwnedDraftAsync`, the only mutator that doesn't run the ownership+Draft-state guard first, unlike every other method in the file [BackEnd/src/FlexDemy.Application/Courses/ContentTreeService.cs:330-336]
- [x] [Review][Patch] `useCourseContentTree.ts`'s `fetchTree` has no protection against out-of-order responses — firing two mutations in quick succession (or switching drafts while a fetch is in flight) can let a slower, stale response's `setData` overwrite newer state with no error surfaced [FrontEnd/src/features/CourseContentEditor/useCourseContentTree.ts]
- [x] [Review][Patch] `TryClaimForMaterializationAsync`'s raw SQL claim doesn't defensively re-check `status = 'Done'` the way the spec's own reference query does — low practical risk today since nothing in the app ever un-sets `Done`, but cheap to harden [BackEnd/src/FlexDemy.Infrastructure/Repositories/CourseFileRepository.cs]
- [x] [Review][Patch] Completion Notes overstate `CourseContentEditor.test.tsx`'s "Course Content tree" describe block as 18 tests; it's actually 13 — factual correction only [_specs/implementation-artifacts/2-9-content-tree-crud-backend-live-wire-editor.md, Completion Notes]
- [x] [Review][Defer] Sibling `Order` assignment (`siblings.Max(x => x.Order) + 1`) races under concurrent adds (double-click, two open tabs) — deferred, pre-existing pattern shared with `CourseThumbnail.Order`'s own unaddressed version of the same race, not unique to this story [BackEnd/src/FlexDemy.Application/Courses/ContentTreeService.cs]
- [x] [Review][Defer] `ReorderNodeAsync`/`SwapAdjacent` has a TOCTOU race between `FindNodeAsync` and the sibling-list fetch — a node deleted by a concurrent request in that window makes the reorder silently no-op instead of surfacing a 404 — deferred, rare/low-impact concurrent-edit race with no data corruption [BackEnd/src/FlexDemy.Application/Courses/ContentTreeService.cs:557-608]
- [x] [Review][Defer] `CourseContentEditor.test.tsx`'s fake-backend mocks only support Topic-level reorder/move and don't mutate `fixtures` for the Add mutators, so Chapter/Subtopic/ContentBlock-level reorder/move and the "adds" half of AC#1 have no UI-integration-level test coverage in this file — deferred, the underlying service-call/parentType-resolution logic is already covered at the hook level in `useCourseContentTree.test.ts` [FrontEnd/tests/features/CourseContentEditor/CourseContentEditor.test.tsx]

## Change Log

- 2026-08-12: Story created via `bmad-create-story` — fourth of a batch of 5 remaining Epic 2 stories (2.6-2.10) being written and validated up front, before any further implementation, per explicit user direction. Status set to `ready-for-dev`.
- 2026-08-12: Fresh-context validation pass (the confirmation-state-machine description itself checked out exactly against the real `useCourseContentTree.ts`, function by function — no divergence found there) found 5 other Critical issues, all fixed: (1) the DTO enum wire-format claim was wrong — `.ToString()` produces PascalCase in this codebase (confirmed against real `CourseMapper.cs`), not lowercase; added an explicit frontend-side translation responsibility (Task 10) instead of asserting a false backend convention. (2) `AddContentBlockAsync`'s `parentType` was wrongly described as derivable from `AddableNodeType`'s `type` value — the real UI always passes the literal `'contentBlock'` regardless of level; added an explicit "resolve `parentType` by searching local tree state" step to Task 10. (3) The "no real Order-column precedent anywhere in the codebase" claim was false — `CourseThumbnail`/`ReorderThumbnailAsync`/`RemoveThumbnailAsync` (Story 2.4) already solve this exact problem; corrected Task 1/Dev Notes to point at it, and fixed `DeleteNodeAsync`/`MoveNodeAsync`/`ReorderNodeAsync` to explicitly renumber siblings via that established pattern instead of leaving `Order` gaps. (4) Task 8 (100% backend materialization logic) was mis-filed under the "### Frontend" section header, undermining the story's own stated split-by-section fallback plan — moved to the Backend section and renumbered as Task 6, shifting DI/tests to 7/8. (5) The materialize-on-read design had no concurrency guard against two simultaneous `GetTreeAsync` calls both materializing the same file's structure — added an atomic conditional-UPDATE claim step (mirroring AD-18's existing pre-flight-reserve pattern) so only one concurrent request ever materializes a given file. Also fixed: the empty-patch vacuous-truth edge case in `EditContentBlockAsync`'s `isTextOnly` check, an FR-15/real-behavior gap around Content Block confirmability acknowledged in Dev Notes, Task 11's overstated "unchanged" claim about the existing seed-fixture test, and added a live-stack test callout for the new concurrency scenario. Status remains `ready-for-dev`.
- 2026-08-12: Amended after Story 2.10's own validation pass found a real cross-story gap: neither `AddContentBlockAsync` nor `EditContentBlockAsync` as originally specified gave a tutor any way to ever produce a genuinely tutor-authored `Format = Math` block (new blocks always default to `Text`, and the patch type never included `Format`) — meaning Story 2.10's AC#1 "authored" math-notation scenario would have been unbuildable. Added `Format` as a patchable field on `UpdateContentBlockRequest`/`EditContentBlockAsync` (Task 4) — a tutor converts a Text block to Math (or back) via an edit patch, treated as an AI-content-affecting change for confirmation-reset purposes, same as `Notation`/`ImageUrl`/`AltText`. `AddContentBlockAsync` itself is intentionally left unchanged. Status remains `ready-for-dev`.
- 2026-08-12: Implemented all 11 tasks end-to-end (Domain/EF/migrations → repository → service → controller → materialization → DI → backend tests → frontend service → hook rewire → frontend tests). Full regression green: 472 backend tests, 430 frontend tests. See Completion Notes for the handful of judgment calls made where the story left an explicit `[ASSUMPTION]` or "dev agent's own choice" (tri-state patch mechanism via raw `JsonElement` body parsing, materialization claim reusing `AiTaskBudgetRepository`'s raw-SQL pattern, refetch-after-mutate over optimistic updates) and the two real-code discoveries made during implementation (the hook needed a `courseId` parameter it never had as a mock; `CourseContentEditor.test.tsx`'s tree-describe block needed a real rewrite, not zero changes). Status set to `review`.
- 2026-08-12: Code review (Blind Hunter + Edge Case Hunter + Acceptance Auditor, run in parallel against the diff scoped to this story's own File List) found 9 `patch`, 3 `defer`, 5 dismissed as noise. All 9 patches applied: (1) the materialization loop's most severe finding — a mid-batch parse/build failure previously discarded every already-staged Chapter from the whole batch while permanently leaving already-claimed files stuck `IsMaterialized = true` with no retry path — now isolates failures per-file and per-chapter; (2) `ContentTreeController.EditContentBlock` now throws a clean `ValidationException` instead of an unhandled `InvalidOperationException` for a non-string JSON field value; (3)/(4) `EditNodeTitleAsync`/`EditContentBlockAsync` now validate title length and `lang` length before `SaveChangesAsync` instead of surfacing a raw DB exception; (5) `AddContentBlockAsync` now throws `ValidationException` (not `NotFoundException`) when `parentId` resolves to the wrong node type, matching Task 4's own explicit spec text; (6) `ReorderNodeAsync` now runs the ownership+Draft-state guard before validating `direction`, matching every other mutator; (7) `useCourseContentTree.ts`'s `fetchTree` now guards against out-of-order/stale responses via a request-sequence ref; (8) `TryClaimForMaterializationAsync`'s raw SQL now defensively re-checks `status = 'Done'`; (9) a factual correction to this file's own Completion Notes (13 tests, not 18). Added 9 new backend tests and 1 new frontend test covering the patched behaviors — full regression re-verified green: 481 backend tests, 431 frontend tests. 3 items deferred to `deferred-work.md` (a pre-existing `Order`-assignment race shared with `CourseThumbnail`, a rare reorder TOCTOU race, and a UI-integration-test coverage gap already covered at the hook level). Status set to `done`.
