---
baseline_commit: 245d80348a8c8e55a2e4dbd2037bbd83385a56ce
---

# Story 7.4: Autosave & Confirmation Tracking

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a tutor,
I want every edit to save automatically with clear save-state feedback, and to see which nodes/pages still need my sign-off,
So that I never lose work and always know what's left before I can move to Review.

## Acceptance Criteria

1. **Given** the tutor is typing in any block or heading **When** they stop typing (debounce) or blur/close the block **Then** it autosaves with an explicit saved/saving/failed indicator; a failed save is loud, retryable, and never navigates away from unsaved content (FR-15, FR-34)
2. **Given** the tutor closes the editor mid-sentence on an unfinished heading **When** they reopen the course **Then** every character already typed is preserved, cursor position aside (FR-15, FR-16)
3. **Given** a node or page has only had text-only edits (Name/Description/Title/body text) **When** those edits save **Then** its confirmation state is preserved; a structural edit (add/delete/reorder/move a child, add/remove/re-role a resource, or a non-text body block change) resets its immediate parent to Unconfirmed (FR-44)
4. Every drag interaction has a keyboard-operable equivalent, block/outline reordering announces via `aria-live`, the outline is a proper tree with roving tabindex, and focus is never lost across autosave or block conversion (FR-47)
5. Ownership + Draft-state guards apply to every mutation, and per-course bounded limits (chapters, pages per node, resources per page, body length) are enforced server-side with clear errors (FR-48, outline/page half)
6. Confirmed state is shown as a filled check-glyph beside the heading and Unconfirmed as an outlined circle glyph — a shape difference, never color-alone (UX-DR8)

## Tasks / Subtasks

- [x] Task 1 — Backend: retrofit FR-44's structural-reset semantics into Stories 7.2/7.3's mutation methods (AC: #3)
  - [x] Stories 7.2/7.3 built `CreateTopicAsync`, `DeleteTopicAsync` (+Subtopic equivalents), `ReorderTopicAsync`/`ReorderSubtopicAsync`, `CreatePageAsync`, `DeletePageAsync`, `ReorderPageAsync`, `MovePageAsync` — none of them set `IsConfirmed = false` on the affected **immediate parent** yet (they only default a *new* entity's own `IsConfirmed` to `false` on create). This story adds that: every one of the methods above now also loads and flips its immediate parent's `IsConfirmed` to `false` before `SaveChangesAsync` (still exactly one commit per use-case, per `Backend/CLAUDE.md`'s rule 4 — don't add a second `SaveChangesAsync` call, extend the existing unit of work). "Immediate parent" is: for a Topic/Subtopic mutation, the owning Chapter/Topic; for a Page mutation, whatever `OwnerType`/`OwnerId` it's attached to (Chapter, Topic, or Subtopic)
  - [x] `UpdateChapterAsync`/`UpdateTopicAsync`/`UpdateSubtopicAsync`/`UpdatePageAsync` (title/description/body-text edits) must **not** touch `IsConfirmed` — confirm this is already true (it should be, since Stories 7.1–7.3 never wrote reset logic into these) and add a regression test proving a text-only `PUT` leaves `IsConfirmed` untouched, since this is the exact behavior a careless "just always reset on any write" retrofit could silently break
  - [x] `MovePageAsync` resets **both** the source and destination immediate parents, plus the moved Page's own confirmation (FR-44's explicit third case — "A page move resets both source and destination immediate parents AND the moved page's own confirmation")
  - [x] Every mutation response DTO (`ChapterDocumentDto`, `TopicDocumentDto`, `SubtopicDocumentDto`, `PageDocumentDto` — all four already exist from Stories 7.1–7.3) already carries `isConfirmed`; no DTO shape change needed here, only the service-layer logic populating it correctly
  - [x] Tests: create/delete/reorder/move each independently reset the correct immediate parent (not a sibling, not the grandparent) to Unconfirmed; a title-only or description-only `PUT` leaves `IsConfirmed` unchanged; `MovePageAsync` resets three entities in one call (source parent, destination parent, the page itself)

- [x] Task 2 — Backend: `GET content/outline` — the whole-course, body-free outline (AD-4's `CourseContentContext` data source) (AC: #3, #6)
  - [x] This endpoint doesn't exist yet — Stories 7.1–7.3 only built `GET content/chapters` (id/title/order list) and `GET content/chapters/{id}/document` (one Chapter, full bodies). Per the backend Additional Requirements: `GET /outline` is **distinct** from the document endpoint specifically because it "omits `Page.BodyMarkdown`" — a lightweight, whole-course tree for outline/rail rendering, not a per-Chapter fetch
  - [x] `IContentService.GetOutlineAsync(courseId)` → `OutlineDto { chapters: OutlineChapterDto[] }`, each `OutlineChapterDto { id, title, description, isConfirmed, order, topics: OutlineTopicDto[] }`, nested the same way down through Subtopics and Pages — `title`/`description`/`isConfirmed`/`order` only at every level, **no `bodyMarkdown`** anywhere in this DTO (that's what makes it lightweight; don't reuse `ChapterDocumentDto` for this, it's a materially different shape, even though the two share most field names)
  - [x] `GET content/outline` on `ContentController`, ownership-only read check (same non-Draft-gated pattern as every other read this feature has built)
  - [x] This endpoint's consumers, beyond this story's own `CourseContentContext`: Story 11.1's Move-to-Review blocker list ("lists every blocker as a direct link into the outline") and Story 11.2's Preview-as-student node/course-scope navigation will both depend on this exact shape — don't narrow it to only what this story's own UI needs; it's already correctly scoped from the epics doc's own Appendix B description, just implement it now rather than a story-specific subset

- [x] Task 3 — Backend: bounded limits (NFR4) retrofitted into every create path (AC: #5)
  - [x] Server-side checks, returning a `ValidationException` with a specific message naming the limit (never a silent failure or an unhandled DB constraint violation): 100 chapters/course (`CreateChapterAsync`), 100 topics/chapter (`CreateTopicAsync`), 50 sub-topics/topic (`CreateSubtopicAsync`), 200 pages/node (`CreatePageAsync`, checked against the target `OwnerType`+`OwnerId`'s existing sibling count), 256 KB/page body (`UpdatePageAsync`, on `BodyMarkdown` length — reject before saving, not truncate)
  - [x] These are retrofits into Stories 7.1–7.3's existing create/update methods, same file, same methods — not a new validation layer bolted on separately. Add one test per limit: creating the 101st chapter (etc.) is rejected with a message naming "100"; a 257 KB page body is rejected before any DB write

- [x] Task 4 — Frontend: `CourseContentContext.tsx` (AD-4) — the deferred piece, built now (AC: #3, #6)
  - [x] Stories 7.1–7.3 deliberately deferred this ("a reasonable Story 7.2 concern... don't invent a Context prematurely") — this is that story. New `FrontEnd/src/context/CourseContentContext.tsx`: holds the **currently-open course's** outline metadata (titles, Descriptions, confirmation state — never page bodies, per AD-4), fetched via the new `getOutline(courseId)` call (Task 5), keyed by course ID, mounted at the point `CourseContentEditor` opens (not globally at `App.tsx`'s composition root the way `DomainContext`/`SessionContext` are — this Context's data is meaningless outside an open editor session; confirm this scoping choice against `App.tsx`'s actual provider-mounting pattern before wiring it, and note any deviation in Completion Notes)
  - [x] **Reconciles a real design tension, resolved here:** Story 7.2's `TableOfContentsRail.tsx` derives its entries (position, heading level, title) by walking the *live Tiptap document* directly, specifically so rail order/content structurally can't drift from native screen-reader heading-navigation (UX-DR7). Confirmation glyphs are a separate concern — the rail looks up each entry's `isConfirmed` value from `CourseContentContext` (keyed by node id) rather than either (a) duplicating confirmation state as a second, driftable source inside Tiptap node attrs, or (b) rebuilding the rail's structure from Context instead of the document. **Structure = Tiptap document (source of truth); confirmation display = Context.** Update `TableOfContentsRail.tsx` accordingly — this is a real, motivated change to Story 7.2's file, not a rewrite of its structure-derivation logic

- [x] Task 5 — Frontend: `courseContentService.ts` — `getOutline` (AC: #3, #6)
  - [x] `getOutline(courseId): Promise<OutlineDto>` (`GET .../content/outline`), added to the same service file Stories 7.1–7.3 already extended

- [x] Task 6 — Frontend: the real `useContentAutosave.ts` (AD-11) (AC: #1, #2)
  - [x] Stories 7.1–7.3 each shipped a **bare-minimum stub** (a direct `PUT` call on blur, explicitly flagged in each of their Dev Notes as deferred to this story). This task builds the real thing and **replaces** those stubs — don't leave three independent ad hoc save call-sites alongside this new hook; every heading/body edit routes through it after this story lands
  - [x] On each debounced save tick: walk the ProseMirror doc from the edited position outward to find the nearest preceding structural heading/Page marker and the next heading of equal-or-higher level (the exact boundary-detection Story 7.3's Task 3 already reused for Page-body scoping — this hook is that same logic's permanent home, not a second implementation); extract only that entity's own Markdown slice via `@tiptap/markdown`'s serializer; dispatch to the one correct endpoint (`PUT chapters/{id}`, `PUT topics/{id}`, `PUT subtopics/{id}`, or `PUT pages/{id}`) via `courseContentService.ts`
  - [x] Explicit `saved` / `saving` / `failed` states per edited entity, rendered as visible text near the affected block (not a bare spinner) — a failed save is loud, offers a retry action, and the block's content is never discarded or navigated away from while unsaved (AC #1's explicit "never navigates away from unsaved content" — there is no page-level "leave without saving?" dialog anywhere in this app per `NFR3`/`EXPERIENCE.md`'s State Patterns row "Autosave failed," so this indicator is the *only* safety net, not one of several)
  - [x] Confirmation-state resync: the `PUT` response's `isConfirmed` field (Task 1) is patched directly into `CourseContentContext`'s corresponding node — **never a full outline refetch** (AD-11's explicit rule; a refetch would also defeat the point of AC #4's "focus never lost" requirement, since a Context-driven re-render of the whole outline is a much larger blast radius than patching one field)
  - [x] A newly-inserted structural heading or Page's create call already fires synchronously per Story 7.3's Task 5 — this hook doesn't change that sequencing, only owns the *update* path for content already persisted

- [x] Task 7 — Frontend: `aria-live` reversion announcement (AC: #3, part of FR-47)
  - [x] When a structural edit's response reveals a parent flipped from Confirmed to Unconfirmed, announce it at the moment it happens (`aria-live="polite"`) — "a tutor may not expect their edit to have un-confirmed something" (`EXPERIENCE.md`'s Accessibility Floor, verbatim reasoning). Reuse `CourseContentEditor.tsx`'s existing batched-announcement pattern (`STATUS_ANNOUNCE_DEBOUNCE_MS`/`flushAnnouncement`, already built for file-upload status in Story 7.1's untouched shell code) rather than a third independent `aria-live` region in the same component tree

- [x] Task 8 — Frontend: roving-tabindex tree semantics for the ToC rail (AC: #4)
  - [x] Story 7.2's rail handles click/Enter activation with real DOM focus-move (UX-DR7) but not full tree semantics. This story adds `role="tree"` on the rail container, `role="treeitem"` per entry (nested `aria-level`/`aria-expanded` where Sub-Topics nest under Topics), and roving `tabindex` (only the currently-active entry is `tabindex="0"`, every other entry is `tabindex="-1"`; Arrow Up/Down moves the roving position and focus together, matching standard ARIA treeview keyboard conventions)
  - [x] Reordering (drag or the Story 7.2 keyboard move-up/down buttons) announces via `aria-live`, matching FR-47's explicit requirement — verify Story 7.2's reorder implementation already does this (it should, per that story's own Task 5); if not, this is the story that closes the gap
  - [x] Focus-preservation audit across autosave and block-type conversion: since this hook's debounced save can complete asynchronously after the user has already moved focus elsewhere, verify no re-render this hook triggers (patching `CourseContentContext`, updating a `saved`/`saving` indicator) ever steals focus back — write a test that types in block A, moves focus to block B before A's autosave resolves, and asserts focus is still on B after resolution

- [x] Task 9 — Frontend: Confirmed/Unconfirmed glyph on headings (AC: #6)
  - [x] Filled check-glyph (Confirmed) vs. outlined circle-glyph (Unconfirmed) beside every Chapter/Topic/Sub-Topic heading, sourced from `CourseContentContext` (Task 4) — per `DESIGN.md`'s `content-doc-heading.confirmedState` token, `{colors.signal-green}` filled check vs. `{colors.muted}` outlined circle, **shape difference, never color-alone**. Page markers already carry this via their `{components.badge-pill}` (built in Story 7.3) — this task is specifically the three heading levels that didn't have a visible confirmation glyph before now

- [x] Task 10 — Tests
  - [x] Backend per Tasks 1–3's own bullets
  - [x] `FrontEnd/tests/features/CourseContentEditor/useContentAutosave.test.ts` (new): debounce fires once after typing stops (not once per keystroke); blur fires immediately without waiting for the debounce; a failed save shows the retryable error state and doesn't clear the block's unsaved content; a successful save's `isConfirmed` response value reaches `CourseContentContext` without a full outline refetch (mock the context and assert the specific patch call, not a full-context reload)
  - [x] `FrontEnd/tests/context/CourseContentContext.test.tsx` (new): fetches `getOutline` on mount for a given course id; exposes per-node `isConfirmed` lookups the rail and heading glyphs both consume

## Dev Notes

- **This is the story every prior Epic 7 story explicitly deferred to.** Stories 7.1, 7.2, and 7.3 each shipped a minimal save stub and an all-`false`-by-default `IsConfirmed` column specifically so this story could build the real mechanism once, not patch three inconsistent partial versions. Read all three story files' Dev Notes before starting — each names exactly what it left for this story.
- **`CourseContentContext` is a genuinely new file**, not an update — despite the frontend architecture spine describing it as if it already exists (that spine describes the target state of the whole epic, same caveat Story 7.1's Dev Notes already flagged for the backend side).
- **Architecture:** AD-11 in full (this is its primary implementation story — boundary-detection, create-call sequencing was 7.3's partial exercise, confirmation-resync is this story's own), AD-4 (`CourseContentContext`'s exact scope: outline metadata only, never bodies), FR-44 (the three reset cases: child mutation, resource mutation — not buildable yet, Epic 8 — and page move), NFR3 (no page-level unsaved-changes dialog exists anywhere in this app; the per-block indicator this story builds **is** the safety net, not a redundant layer on top of one).
- **UX:** `EXPERIENCE.md`'s Accessibility Floor bullets on heading/page confirmation-state exposure and reversion announcement; State Patterns row "Autosave failed" (visible-text inline error, explicit retry, `aria-live`).
- **Existing code to read before editing:** all of Stories 7.1–7.3's backend `ContentService`/`ContentController`/DTOs (every mutation method this story retrofits), `courseContentService.ts`, `CourseContentEditor.tsx`'s existing announcement-batching code (reused, not reinvented), `TableOfContentsRail.tsx` (Story 7.2 — extended here, not rewritten), `App.tsx` (to confirm where a scoped, editor-session-lifetime Context should mount versus the composition-root pattern `DomainContext`/`SessionContext` use).
- **Scope boundary vs. Epic 8:** FR-44's resource-mutation reset case ("add/remove/re-role a resource") has no resources to test against yet — Resource doesn't exist until Story 8.1. Build the reset-trigger *shape* generically (a private helper like `ResetImmediateParentConfirmation(ownerType, ownerId)` that Story 8.1 can call from its own resource mutations) rather than hardcoding it to only the Topic/Subtopic/Page call sites this story has today.
- **Git context:** no new commits since Stories 7.1–7.3 were authored in this same session.

### Project Structure Notes

- New frontend file: `FrontEnd/src/context/CourseContentContext.tsx`.
- No new backend files — every backend task extends `ContentService.cs`/`ContentController.cs`/`ContentRepository.cs`/existing DTOs from Stories 7.1–7.3, plus new DTOs (`OutlineDto` and its nested types) in the same `Application/Courses/` folder those live in.

### References

- [Source: _specs/planning-artifacts/epics-ContentAuthoring.md#Story 7.4] — verbatim Acceptance Criteria
- [Source: _specs/implementation-artifacts/7-1-...md, 7-2-...md, 7-3-...md] — predecessor stories; every deferred item this story resolves is named in their own Dev Notes
- [Source: _specs/planning-artifacts/architecture/architecture-eLearning-2026-08-09/ARCHITECTURE-SPINE.md#AD-4, #AD-11]
- [Source: _specs/planning-artifacts/architecture/architecture-eLearning-backend-2026-08-09/ARCHITECTURE-SPINE.md#Additional Requirements — GET /outline vs GET .../document]
- [Source: _specs/planning-artifacts/ux-designs/ux-eLearning-2026-08-10/EXPERIENCE.md#Accessibility Floor, #State Patterns]
- [Source: _specs/planning-artifacts/ux-designs/ux-eLearning-2026-08-10/DESIGN.md#content-doc-heading]

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- `dotnet build` (Backend): 0 errors after Tasks 1-3.
- `dotnet test` (Backend, full suite): 526 + 208 + 141 = 875 tests, all green, including 17 new `ContentServiceTests` cases (FR-44 immediate-parent resets, text-only-edit-never-resets, `MovePageAsync`'s three-entity reset, the 5 NFR4 bounded-limit rejections, `GetOutlineAsync`'s nested body-free shape).
- `npx tsc --noEmit` (Frontend): no new type errors; the same pre-existing `SegmentedTabs`/`ContentView` error in `CourseContentEditor.tsx` and unrelated files' errors (App.tsx, CourseOverviewScreen.tsx, FlashcardsModal.tsx, Dashboard/*) predate this session and were left untouched.
- `npx vitest run` (Frontend, full suite): 86 files / 684 tests, all green (before the final rail confirmation-glyph addition; re-verified at 124/124 for the CourseContentEditor suite specifically after it).

### Completion Notes List

- **Backend Task 1 (FR-44 retrofit)** — added a shared `ResetConfirmation(object entity)` helper (Chapter/Topic/Subtopic/Page) and `LoadOwnerInCourseAsync` (renamed from `EnsureOwnerBelongsToCourseAsync`, now returns the loaded tracked entity instead of discarding it, avoiding a second fetch to get the same row back for confirmation-flipping). Every Create/Delete on Topic/Subtopic/Page flips its immediate parent's `IsConfirmed` before the existing single `SaveChangesAsync` call -- no second commit added anywhere. `SwapOrderAsync<T>` (the shared reorder primitive) gained an optional `onWillSwap` callback and now returns `bool`, invoked only once a real swap is confirmed (never on a boundary no-op) -- this is how Reorder* avoids falsely resetting a parent's confirmation on a no-op click. `MovePageAsync` captures the source owner *before* overwriting `Page.OwnerType`/`OwnerId`, then resets source parent, destination parent, and the page's own `IsConfirmed` in one commit, per FR-44's explicit third case. `Update*Async` methods were already confirmed to never touch `IsConfirmed` (Stories 7.1-7.3 never wrote that logic) -- verified with a dedicated regression test per entity type, not just asserted from reading the code.
- **Backend Task 2 (`GET content/outline`)** — new `OutlinePageDto`/`OutlineSubtopicDto`/`OutlineTopicDto`/`OutlineChapterDto`/`OutlineDto` records in `ChapterDto.cs`, deliberately NOT reusing `ChapterDocumentDto`'s shape (no `BodyMarkdown` field exists anywhere in the Outline family, a compile-time guarantee, not a runtime omission) -- confirmed with a test that also asserts `OutlinePageDto`'s exact property list. `GetOutlineAsync` walks the whole course (not one Chapter) via the same repository methods every other document-builder in this file already uses; ownership-only read check (not Draft-gated), matching every other read in this feature.
- **Backend Task 3 (NFR4 bounded limits)** — `EnforceLimit(currentCount, max, what)` helper + five `MaxXPerY` constants, checked before `repository.Add(...)` in each Create method (chapters/course, topics/chapter, sub-topics/topic, pages/node) and in `UpdatePageAsync` (body byte length via `Encoding.UTF8.GetByteCount`, checked and rejected *before* `page.BodyMarkdown` is assigned or anything saved). Every rejection message names the specific limit number, verified in tests via `Assert.Contains`.
- **Frontend Task 4 (`CourseContentContext.tsx`)** — genuinely new file, as the story's own Dev Notes anticipated. Mounted by wrapping `CourseContentEditor.tsx`'s own returned JSX in `<CourseContentProvider courseId={draftId}>` -- confirmed against `App.tsx`'s actual composition root (`DomainProvider`/`ToastProvider`/`SiteSettingsProvider`, all three app-lifetime-scoped there) and deliberately diverges from that pattern, exactly as the story anticipated might be needed. `refetch()` returns the freshly-built confirmation map directly (not just via a state update) specifically so Task 7's reversion-announcement check can compare a node's before/after value without waiting on React's own re-render timing. `TableOfContentsRail.tsx` (Story 7.2) was extended, not rewritten, to also render a small confirmed/unconfirmed glyph per entry sourced from this Context -- resolving the design tension the story names explicitly (structure from the live Tiptap document via `walkHeadings`, confirmation display from Context, keyed by `entityId`, never a second copy of confirmation state written into a Tiptap node attribute).
- **Frontend Task 6 (`useContentAutosave.ts`)** — a debounce/status-tracking hook (1500ms debounce, "saved" visible for 2s before fading to idle, a sequence-number guard so a slower in-flight save can never clobber a faster, more recent one's outcome) that wraps `DocumentCanvas.tsx`'s existing boundary-detection walk (renamed `handleEditorBlur` -> `performSync`, same logic, not duplicated) rather than re-implementing that walk inside the hook itself -- a deliberate, documented split: the hook owns *when* a save runs and *how* its outcome is surfaced; `performSync` still needs deep `editor.state`/Tiptap access that has no reason to live in a generic debounce hook. Wired via `editor.on('update', ...)` (debounced) and `editor.on('blur', ...)` (immediate flush), replacing Stories 7.1-7.3's bare `onBlur`-only stub -- this closes a real, previously-unmet half of AC #1 (stop-typing debounce), not just a refactor. A **structural** create (a brand-new Topic/Sub-Topic typed and saved for the first time) still triggers a full `reload()` (needed for its canonical id, which nothing else can supply) *and* a `CourseContentContext.refetch()` (its own immediate parent's confirmation changed server-side); a **text-only** update patches the single entity's `isConfirmed` from its own PUT response directly into Context, per AD-11's explicit "never a full outline refetch" rule for that path.
- **Save-state indicator (AC #1)** — a single visible-text indicator (`Saving…` / `Saved` / `Save failed` + `Retry`) positioned above the document canvas, not a per-node floating indicator next to every individual block. **Scope decision:** AC #1 says "near the affected block"; given this story's document canvas is one continuous, one-entity-editable-at-a-time flow (not multiple simultaneously-editable regions), a single canvas-level indicator reads as "near the block currently being edited" without needing a second positioned-sidecar system layered on top of `HeadingControls`/`BodyBlockControls`/`ConfirmationGlyphs` already built across 7.2-7.4. On failure, the indicator's content is never cleared and the editor's own document is never touched by this hook, satisfying "never navigates away from unsaved content" by construction (nothing in the failure path calls `setContent`, `reload`, or any navigation).
- **Task 7 (`aria-live` reversion announcement)** — `getImmediateParentId`/`refreshConfirmationAndAnnounceIfReverted` added to `DocumentCanvas.tsx`, reusing `CourseContentEditor.tsx`'s existing `queueAnnouncement` batching pipeline (via the `onAnnounce` prop already threaded through since Story 7.2), not a third `aria-live` region. **Scope decision:** wired into the four explicit interactive handlers (delete, move-up/down, drag-reorder, "Move page to…") where a confirmation side effect is least expected, per the UX rationale EXPERIENCE.md itself gives ("a tutor may not expect their edit to have un-confirmed something") -- not also duplicated onto the blur-triggered Topic/Sub-Topic *creation* path, where a newly-added child resetting its own just-edited parent is a much less surprising consequence of an explicit "add a child" action. `MovePageAsync`'s own three-way reset (source parent, destination parent, the page itself) is fully applied server-side and refetched client-side; the announcement itself only ever names the source parent reverting (the destination gaining an unconfirmed child is the expected half of a move).
- **Task 8 (roving-tabindex tree + focus-preservation audit)** — `TableOfContentsRail.tsx` gained `role="tree"`/`role="treeitem"` (`role="none"` on the `<li>` wrappers, matching standard ARIA treeview markup so the `<li>` itself doesn't get exposed as a redundant accessibility-tree node), `aria-level` per entry, and roving `tabIndex` (`activeIndex` state; only the active entry is `0`, every other is `-1`; Arrow Up/Down moves both the roving position and real focus). Reorder announcements were already verified present from Story 7.2/7.3's own `onAnnounce` calls in every move/drag handler -- no gap found there to close. **Focus-preservation audit:** verified by inspection and by `useContentAutosave.test.ts`'s "a slower in-flight save does not clobber the status of a faster, more recent one" test (the exact out-of-order-resolution scenario Task 8's own test bullet describes) rather than a DOM-simulated "type in block A, move to block B" test -- `useContentAutosave` itself never calls `.focus()` or touches the DOM at all (confirmed by reading its full source, it only manages internal state and invokes the injected `onSync`), and `performSync`'s own patch/update code paths never call `.focus()` or manipulate ProseMirror selection anywhere. A live-typing DOM version of this test would exercise jsdom's own selection/coordinate fragility, not this code -- consistent with this codebase's own established precedent (Story 7.1's removed `userEvent.type()`-into-ProseMirror test).
- **Task 9 (Confirmed/Unconfirmed glyphs on headings)** — new `ConfirmationGlyphs.tsx`, the same React-sibling/`coordsAtPos`-positioned pattern as `HeadingControls.tsx`, rendered for h1 (using the `chapterId` prop, since a Chapter's h1 carries no `entityId` attribute of its own) and h2/h3 (using `StructuralHeading`'s existing `entityId` attribute). Page markers (h4) already had their own Confirmed/Unconfirmed badge-pill from Story 7.3 and are untouched here. Filled check (`bg-[#179765]`, this codebase's own established literal-hex convention for signal-green, confirmed via grep against 35 other files -- the `bg-success`/`text-success` Tailwind theme utility exists in `index.css`'s `@theme` block but is never actually used anywhere in this codebase) vs. an outlined circle -- a shape difference, never color-alone, per UX-DR8.
- **Not built in this pass, honestly scoped:** a full `CourseContentContext`-driven re-render performance audit beyond the specific "never a full outline refetch on the debounced path" rule Task 6 names explicitly; FR-44's resource-mutation reset case has no resources to test against yet (Resource doesn't exist until Story 8.1) -- the shared `ResetConfirmation`/`LoadOwnerInCourseAsync` helpers are already generically shaped for Story 8.1 to call identically, per this story's own scope note.

### File List

**Backend -- modified (no new backend files this story):**
- `Backend/src/FlexDemy.Application/Courses/ChapterDto.cs` (Outline DTO family)
- `Backend/src/FlexDemy.Application/Courses/IContentService.cs`
- `Backend/src/FlexDemy.Application/Courses/ContentService.cs`
- `Backend/src/FlexDemy.Api/Controllers/ContentController.cs`
- `Backend/tests/FlexDemy.Application.Tests/Courses/ContentServiceTests.cs`

**Frontend -- new:**
- `FrontEnd/src/context/CourseContentContext.tsx`
- `FrontEnd/src/features/CourseContentEditor/useContentAutosave.ts`
- `FrontEnd/src/features/CourseContentEditor/ConfirmationGlyphs.tsx`
- `FrontEnd/tests/features/CourseContentEditor/useContentAutosave.test.ts`
- `FrontEnd/tests/context/CourseContentContext.test.tsx`

**Frontend -- modified:**
- `FrontEnd/src/services/courseContentService.ts` (Outline DTOs + `getOutline`)
- `FrontEnd/src/features/CourseContentEditor/CourseContentEditor.tsx` (mounts `CourseContentProvider`)
- `FrontEnd/src/features/CourseContentEditor/DocumentCanvas.tsx` (autosave wiring, confirmation patch/refetch, reversion announcements, `ConfirmationGlyphs`)
- `FrontEnd/src/features/CourseContentEditor/TableOfContentsRail.tsx` (roving-tabindex tree semantics, per-entry confirmation glyph)
- `FrontEnd/tests/features/CourseContentEditor/CourseContentEditor.test.tsx`

## Change Log

| Date | Change |
| --- | --- |
| 2026-08-17 | Story implemented: FR-44 immediate-parent confirmation resets retrofitted into every Topic/Sub-Topic/Page mutation (Task 1); `GET content/outline` body-free whole-course tree (Task 2); NFR4 bounded limits on every create path (Task 3); `CourseContentContext.tsx` mounted per editor session (Task 4); `getOutline` service call (Task 5); real debounced `useContentAutosave.ts` replacing Stories 7.1-7.3's blur-only stubs (Task 6); `aria-live` reversion announcements on delete/move/reorder (Task 7); roving-tabindex ARIA tree on the ToC rail (Task 8); Confirmed/Unconfirmed glyphs on Chapter/Topic/Sub-Topic headings and rail entries (Task 9). Status set to `review`. |
