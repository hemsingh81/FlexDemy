## Deferred from: code review of 1-1-admin-ai-configuration-ui-mock-data (2026-08-11)

- Draft state in `AiTaskConfigRow` (`FrontEnd/src/features/Admin/AiConfiguration/AiTaskConfigRow.tsx:32-38`) doesn't resync if the `task` prop changes underneath it. Pre-existing pattern with no current trigger — the only way `task` changes today is via this same row's own `updateTaskConfig` call. Story 1.5's live-wire (real backend data replacing mock state) is the natural point to add resync logic (e.g. a `useEffect` keyed on `task` identity).
- `AdminPanel.tsx` has zero dedicated render tests for any of its 5 sub-tab conditional-render branches. Pre-existing gap predating Story 1.1, not introduced or worsened by it — none of the 4 prior sub-tabs (`masterdata`, `support-users`, `role-visibility`, `tutor-approvals`) had this coverage either.
- Admin sub-tab role-gating relies entirely on the caller passing a correctly role-filtered `activeSubTab`/`availableSubTabs` — a theoretical bypass exists if a future caller sets `activeSubTab` directly, bypassing `useAdminPanel`'s role filter. Pre-existing pattern shared identically by all 4 prior sub-tabs, not introduced by Story 1.1. The documented real safety net is backend `[Authorize(Policy = ...)]` on each write endpoint, not client-side gating (see `useAdminPanel.ts`'s own comment).

## Deferred from: re-review of 1-1-admin-ai-configuration-ui-mock-data (2026-08-11)

- Fallback provider/model can be saved identical to the primary provider/model in `AiTaskConfigRow.tsx` (no distinctness check), silently providing no real failover. Deferred: no AC requires this, and Story 1.5's real backend is the natural place to validate provider/model combination sanity, not a mock-only UI.

## Deferred from: code review of 1-2-admin-ai-usage-cost-dashboard-mock-data (2026-08-11)

- `TASK_LABELS[taskId]` lookups in `AiUsageSummary.tsx` have no `??` fallback for an unrecognized taskId. Deferred: currently unreachable under the `AiTaskId` union type (TypeScript guarantees every `taskId` in mock data is a known value); only becomes a real risk once Story 1.7 introduces loosely-typed real API responses that could carry an unexpected task identifier.
- Mock usage dataset in `useAiUsage.ts` is generated once at module load with no upper-date-bound guard against future-dated entries. Deferred: mock-only-story artifact — moot once Story 1.7 replaces it with real backend-computed ranges, which won't have this shape of bug.

## Deferred from: code review of 1-3-admin-tag-management-ui-mock-data (2026-08-11)

- Tag Management's (and `ai-configuration`'s) role-gating is hardcoded in `useAdminPanel.ts` rather than routed through the `FeatureKeys`/`RoleVisibilityManager` system that governs every other admin capability's write permissions. Same underlying pattern already tracked above (from Story 1.1's review: "Admin sub-tab role-gating relies entirely on the caller..."), now confirmed to affect a second sub-tab (`tag-management`) too. Still deferred for the same reason: pre-existing pattern, and the real safety net is backend `[Authorize(Policy = ...)]`, not client-side gating.
- `AdminPanel.tsx` still has zero dedicated render tests for any sub-tab conditional-render branch, now including `tag-management`. Same gap already tracked above from Story 1.1's review — pre-existing, systemic, not worsened by this story specifically.
- Duplicate-name/not-found validation in `TagManagement.tsx` exists only in its mock closures, with nothing guaranteeing Story 1.9's real `tagsService.ts` replicates the same case-insensitive uniqueness rule. Deferred: explicitly Story 1.9's responsibility per this story's own Dev Notes.
- `nextIdRef`-generated client-side tag IDs (`tag-N`) could collide with server-assigned IDs if Story 1.9's swap from mock to real backend isn't done carefully. Deferred: Story 1.9 concern, not actionable in a mock-only story.

## Deferred from: code review of 1-4-ai-service-layer-interface-gateway-client (2026-08-11)

- No resiliency policy (retry/circuit-breaker) exists yet on `IAiGateway` calls beyond the raw `HttpClient.Timeout`. Deferred: explicitly Story 1.6's scope (Polly 8.7.0 per `ARCHITECTURE-SPINE.md` AD-14).
- `PortkeyAiGateway.BuildRequest`'s hardcoded leading-slash relative paths would silently drop any path prefix on a configured `AiGateway:BaseUrl` per `Uri` combination rules. Deferred: unreachable today since the documented `BaseUrl` shape is a bare `http://host:port`; revisit if that ever changes.
- No startup-time validation (`ValidateOnStart`) of `AiGatewayOptions.BaseUrl` — an invalid URL currently only surfaces on the first real gateway call, not at app startup. Deferred: fail-fast nicety, not blocking.
- `PortkeyAiGateway` takes `IOptions<AiGatewayOptions>` (snapshot at construction) rather than `IOptionsMonitor`/`IOptionsSnapshot`, so a rotated API key or changed `BaseUrl` isn't picked up without a process restart. Deferred: no stated hot-reload requirement.
- `AiGatewayException` (and every other `AppException` subtype) has no inner-exception-preserving constructor, losing the original stack trace when wrapping `HttpRequestException`/`JsonException`/etc. Deferred: a cross-cutting change better made consistently across all `AppException` subtypes together, not uniquely for this story's new one.
- `AiGatewayResponse`/`AiEmbeddingResponse` echo back the *requested* `Provider`/`Model` rather than one parsed from the gateway's actual response body (which may reflect a substituted route). Deferred: Portkey's exact response-echo shape for provider/model substitution isn't verified against a live instance in this dev environment (no running Portkey container — Docker builds hit a known NuGet/TLS-interception issue per AD-13). Revisit with real integration testing once a gateway instance is reachable.

## Deferred from: code review of 1-5-ai-task-configuration-store-live-wire-config-ui (2026-08-11)

- `AiTaskConfig.TaskId` / `AiPromptVersion.(TaskId, Version)` unique indexes aren't filtered by `IsDeleted`. Deferred: matches `CountryConfiguration.cs`'s identical existing pattern; currently unreachable (no delete endpoint exists for either entity).
- No cross-field validation that `Model` is compatible with `Provider` (e.g. an OpenAI-only embedding model paired with a `Local` provider). Deferred: no AC requires this; FR-2 treats provider/model choice as the admin's own responsibility.
- No optimistic concurrency control (`RowVersion`) on `AiTaskConfig`. Deferred: matches every other Master Data entity's identical lack of a concurrency token.
- No `AiConfigController`-level HTTP/auth-policy test verifying `FeatureKeys.AiConfigManage` blocks non-Master callers. Deferred: matches `CountriesController`'s identical existing lack of a dedicated controller test file.
- `RolePermissionSeedData`'s new `AiConfigManage` grant won't reach an already-seeded environment (`EnsureRolePermissionsAsync` skips entirely once any row exists). Deferred: pre-existing systemic pattern shared by every prior `FeatureKeys` addition, not introduced by this story.
- `BudgetThreshold` isn't validated for decimal-precision/magnitude beyond `>= 0`. Deferred: low-likelihood for a small-cardinality admin budget field.
- `aiConfigService.ts`'s `get`/`write` don't guard `response.json()` against a non-JSON/empty body on the success path. Deferred: matches `masterDataService.ts`'s identical existing gap.

## Deferred from: code review of 1-6-per-task-fallback-retry (2026-08-11)

- A missing `AiTaskConfig` row for a known `AiTaskIds` value (a seeding gap, or a future 8th task not yet backfilled) causes `AiTaskGateway.DispatchAsync`/`.GenerateEmbeddingAsync` to let `IAiConfigService.GetTaskConfigAsync`'s `NotFoundException` propagate unwrapped — a real feature caller gets a 404 naming the internal `AiTaskConfig` entity, not this story's 502/503 semantics. Deferred: `NotFoundException`'s entity-name-in-message shape is the same systemic, pre-existing convention used everywhere else in this codebase (e.g. `CountriesController`); not a defect introduced by this story, and a missing config row is a misconfiguration/seeding problem distinct from "both providers failed" (the actual AC #4 scope).
- Fallback provider/model saved identical to primary (`AiConfigService.UpdateTaskConfigAsync` still has no distinctness check — same gap already tracked from Story 1.5's review) now has a concrete downstream consequence in `AiTaskGateway`: the "fallback" becomes a byte-for-byte repeat of the call that just failed, doubling latency/cost before still throwing `AiTaskUnavailableException`. Still deferred for the same reason (no AC requires it), but worth surfacing that the cost of leaving it unfixed is now real, not just theoretical.
- An empty `AiTaskRequest.Messages` / empty embeddings `input` list is a caller/validation error, but `PortkeyAiGateway`'s own request validation (Story 1.4) throws the same `AiGatewayException` type as a genuine transport failure — so `AiTaskGateway` can't distinguish them, attempts a wasted fallback round-trip, and reports `AiTaskUnavailableException` (503, "unavailable") for what is actually a 400-shaped caller bug. Deferred: this is the exact tradeoff this story's own Dev Notes named explicitly ("any `AiGatewayException` is fallback-worthy... a future story needs finer-grained control... that's a deliberate, separate scope decision"); fixing it requires `IAiGateway` (Story 1.4) to distinguish validation from transport failures, which is out of this story's scope.
- `ExceptionHandlingMiddleware` only catches `AppException`, so an `OperationCanceledException`/`TaskCanceledException` from a caller-cancelled request (increasingly likely once real, multi-second AI Task calls exist) falls through to ASP.NET Core's default unhandled-exception handling instead of a clean response. Deferred: pre-existing, systemic gap in the middleware itself (not introduced by this story), applies to every endpoint, not just `IAiTaskGateway` callers.
- `IAiTaskGateway`/`IAiConfigService` sit on a `Scoped` DI lifetime backed by a scoped EF Core `DbContext`; nothing prevents or documents-against a future caller invoking two `IAiTaskGateway` methods concurrently within one request scope (e.g. `Task.WhenAll` across independent AI Task calls), which EF Core does not support and would throw an unmapped 500. Deferred: general EF Core/DI-lifetime caveat that applies to any scoped-DbContext-backed service in this codebase, not specific to this story; no current caller does this.
- (Resolved, noted for tracking) Story 1.4's review had deferred "no `AppException` subtype has an inner-exception-preserving constructor" as a cross-cutting change. This story added an optional `innerException` parameter to the `AppException` base constructor and used it for `AiTaskUnavailableException` specifically (the only subtype this story introduces that wraps another exception). The base capability now exists for the other 5 subtypes to adopt the same pattern, but none of them do yet — `AiGatewayException` (Story 1.4) still doesn't pass through the `HttpRequestException`/`JsonException` it originally deferred on.

## Deferred from: code review of 1-7-usage-cost-tracking-live-wire-usage-dashboard (2026-08-11)

- `AiUsageService.RecordUsageAsync` fetches `AiTaskConfig` via `IAiConfigService.GetTaskConfigAsync` on every successful call, even though `AiTaskGateway.DispatchAsync`/`GenerateEmbeddingAsync` already fetched the identical row moments earlier for provider/model resolution — a real, measurable double DB read on the hot path of every AI Task invocation. Deferred: fixing it requires either widening `IAiUsageService.RecordUsageAsync`'s signature to accept a pre-fetched config (leaking an `AiConfig`-shaped parameter into a cross-feature service boundary) or introducing a request-scoped config cache — both are real design changes, not justified at this story's MVP scale (AD-18's own "single container, moderate volume" framing) without a demonstrated need.
- `GET /api/v1/ai-usage`'s `range` query parameter is a non-nullable `string` on a `[ApiController]` action, so ASP.NET Core's automatic model validation rejects a missing/empty value with its own 400 shape (an `errors` map) before `AiUsageService.GetUsageAsync`'s own `ValidationException`-based check (a `ProblemDetails.detail` message) is ever reached — two different error contracts for what is logically the same "bad range" failure. Deferred: low practical impact (the only real caller, `aiUsageService.ts`, is TypeScript-typed and can never send an empty/missing range), and the underlying cause (`[ApiController]`'s auto-validation on required non-nullable parameters) is a systemic ASP.NET Core behavior affecting any controller action in this codebase with a required string parameter, not unique to this endpoint.
- `useAiUsage.ts`'s error branch (`AiConfiguration.tsx`) checks `usageError` truthiness, not `!== null` — an `Error` with an empty-string message (no current producer creates one; `aiUsageService.ts`'s `AiUsageError` always supplies a non-empty fallback) would fall through to rendering stale, from-the-previous-successful-fetch data as if the fetch had succeeded. Deferred: currently unreachable given every real error producer in this diff, matching this codebase's established practice of not hardening against inputs no code path can actually produce.
- Fallback provider pricing (`AiTaskConfig.FallbackPricePerMillion{Input,Output}Tokens`) is seeded at `0m` for every task, same as the primary — but unlike the primary (genuinely free-tier, so `0` is accurate), every seeded fallback provider is `OpenRouter`, a real paid API. A fallback-served call is under-reported at `$0.00` until an admin sets real fallback pricing. Deferred: no fabricated rate was seeded in its place (a guessed number would be worse than an honest `0`/unknown), and this story's own Dev Notes already establish that pricing is read-only with no edit UI yet — setting real values requires that future UI (or a direct DB update) regardless of what's seeded here.

## Deferred from: code review of 1-8-budget-threshold-enforcement (2026-08-11)

- A missing `AiTaskBudget` row (a seeding gap) makes `AiTaskGateway`'s pre-flight reserve fail identically to a genuinely exhausted budget (`AiTaskBudgetExceededException`, 429), while `AiConfigService`'s config-read path (`GetAllTaskConfigsAsync`/`GetTaskConfigAsync`) defaults the same missing row to `CurrentSpend: 0m` — an admin sees the task reading as healthy ("$0.00 / $80.00 spent") while every real call for it is instantly rejected. Deferred: this story's own Dev Notes already named and deliberately accepted this exact ambiguity ("a missing `AiTaskBudget` row blocks the call, indistinguishably from a genuinely exhausted budget") — adding a second, non-atomic existence check before the reserve to distinguish the two would reintroduce the concurrency race the atomic reserve exists to prevent. Both independent reviewers (Blind Hunter, Edge Case Hunter) re-surfaced this from different angles during this story's review, confirming it's worth tracking even though it isn't being fixed here.
- If `AiTaskConfig.PricePerMillionInputTokens`/`PricePerMillionOutputTokens` are both `0` for a task (the seeded default for every free-tier task today), `EstimateChatReservationCost`/`EstimateEmbeddingReservationCost` always compute a `$0` reservation regardless of `MaxTokens`/input size — the pre-flight gate trivially passes every time. Deferred: correct behavior for a genuinely free task (there is no real budget to protect), and becomes a live concern only once real, nonzero pricing is set for a task — at which point the already-fixed prompt+completion estimate formula (this story's own review patch) prices it properly; not a gap specific to this story's design.
- An admin lowering `AiTaskConfig.BudgetThreshold` below a task's current `AiTaskBudget.Spent` (via `UpdateTaskConfigAsync`) permanently blocks all future calls for that task — `TryReserveAsync`'s `spent + estimatedCost <= threshold` check fails even for a `$0` estimate once `spent` alone exceeds the new threshold, and nothing resets/reconciles `Spent` on a threshold change. Deferred: this is the correct, intended behavior of a live-threshold enforcement system (AD-18 explicitly reads the threshold live, not a snapshot) — an admin who lowers a threshold below current spend is knowingly locking out further spend until the threshold is raised again or spend naturally has room; no AC asks for automatic reconciliation, and building one (a rollover, a spend-reset job) is a real, separate feature decision.
- `AiTaskBudgetRepository`'s two raw-SQL methods (`TryReserveAsync`, `AdjustSpentAsync`) have no `AND is_deleted = false` clause, so they would still find and mutate a soft-deleted `AiTaskBudget` row, while the LINQ-based reads (`GetByTaskIdAsync`/`GetAllAsync`, and `AiTaskBudgetConfiguration`'s `HasQueryFilter`) treat that same row as absent. Deferred: no delete path exists anywhere for `AiTaskBudget` in this diff (matches the identical, already-deferred "unique indexes not filtered by `IsDeleted`" gap tracked from Story 1.5's review for `AiTaskConfig`/`AiPromptVersion`) — currently unreachable, not a live bug.

## Deferred from: code review of 1-9-tag-management-backend-live-wire-tag-ui (2026-08-11)

- `TagService.CreateAsync`'s duplicate-name check (`GetByNameAsync`, then `Add`+`SaveChangesAsync`) is not atomic and `tags.name` has no DB-level unique constraint (a deliberate choice, documented in this story's own Dev Notes). Two concurrent `POST /api/v1/tags` requests with the same name can both pass the check before either commits, landing two duplicate rows. Deferred: already named and accepted in this story's own Dev Notes as the same category of scale tradeoff as AD-18's "single container, moderate volume" framing; re-surfaced by the Blind Hunter reviewer for visibility, not as a surprise — an admin-only, low-frequency-write screen doesn't need protection against two admins creating the identically-named tag in the same instant. A Postgres functional unique index on `lower(name)` is the fix if this ever becomes real.
- `GET /api/v1/tags` has no role-based `includeInactive` gating — it unconditionally returns every tag, active and inactive, to any authenticated user, unlike `CountriesController`'s (and every other Master Data controller's) equivalent, which restricts seeing inactive rows to Master/Support. Deferred: low-sensitivity data (tag names), no AC asks for restricting visibility, and this story's own Dev Notes already named "no `includeInactive` parameter" as a deliberate scope cut for a different reason (Tag's one consumer, the admin table, always wants everything) — extending that decision with a role check was not in scope, but is a real, worth-tracking deviation from the established Master Data pattern.
- A stale-response race exists between `TagManagement.tsx`'s debounced search-triggered `fetchAll()` reload and a concurrent create/toggle/edit write: the debounce timer runs independently of `MasterDataTable`'s busy state, so if a search-triggered reload resolves after a just-completed write's own optimistic `setRows(...)`, the reload's (older) response silently overwrites the just-written row out of view. Deferred: fixing it requires request-sequencing/cancellation logic inside the shared `MasterDataTable.tsx` component, which this story's own Dev Notes explicitly scoped out ("No changes to `MasterDataTable.tsx`... zero shared-scaffold changes anywhere") — a fix here would ripple to the 6 other entities sharing that component, a larger change than this story's own boundary.
- A tag name longer than 255 characters (the `tags.name` column's `varchar(255)` limit) has no application-level length validation before `SaveChangesAsync`, so Postgres rejects the write with a raw `DbUpdateException`/`PostgresException` that `ExceptionHandlingMiddleware.cs` doesn't map to a clean 400 — it falls through to a generic 500. Deferred: pre-existing, systemic gap shared by every Master Data entity in this codebase (`CountryService.EnsureRequiredFields` also only checks `IsNullOrWhiteSpace`, no `MaxLength` check anywhere in `Application/MasterData/`), not introduced or worsened by this story specifically.

## Deferred from: code review of 2-1-course-metadata-wizard-ui-mock-data (2026-08-11)

- Mock taxonomy fixtures (`Country`/`State`/`City`/`Board`/`ClassLevel`/`Subject`) don't model `isActive` filtering the way the Tags step does. Deferred: no AC requires it in this mock-only story, and every mock taxonomy entity today is `isActive: true`; becomes directly relevant once Story 2.5 live-wires real master data that can include inactive entities.
- No image-type validation on thumbnail uploads — `accept="image/*"` is a soft UI hint only, not enforced in `StepThumbnails.tsx`'s `handleFileSelected`. Deferred: no AC requires file validation in this mock-only story; real upload validation is a live-wire concern.
- No file-size cap on thumbnail uploads. Deferred: same reasoning as the image-type gap above, not required by any AC in this mock-only story.
- `TypeaheadMultiSelect`'s new `lockedValues` prop (added by this story) has no dedup guard against a `selected`/`options` overlap. Deferred: unreachable today since `StepTags.tsx`'s active/locked tag sets are mutually exclusive by construction (a tag is either active or inactive, never both); worth hardening if a future caller of this shared `ui/` primitive doesn't maintain that invariant.
- `useCourseDraft.ts`'s `isTaxonomyStepValid` silently treats an unrecognized `boardId` as valid (falls through to `true` when the board lookup fails). Deferred: unreachable via the UI today since `boardId` only ever comes from the rendered `<option>` list, always a subset of the `boards` array passed in; a defensive-programming gap only if a future caller sets hook state directly.

## Deferred from: code review of 2-2-file-upload-ui-mock-data (2026-08-11)

- `sizeBytes` is captured on every uploaded file entry (`useFileUpload.ts`) but never displayed anywhere in `CourseContentEditor.tsx`'s UI. Deferred: no AC requires a size indicator; a real gap worth tracking for a future polish pass, not a defect in what this story actually asked for.

## Deferred from: code review of 2-6-file-upload-malware-scanning-secure-storage (2026-08-12)

- No FK/cascade relationship between `CourseFile.CourseId` and `Course.Id` (only an index). Deferred: mirrors this codebase's existing loosely-coupled-reference convention — `Course.TutorId` has the identical characteristic, by the same documented rationale on that field — and no course-delete feature exists anywhere in this codebase yet to trigger orphaning.
- `clamav/clamav:latest` is unpinned in `docker-compose.yml`, unlike every other new dependency added by this story (`Hangfire.AspNetCore 1.8.24`, `Hangfire.PostgreSql 1.21.1`, `nClam 9.0.0`, all pinned). Deferred: pinning to an unverified tag risks breaking the build in this environment (no way to confirm the exact current stable tag); needs a confirmed tag before pinning.
- `ClamAvFileScanner` has no configurable scan timeout — `ClamAvOptions` only exposes `Host`/`Port`. Deferred: minor, no AC requires it; a genuinely slow (not fast-refused) ClamAV connection could tie up a Hangfire worker longer than the "fail-closed after 5 attempts" design implicitly assumes.
- `useFileUpload.ts`'s poll timer isn't defensively re-keyed on `courseId` changes at the hook level itself. Deferred: currently masked by `CourseContentEditor.tsx` calling `resetFiles()` on every `draftId` change; a real gap only if this hook is ever reused elsewhere without that same safety net.
- ClamAV's own internal `StreamMaxLength` could reject a legitimate large-but-under-this-story's-50MB-cap file; `ClamAvFileScanner` currently maps any non-Clean/non-VirusDetected result generically to `FileScanUnavailableException`, so this surfaces as "scanning unavailable" after retries exhaust rather than a distinct "file too large to scan" reason. Deferred: distinguishing this precisely needs research into nClam's actual `ClamScanResult.RawResult` format for this case, not verifiable without a live ClamAV instance in this dev environment.
- `LocalFileStorageService.SaveAsync` leaves a partially-written file on disk with no cleanup if `CopyToAsync` fails mid-write (disk full, cancellation, client disconnect). Deferred: pre-existing behavior in `SaveAsync`'s core copy logic since Story 2.4's original implementation, unchanged by this story's `category`/`OpenReadAsync`/`DeleteAsync` additions.
- `CourseService.GetCourseByIdAsync`'s non-owner guard (`course.LifecycleState != Published && course.TutorId != currentUserService.UserId`) is bypassed when both `TutorId` and the caller's id are `null` — an unauthenticated caller can view a tutor-less non-Published course. Deferred: pre-existing code from Story 2.4, not touched by this story's diff at all.

## Deferred from: code review of 2-7-document-parsing-ocr-pipeline (2026-08-12)

- Whether docling-serve's `"FAIR"` confidence grade should also fail the parse's confidence gate (vs. only `"POOR"`) is a genuine product decision, already flagged `[ASSUMPTION]` in the story's own text. Deferred: needs product confirmation against a real docling-serve instance, not a code defect to patch blindly.
- No size bound on `CourseFile.ParsedContent` before persisting a parsed document's text. Deferred: the column is deliberately unbounded Postgres `text` per its own documented rationale, and no AC requires a cap.
- `ParseFileJob` trusts its `courseFileId` argument unconditionally, with no structural check that the file's malware scan actually passed before its bytes are read and sent to the third-party Docling service. Deferred: not exploitable today (only `ScanFileJob`'s clean-scan branch enqueues it) — a defensive concern for a future caller/bug, not a current one.

## Deferred from: code review of 2-8-ai-structure-extraction-extractstructure (2026-08-12)

- `ExtractionPromptBuilder` inserts the entire `CourseFile.ParsedContent` verbatim with no length cap/chunking, risking a context-window overflow for very large source documents. Deferred: already explicitly flagged `[ASSUMPTION: out of scope for this story]` in the story's own Dev Notes; a future story should add chunking/map-reduce-style extraction if this becomes a real problem in practice.
- `ExtractStructureJob`'s generalized retry-then-fail-closed catch retries a permanent failure (e.g. a `NotFoundException` from the new `ICourseService.GetOwningTutorIdAsync` call) the same as a genuinely transient one, burning up to 5 attempts pointlessly before failing closed. Deferred: matches this story's own explicit, reasoned design decision (uniform retry-then-fail-closed handling rather than enumerating exception types by name) — re-litigating it here would contradict the story's own stated reasoning, not fix a bug.
- No optimistic-concurrency protection (e.g. a `RowVersion`) around any of the three pipeline jobs' (`ScanFileJob`/`ParseFileJob`/`ExtractStructureJob`) terminal writes — two concurrent executions of the same `courseFileId` could both pass their idempotency guard and (for `ExtractStructureJob` specifically) double-spend against the AI budget reservation. Deferred: systemic across all three jobs, not unique to this story, and no AC requires it.
- An out-of-schema `lang` value in a content block (e.g. the model emitting `"fr"` instead of `"en"`/`"hi"`) is silently coerced to `"en"` rather than failing the parse. Deferred: matches this story's own documented intent ("lang should default to en when the model omits it or is uncertain"); Story 2.10 owns real Hindi/Devanagari detection.
- Uploaded document content flows into the AI extraction prompt with no sanitization against prompt injection. Deferred: no clear code-level fix exists (an open problem generally); risk is mitigated by schema validation on the output and the staged-not-materialized design (Story 2.9 gates what actually becomes real content). Worth documenting as an accepted risk going forward, not an oversight.
- `ScanFileJob`/`ParseFileJob`/`ExtractStructureJob`'s shared `Truncate()` helper could theoretically split a UTF-16 surrogate pair at its 1024-char cut, producing malformed text. Deferred: extremely low practical likelihood (would require a non-BMP character landing exactly at the boundary), and the identical pattern already exists unaddressed in the two earlier jobs from Stories 2.6/2.7 — a systemic, pre-existing characteristic, not unique to this story.

## Deferred from: code review of 2-9-content-tree-crud-backend-live-wire-editor (2026-08-12)

- Sibling `Order` assignment (`siblings.Max(x => x.Order) + 1`) in `AddChapterAsync`/`AddTopicAsync`/`AddSubtopicAsync`/`AddContentBlockAsync` races under concurrent adds (double-click, two open tabs) — two near-simultaneous adds can both read the same max and insert duplicate `Order` values. Deferred: pre-existing pattern shared identically by `CourseThumbnail.Order`/`CourseService.AddThumbnailAsync` (Story 2.4), not introduced or worsened by this story; fixing it here alone without addressing the identical Thumbnail case would be inconsistent.
- `ReorderNodeAsync`/`SwapAdjacent` has a TOCTOU race between `FindNodeAsync` confirming a node exists and the subsequent sibling-list fetch — a node deleted by a concurrent request in that window makes the reorder silently no-op instead of surfacing a 404. Deferred: rare, low-impact concurrent-edit race with no data corruption (the reorder simply doesn't happen), not blocking.
- `CourseContentEditor.test.tsx`'s fake-backend mocks (`reorderNode`/`moveNode`) only search `chapter.topics` for a matching id, so Chapter/Subtopic/ContentBlock-level reorder/move have no UI-integration-level test coverage in this file; the `addChapter`/`addTopic`/`addSubtopic`/`addContentBlock` mocks also return a canned DTO without mutating the shared `fixtures`, so the "adds" half of AC#1 isn't verified end-to-end here either. Deferred: the underlying service-call arguments and `parentType`-resolution logic are already covered at the hook level in `useCourseContentTree.test.ts`; this is a coverage-symmetry nice-to-have, not a verified gap in actual behavior.
- `FindNodeAsync` (`ContentTreeRepository.cs`) performs up to 4 sequential DB round-trips (chapters, then a topics-join, then a subtopics-join, then a content-blocks query plus an ownership sub-query) instead of one combined query, and nearly every mutator calls it once or twice. Deferred: matches this story's own stated "cheap at this entity count/course scale" design intent (Task 3), consistent with AD-18's "single container, moderate volume" framing used elsewhere in this codebase; revisit if course trees grow large enough for this to become measurable.

## Deferred from: code review of 2-10-subject-aware-alt-text-language-tagging (2026-08-12)

- Silent best-effort alt-text failure gives no signal to the tutor beyond the alt-text field now being visible and empty/unchanged — already an explicitly named, accepted tradeoff in this story's own Dev Notes ("some Content Blocks may end up with `AltText = null` if generation failed... a future story could add a 'regenerate missing alt-text' batch/retry mechanism if this becomes a real problem in practice — not built here, since no AC requires it"). Re-surfaced by this review for visibility, not as a new gap.
- Indirect prompt-injection risk: `proposed.Notation` for a materialized Math block originates from AI-parsed, tutor-uploaded document content (Story 2.8's extraction) and is fed unsanitized into `NotationDescriptionPromptBuilder.BuildMessages` as the "user" message, with whatever `DescribeNotationAsync` returns saved as `AltText` and rendered to students with no human review gate visible in this diff. Deferred: same class of already-deferred risk as Story 2.8's own "Uploaded document content flows into the AI extraction prompt with no sanitization against prompt injection" finding (no clear code-level fix exists generally; mitigated by the staged-not-materialized design and this being low-value injection surface), now extended to a second AI-Task prompt.
- Generated alt-text carries no language hint of its own — the new alt-text `EditableField` (`ContentTreeNode.tsx`) never receives a `lang` prop the way the Text-format field does, and `NotationDescriptionPromptBuilder`'s prompt/examples are English-only regardless of the course's language. Deferred: a legitimate enhancement, but no AC requires unifying this with AC#2's scope (Hindi/Devanagari *text* content blocks) — math/chemistry notation is not itself Hindi text, and describing it in Hindi for a Hindi-medium course is a real but separate future idea, not something this story's ACs ask for.

## Deferred from: code review of 4-1-correlation-id-assignment-and-propagation (2026-08-13)

- No integration-level test exercises the real composed `Program.cs` pipeline (middleware order, DI wiring for `ICorrelationIdAccessor`/`CorrelationIdMiddleware`). Deferred: pre-existing, repo-wide gap — no `WebApplicationFactory<Program>`-based test infrastructure exists anywhere in this codebase yet (confirmed: every existing `FlexDemy.Api.Tests` file constructs controllers/middleware directly). Building that infrastructure is a real investment better scoped as its own task than bolted onto this story; until then, an accidental removal/reorder of this middleware or its DI registration would not be caught by any test.
- All 4 Hangfire job interfaces (`IScanFileJob`/`IParseFileJob`/`IExtractStructureJob`/`IPublishNodeContentJob`) gained a new required `string? correlationId` parameter inserted mid-signature. Any job instance already serialized in Hangfire's Postgres-backed job store before this deploys (mid-retry or delayed) was queued against the old 3-parameter signature. Deferred: this is a general Hangfire-signature-versioning concern that applies to any future change to these same job classes (Story 4.3 will touch them again for error-capture wiring), not unique to this diff, and the correct handling depends on deployment posture (is there a live queue with in-flight jobs right now?) that's a project/ops decision outside a single story's scope — revisit as a documented deploy practice (e.g. drain the Hangfire queue before deploying a job-signature change) before this project's first production deployment, consistent with the backend architecture spine's own already-deferred "Production migration strategy" item.

## Deferred from: code review of 4-2-errorrecord-data-model-and-centralized-capture-service (2026-08-13)

- `ErrorRedactor`'s provider-key regex (`gsk_`/`sk_`/`AIza`) doesn't cover hyphenated real-world secret-key formats (OpenAI's `sk-...`, Anthropic's `sk-ant-...`). Deferred: the PRD's own Open Question #8 ("Is the starting secret-pattern list... enough for free-text redaction, or does it need to cover more provider-specific key formats?") already flags exactly this gap as pending product confirmation — expanding the pattern list unilaterally risks false positives against ordinary hyphenated text without that confirmation.
- `ErrorPriorityAssigner`'s Phase B spike-threshold formula (beyond the off-by-one mutation-ordering bug fixed in this review round) is an approximation of the PRD's literal "10x the prior 24h average within a 1-hour window," derived instead from the record's own lifetime average interval since no per-occurrence timestamp history is stored. Deferred: the PRD's own Open Question #6 already flags the spike-frequency threshold value itself as unconfirmed pending real occurrence-volume data — not resolvable by code inspection alone.
- `ErrorCaptureService.CaptureAsync` performs a synchronous `await`ed DB round-trip (fingerprint lookup + insert/update) directly on the caller's request/job thread, no queueing or fire-and-forget dispatch. Deferred (user-confirmed 2026-08-13): accept the current synchronous-but-fast approach for now, matching the PRD's own Open Question #4 working assumption ("fire-and-forget acceptable for an internal admin tool"); revisit with real latency measurements against AC #6's ~50ms p99/~200ms job budget if it's ever found violated in practice, before building unscoped background-dispatch infrastructure.

## Deferred from: code review of 4-3-backend-error-capture-wiring (2026-08-14)

- Every exception thrown before a job's own `try` block (`NotFoundException` from the opening `GetByIdAsync` lookups in all 4 jobs, `PublishNodeContentJob`'s `InvalidOperationException` for a missing `TopicId`/`SubtopicId`) is invisible to Story 4.3's capture wiring — Hangfire's own `[AutomaticRetry]` still retries these at the framework level, but after exhausting retries there's no code path to write anything at all, not even the existing `CourseFile.FailureReason`/`PublishBatchItem.ProgressText` fields. Deferred: this blind spot predates Story 4.3 (the per-entity failure fields have the identical gap today, unrelated to error capture) — fixing it requires wrapping each job's entire `RunAsync` body, not just the existing manual-retry-tracking `try` block, a bigger restructure than a wiring story's scope.
- `PublishNodeContentJob`'s finalize step (`DecrementRemainingAsync`/`CreateSnapshotAsync`/`MarkPublishedAsync`) runs entirely outside the `try`/`catch` that has the `MarkFailedAsync`/`CaptureAsync` wiring — if it throws, there's no capture at any retry, including the final exhausted attempt. Deferred: this is Story 3.8's original design, whose extensive existing code-review commentary already documents why the decrement/finalize step is structured this way for retry-safety; reopening it risks reintroducing the exact race that design already fixed once, and is a bigger change than this story's error-capture-wiring scope.

## Deferred from: code review of 4-4-frontend-global-error-capture-and-reporting-endpoint (2026-08-14)

- The module-level `currentCorrelationId` in `httpClient.ts` holds only the most-recently-seen value ("last response wins"), so under concurrent in-flight requests an error report can be tagged with an unrelated request's correlation ID. Deferred: this is the literal, deliberate frontend AD-7 architecture decision (a single module-level store, not per-request state) — revisiting it means reopening an already-ratified architecture decision, out of this story's scope.
- `ErrorReportingRateLimiterPolicy.GetPartition`'s per-IP key collapses to a shared `"unknown"` bucket when `RemoteIpAddress` is null, and collapses to one shared budget for any clients behind the same NAT/reverse proxy. Deferred: a real limitation of "per source IP" limiting generally; properly fixing the proxy case requires `ForwardedHeadersMiddleware` with an explicit trusted-proxy configuration decision (blindly trusting `X-Forwarded-For` without restricting to known proxies is itself a spoofing risk) — a deployment-topology decision bigger than this story's rate-limiting scope, and `docker-compose.yml` puts no reverse proxy in front of the API today.
- Unauthenticated `Message`/`Stack`/`Url` are persisted with no sanitization for future rendering. Deferred: not a risk in this story — nothing renders these fields yet (Story 4.5 builds the admin error-log viewer), and React's default JSX escaping means no code path is unsafe unless a future admin UI deliberately opts into `dangerouslySetInnerHTML`. Flagged here so Story 4.5's own code review re-checks this once that UI exists.
- `globalErrorHandlers.ts`/`ErrorBoundary.tsx` have no dedup/debounce for a tight loop of repeated identical errors, which could flood the reporting endpoint. Deferred: a real resilience enhancement, but no AC requires it, and the (patched) rate limiter already provides basic flood protection at 30/min/IP.

## Deferred from: code review of 4-5-admin-error-log-list-filter-and-detail (2026-08-14)

- **Re-check of Story 4.4's own deferred item on unsanitized `Message`/`Stack`/`Url` rendering**: confirmed resolved. `ErrorLogTable.tsx`/`ErrorDetailPanel.tsx` (this story's admin UI, the first thing to render these fields) render `message`/`stackTrace`/etc. as plain JSX text content everywhere — no `dangerouslySetInnerHTML` usage anywhere in `features/Admin/ErrorLog/` (confirmed via grep) — so React's default escaping protects against the stored-XSS risk Story 4.4 flagged as a future concern for whichever UI first rendered these fields. No action needed.
- No dedicated unit tests for `ErrorLogFilters.tsx`, `ErrorLogTable.tsx`, `ErrorDetailPanel.tsx`, or `Pagination.tsx` as standalone components. Deferred: covered indirectly through `ErrorLog.test.tsx`'s render/interaction tests; a reasonable future enhancement (debounce internals, pagination boundary math, badge fallback colors), not a gap in this story's own AD-5 requirement.
- `ErrorRecordRepository`'s `Search` filter is only tested against the EF Core InMemory provider, not the real relational provider. Deferred: matches this codebase's own stated, pre-existing testing boundary (`BackEnd/CLAUDE.md`), and unlike `EF.Functions.ILike` (deliberately avoided here for a known Npgsql-translation bug), plain `.ToLower().Contains()` has no provider-specific translation quirk to begin with.
- `ErrorRecordMapper.Truncate` slices by UTF-16 code unit, which can split a surrogate pair (e.g. an emoji) in half at the 200-char boundary. Deferred: low-probability, cosmetic-only (a slightly malformed truncated preview string; the full correct `Message` is always available in the Detail panel) — not worth grapheme-aware truncation complexity for an internal admin tool's list-row preview.
- Frontend renders `ErrorLog` for any `activeSubTab === 'errors'` with no client-side role re-check of its own, relying on `useAdminPanel`'s tab-list filtering plus the backend's `[Authorize(Policy = FeatureKeys.ErrorsManage)]` as the real gate. Deferred: identical, pre-existing pattern every other admin sub-tab already uses — not a new gap.
- No seed-data/migration-level test verifies the `errors.manage` `RolePermissionSeed` row itself gets applied. Deferred: no `FeatureKey` in this codebase has this kind of test today — not a gap specific to this story.
- `ErrorsController.GetList`'s enum-typed query fields have no test for an invalid/garbage value. Deferred: ASP.NET Core's default model-binding behavior for an invalid enum on a nullable property is a framework-level concern (typically binds to `null`) — low risk, nice-to-have coverage.
- AC #4's "link back to the originating record" renders as plain text, not a functional link. Deferred: the story's own explicitly pre-authorized `[ASSUMPTION]` fallback — no concrete per-entity-type route was confirmed at build time; revisit once one exists.

## Deferred from: code review of 4-6-error-lifecycle-actions (2026-08-14)

- `GetPurgeCandidatesAsync`/`RemoveRange` load and delete everything in one unbatched pass, with no chunking. Deferred: no evidence of scale need yet at this app's current volume — revisit if the Resolved/Archived backlog grows large enough for this to become a real single-transaction/memory concern.
- No lower-bound safety net on `RetentionDays` beyond `> 0` — a fat-fingered low value (e.g. `1`) is accepted with no confirmation and permanently deletes nearly the entire dismissed-error history on the next purge run. Deferred: a UX/product safeguard (e.g. a confirmation dialog below some threshold), not a correctness defect — no AC specifies this, and the underlying validation (`> 0`) is correct.
- No optimistic-concurrency protection across concurrent lifecycle actions on the same `ErrorRecord` (e.g. two admins simultaneously Archive one record and Increase-Priority the same one — the later write silently wins). Deferred: a systemic gap with no existing precedent anywhere else in this codebase's admin write paths either — not unique to this story, and the practical exposure (two Master admins racing on the exact same record) is low.

## Deferred from: code review of 4-7-correlation-id-trace-view (2026-08-14)

- The `CorrelationId` exact-match filter is case-sensitive with no normalization, unlike the adjacent `Search` filter's `.ToLower()`. Deferred: judged as correct-as-specified rather than a bug — AC #2 says "exact match," Correlation IDs are opaque system-generated tokens rather than human-typed free text, and the primary interaction path (clicking a Correlation ID) always passes the exact stored casing by construction; only the secondary manual-entry filter field is exposed to a hand-typed casing mismatch.
- Latent max-length mismatch: `CorrelationIdValidator.MaxLength` allows inbound values up to 128 characters, but `ErrorRecordConfiguration` caps the persisted `CorrelationId` column at `HasMaxLength(64)`. Deferred: pre-existing gap from Story 4.1's original schema, not introduced by this story — this story is simply the first to round-trip the value through a user-facing filter, surfacing the mismatch's implications for the first time. Fixing the ingestion-side validation/persistence boundary is out of this story's scope.

## Deferred from: code review of 5-1-relocate-the-new-course-wizard-trigger-into-my-courses (2026-08-15)

- New Course Wizard button isn't disabled while the wizard itself is already open (`MyCoursesSection.tsx`'s `disabled={isContentEditorOpen}` doesn't also check `courseCreationFlow.isNewCourseWizardOpen`, which already exists and is passed to the wizard's own `isOpen` prop elsewhere). Deferred: pre-existing behavior carried over unchanged from the old `TeachingStatsCards.tsx` trigger card this story relocated — not introduced by this diff.
- `Plus` icon on the relocated trigger has no `aria-hidden="true"`, risking double-announcement by screen readers alongside the adjacent button text. Deferred: this exact pattern (icon + text button, no `aria-hidden`) already exists unaddressed elsewhere in the codebase — a systemic gap, not specific to this story's rewritten button.
- Disabled New Course Wizard button has no `title`/tooltip explaining why it's disabled. Deferred: carried over unchanged from the prior implementation, not introduced by this story.

## Deferred from: combined code review of 6-1/6-2/6-3 (Settings, Font Pairing, Change History) (2026-08-16)

- AC #5's "IsActive false → falls back to hardcoded default" behavior is correctly implemented and unit-tested, but no code path anywhere in Stories 6.1-6.3 ever sets a Setting's `IsActive` to `false` (no deactivate/decurate endpoint exists) — currently unreachable against real data. Deferred: no AC requires a deactivate control to exist yet; the underlying logic is correct and ready whenever one is built.
- TOCTOU: the Font-pairing curation check (`FontPairingDefinitionRepository.GetBySlugAsync`) runs before `SettingsService.ApplyAsync`'s transaction opens, against a row the `FOR UPDATE` lock doesn't cover — a pairing could theoretically be decurated between the check and the commit. Deferred: no decurate endpoint exists anywhere in this diff for this race to be reachable today.
- `SettingRepository.ApplyValueAsync`'s raw SQL (`SELECT ... FOR UPDATE` / `UPDATE`) bypasses `SettingConfiguration`'s `HasQueryFilter(!IsDeleted)` — query filters only apply to LINQ, never raw SQL. Deferred: no delete endpoint exists for `Setting` anywhere in this diff, so no soft-deleted row can currently reach this path.
- No FK relationship (`HasOne`/`WithMany`) ties `SettingChangeHistory.SettingId` back to `Settings` — it's an indexed plain `string` column. Deferred: no delete path exists anywhere for `Setting`, so no orphaning risk exists today.
- `SiteSettingsContext`'s `refetch()` has no guard against a stale in-flight fetch resolving after a newer one and overwriting it. Deferred: only ever triggered by a single deliberate user action (a successful Apply), not a rapid-fire path.
- `Settings.tsx` doesn't refetch the font-pairings list after a successful Apply, so a concurrent curation-list change elsewhere would go stale until reload. Deferred: no admin-facing curate/decurate UI exists yet for this to matter in practice.
- `SettingChangeHistoryRepository`'s `OrderByDescending(h => h.CreatedAt)` has no tiebreaker for two rows sharing an identical timestamp, making the reverse-chronological history order non-deterministic in that edge case. Deferred: requires two applies to the same Setting within the same tick — effectively unreachable for a single-click admin action.
- The closure-based `oldValue`/`updatedAt` capture in `SettingsService.ApplyAsync`'s `ExecuteInTransactionAsync` lambda (mutable locals assigned via closure rather than returned from the lambda) is a latent hazard only if execution-strategy retry is ever configured (it isn't today — `UnitOfWork.cs`'s own comment documents why current behavior is safe). Deferred: `IUnitOfWork.ExecuteInTransactionAsync` only accepts `Func<Task>`, not `Func<Task<T>>` — a clean fix means widening a shared interface for a cosmetic-only improvement; revisit if that interface is ever widened for another reason.
- Authorization tests across `SettingsControllerTests.cs` (pre-existing, systemic pattern) mostly exercise a hand-built `IAuthorizationService` in isolation rather than the real ASP.NET Core pipeline end-to-end — this is exactly the kind of gap that let the critical anonymous-access bug (see story files' own Review Findings) ship undetected through three prior code-review passes. The new `effective-fonts` endpoint's own test asserts directly on attribute metadata as a partial mitigation, but the broader gap (no `WebApplicationFactory`-based integration test infrastructure exists anywhere in this codebase) remains, first flagged from Story 4.1's review and still open.
- Seeder boilerplate: `EnsureSettingsAsync`/`EnsureFontPairingDefinitionsAsync` in `DatabaseSeeder.cs` are near-identical copies of the same "fetch existing keys → filter missing → loop-add → SaveChangesAsync" shape, un-factored into a shared helper. Deferred: consistent with this file's existing precedent (each entity's seeder already follows this shape independently), not a regression introduced here.

## Deferred from: code review of 6-4-curated-font-size-scale-with-the-same-preview-apply-history-flow (2026-08-16)

- `SettingsController.GetEffectiveFonts` has no `Cache-Control` header despite being hit on every page load by every anonymous visitor, resulting in a full API+DB round-trip for two mostly-static values every time. Deferred: a real caching strategy needs its own design (cache invalidation on Apply, TTL choice) beyond a mechanical patch.
- `SiteSettingsContext`'s `cancelled` flag only gates `setIsLoading(false)`, not the `root.style.setProperty(...)` calls inside `applyFontPairing()` — two overlapping `refetch()`-triggered fetches could resolve out of order and leave a stale value applied. Deferred: same class of race already deferred during Epic 6's combined review (low practical likelihood, only triggered by two rapid-fire admin Apply actions).
- `FontSizePicker` duplicates ~180 lines of `FontPairingPicker`'s structure (candidate/history/apply state and handlers) with only the preview markup differing. Deferred: exactly two picker instances exist today; extracting a shared hook/component now is premature abstraction risk against marginal benefit — revisit if a third picker is ever added.
- `Setting`/`FontPairingDefinition`/`FontSizeDefinition`'s `"Font"`/`"FontSize"`/`"font.pairing"`/`"font.size"` identifiers are duplicated as magic strings across `HardcodedDefaults`, `ApplyAsync`'s KeyType branches, `DatabaseSeeder`'s seed arrays, and `GetEffectiveFontsAsync`, with nothing enforcing they stay in sync. Deferred: a real fix means introducing constants/enums across the whole Settings feature spanning Stories 6.1-6.4, out of scope for a single story.
- `FontSizeDefinition.RootFontScale` has no format validation anywhere — a malformed curated value (typo, non-percentage CSS) would reach the live site with only the browser silently ignoring it as a guardrail. Deferred: matches the PRD's own Non-Goal ("editing a KeyType's curated list content through the Settings UI... not built here") — no admin UI exists yet to create a malformed row in the first place.

## Deferred from: code review of 6-5-combined-typography-presets-with-custom-fallback (2026-08-16)

- No test exercises a real Postgres transaction rollback for `ApplyTypographyCombinationAsync`'s two-write sequence — InMemory can't run raw SQL at all, and no `WebApplicationFactory`/Testcontainers-based integration test infrastructure exists anywhere in this codebase. Deferred: matches the standing action item (first flagged from Story 4.1's review, re-flagged during Epic 6's combined review) for building real integration-test infrastructure — not this single story's job.
- No UI mutual exclusion between the Typography preset grid's busy state and the Custom panel's independent `FontPairingPicker`/`FontSizePicker` busy state — both write to the same two Settings with no shared in-flight indicator. Deferred: same class of low-probability UI race already deferred for `SiteSettingsContext`'s `refetch()`; DB-level locking still prevents corruption, the risk is a silent last-write-wins with no user feedback, not data loss.
- `("font.pairing","Font")`/`("font.size","FontSize")` are now hardcoded independently in `SettingsService.HardcodedDefaults` and in `ApplyTypographyCombinationAsync`'s Setting-row lookup, extending the same magic-string-duplication gap already deferred from Story 6.4's review. Deferred: a real fix means constants/enums across the whole Settings feature, out of scope for one story.
- `SettingsControllerTests`' `Every_other_action_does_NOT_carry_AllowAnonymous` theory enumerates action names by string rather than reflecting over all controller actions, so a future new endpoint (as this story's own two were) is only covered if someone remembers to add it to the `[InlineData]` list. Deferred: a real fix is a test-infrastructure investment, same category as the integration-test gap above.
- `ApplyTypographyCombinationAsync`'s combo/pairing/size curation checks run before the transaction opens with no re-check inside it — a concurrent decuration between the check and the commit could let a stale-curated value through. Deferred: matches the identical, already-deferred TOCTOU pattern from Stories 6.2/6.4's own `ApplyAsync` curation checks.
- `Settings.tsx`'s `otherGroups` filtering removes rows by matching `fontSetting`/`fontSizeSetting`'s specific id rather than deleting the whole KeyType group — behavior for a hypothetical second Setting row sharing KeyType `'Font'`/`'FontSize'` hasn't been explicitly verified. Deferred: currently unreachable, exactly one Setting row exists per Font/FontSize KeyType today.
- `DatabaseSeeder.SeedAsync`'s sequential, un-transacted `Ensure*Async` calls mean a crash between `EnsureSettingsAsync` and `EnsureFontPairingDefinitionsAsync`/`EnsureFontSizeDefinitionsAsync`/`EnsureTypographyCombinationsAsync` could leave a seeded row referencing a curated definition that doesn't exist yet until the seeder reruns. Deferred: pre-existing sequencing from Story 6.1, systemic across every seeder in this file, not introduced by this story.
