<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- FlexDemy mark: same glyph as src/ui/Logo.tsx, on its amber badge. Hex values are
       literal copies of the ink-navy / citrus-amber tokens from src/index.css: this file
       is a standalone document (browser favicon chrome) with no access to the app's CSS
       custom properties, so the tokens can't be referenced directly and must be kept in
       sync by hand if the palette ever changes. -->
  <rect width="32" height="32" rx="7" fill="#EC7B38"/>
  <g transform="translate(4,4)" fill="#143358">
    <path d="M11 19L4 16L3 7L11 4Z"/>
    <path d="M13 19L20 16L21 7L13 4Z"/>
    <circle cx="12" cy="2.4" r="1.3"/>
  </g>
</svg>
